generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  agent
  office_manager
  office_user
  office_admin
}

enum MembershipStatus {
  active
  invited
  disabled
}

enum ListingStatus {
  draft
  active
  hot
  pending
  off_market
  sold
}

enum TaskStatus {
  queued
  in_progress
  completed
  canceled
}

enum NotificationType {
  system
  listing
  follow_up
  event
  task_review_requested
  task_second_review_requested
  task_rejected
  offer_created
  offer_received
  offer_expiring_soon
  signature_pending
  signature_completed
  incoming_update_pending_review
  follow_up_assigned
  follow_up_overdue
  onboarding_assigned
  onboarding_due_soon
}

enum NotificationCategory {
  system
  task
  offer
  signature
  incoming_update
  follow_up
  onboarding
  event
}

enum NotificationSeverity {
  info
  warning
  critical
}

enum NotificationEntityType {
  follow_up_task
  transaction_task
  offer
  signature_request
  incoming_update
  agent_onboarding_item
  event
}

enum EventVisibility {
  all_agents
  office_only
  invite_only
}

enum RsvpStatus {
  going
  maybe
  declined
}

enum ResourceType {
  training_video
  vendor_card
  document
  template
  playbook
}

enum LibraryDocumentVisibility {
  company_wide
  office_only
}

enum TransactionType {
  sales
  sales_listing
  rental_leasing
  rental_listing
  commercial_sales
  commercial_lease
  other
}

enum TransactionStatus {
  opportunity
  active
  pending
  closed
  cancelled
}

enum TransactionRepresenting {
  buyer
  seller
  both
  tenant
  landlord
}

enum TransactionContactRole {
  buyer
  seller
  co_buyer
  co_seller
  tenant
  landlord
  other
}

enum TransactionTaskStatus {
  todo
  in_progress
  review_requested
  completed
  reopened
}

enum TransactionTaskReviewStatus {
  not_required
  pending
  review_requested
  first_approved
  second_review
  approved
  rejected
}

enum TransactionTaskComplianceStatus {
  not_applicable
  pending
  in_review
  approved
  rejected
}

enum TransactionDocumentStatus {
  uploaded
  submitted
  approved
  rejected
  signed
  archived
}

enum TransactionDocumentSource {
  manual_upload
  generated_form
  incoming_update
  synced_external
  email_pdf
}

enum TransactionFormStatus {
  draft
  prepared
  sent_for_signature
  partially_signed
  fully_signed
  rejected
  voided
}

enum SignatureRequestStatus {
  draft
  sent
  viewed
  signed
  declined
  canceled
}

enum IncomingUpdateStatus {
  pending_review
  accepted
  rejected
  applied
}

enum OfferStatus {
  draft
  submitted
  received
  under_review
  countered
  accepted
  rejected
  withdrawn
  expired
}

enum LedgerAccountType {
  asset
  liability
  equity
  income
  expense
  contra_income
  contra_expense
}

enum AccountingTransactionType {
  invoice
  bill
  credit_memo
  deposit
  received_payment
  made_payment
  journal_entry
  transfer
  refund
}

enum AccountingTransactionStatus {
  draft
  open
  posted
  completed
  void
}

enum AccountingLineEntrySide {
  debit
  credit
}

enum AccountingPaymentMethod {
  ach
  check
  wire
  cash
  internal_transfer
  other
}

enum AgentBillingFrequency {
  monthly
  quarterly
  annual
  custom_interval
}

enum AgentPaymentMethodType {
  card_on_file
  bank_account
  check
  manual
  other
}

enum AgentPaymentMethodStatus {
  active
  inactive
  invalid
  expired
  removed
}

enum AgentOnboardingStatus {
  not_started
  in_progress
  complete
}

enum TeamMembershipRole {
  lead
  member
}

enum AgentOnboardingItemStatus {
  pending
  in_progress
  completed
  reopened
}

enum AgentGoalPeriodType {
  monthly
  quarterly
  annual
}

enum TransactionFieldKey {
  price
  important_date
  closing_date
  buyer_expiration_date
  acceptance_date
  company_referral
  company_referral_employee_name
}

enum CommissionCalculationMode {
  split_and_fees
  flat_net
}

enum CommissionPlanRuleType {
  base_split
  brokerage_fee
  referral_fee
  flat_fee_deduction
  sliding_scale
}

enum CommissionRuleFeeType {
  percentage
  flat
}

enum CommissionRecipientType {
  agent
  brokerage
  referral
}

enum CommissionCalculationStatus {
  draft
  calculated
  reviewed
  statement_ready
  payable
  paid
}

enum EarnestMoneyStatus {
  not_received
  overdue
  pending_bank_deposit
  fully_deposited
  distribute_balance
  complete
}

model Organization {
  id                                String                             @id @default(cuid())
  name                              String
  slug                              String                             @unique
  timezone                          String                             @default("America/New_York")
  createdAt                         DateTime                           @default(now())
  updatedAt                         DateTime                           @updatedAt
  offices                           Office[]
  memberships                       Membership[]
  invitations                       Invitation[]
  membershipNotificationPreferences MembershipNotificationPreference[]
  listings                          Listing[]
  clients                           Client[]
  followUpTasks                     FollowUpTask[]
  notifications                     Notification[]
  events                            Event[]
  resources                         Resource[]
  vendors                           Vendor[]
  libraryFolders                    LibraryFolder[]
  libraryDocuments                  LibraryDocument[]
  auditLogs                         AuditLog[]
  transactions                      Transaction[]
  transactionContacts               TransactionContact[]
  transactionTasks                  TransactionTask[]
  taskListViews                     TaskListView[]
  ledgerAccounts                    LedgerAccount[]
  accountingTransactions            AccountingTransaction[]
  accountingTransactionLineItems    AccountingTransactionLineItem[]
  accountingTransactionApplications AccountingTransactionApplication[]
  generalLedgerEntries              GeneralLedgerEntry[]
  earnestMoneyRecords               EarnestMoneyRecord[]
  agentRecurringChargeRules         AgentRecurringChargeRule[]
  agentPaymentMethods               AgentPaymentMethod[]
  transactionDocuments              TransactionDocument[]
  formTemplates                     FormTemplate[]
  transactionForms                  TransactionForm[]
  signatureRequests                 SignatureRequest[]
  incomingUpdates                   IncomingUpdate[]
  offers                            Offer[]
  offerComments                     OfferComment[]
  agentProfiles                     AgentProfile[]
  teams                             Team[]
  teamMemberships                   TeamMembership[]
  agentOnboardingTemplateItems      AgentOnboardingTemplateItem[]
  agentOnboardingItems              AgentOnboardingItem[]
  agentGoals                        AgentGoal[]
  commissionPlans                   CommissionPlan[]
  commissionPlanAssignments         CommissionPlanAssignment[]
  commissionPlanRules               CommissionPlanRule[]
  commissionCalculations            CommissionCalculation[]
  requiredContactRoleSettings       RequiredContactRoleSetting[]
  transactionFieldSettings          TransactionFieldSetting[]
  checklistTemplates                ChecklistTemplate[]
  checklistTemplateItems            ChecklistTemplateItem[]
}

model Office {
  id                                String                             @id @default(cuid())
  organizationId                    String
  name                              String
  slug                              String
  market                            String
  isPrimary                         Boolean                            @default(false)
  createdAt                         DateTime                           @default(now())
  updatedAt                         DateTime                           @updatedAt
  organization                      Organization                       @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  memberships                       Membership[]
  membershipNotificationPreferences MembershipNotificationPreference[]
  notifications                     Notification[]
  listings                          Listing[]
  events                            Event[]
  resources                         Resource[]
  vendors                           Vendor[]
  libraryFolders                    LibraryFolder[]
  libraryDocuments                  LibraryDocument[]
  transactions                      Transaction[]
  taskListViews                     TaskListView[]
  ledgerAccounts                    LedgerAccount[]
  accountingTransactions            AccountingTransaction[]
  accountingTransactionLineItems    AccountingTransactionLineItem[]
  accountingTransactionApplications AccountingTransactionApplication[]
  generalLedgerEntries              GeneralLedgerEntry[]
  earnestMoneyRecords               EarnestMoneyRecord[]
  agentRecurringChargeRules         AgentRecurringChargeRule[]
  agentPaymentMethods               AgentPaymentMethod[]
  transactionDocuments              TransactionDocument[]
  formTemplates                     FormTemplate[]
  transactionForms                  TransactionForm[]
  signatureRequests                 SignatureRequest[]
  incomingUpdates                   IncomingUpdate[]
  offers                            Offer[]
  offerComments                     OfferComment[]
  agentProfiles                     AgentProfile[]
  teams                             Team[]
  teamMemberships                   TeamMembership[]
  agentOnboardingTemplateItems      AgentOnboardingTemplateItem[]
  agentOnboardingItems              AgentOnboardingItem[]
  agentGoals                        AgentGoal[]
  commissionPlans                   CommissionPlan[]
  commissionPlanAssignments         CommissionPlanAssignment[]
  commissionCalculations            CommissionCalculation[]
  requiredContactRoleSettings       RequiredContactRoleSetting[]
  transactionFieldSettings          TransactionFieldSetting[]
  checklistTemplates                ChecklistTemplate[]
  checklistTemplateItems            ChecklistTemplateItem[]

  @@unique([organizationId, slug])
}

model User {
  id          String         @id @default(cuid())
  email       String         @unique
  firstName   String
  lastName    String
  phone       String?
  timezone    String         @default("America/New_York")
  locale      String         @default("en-US")
  isActive    Boolean        @default(true)
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt
  memberships Membership[]
  credential  UserCredential?
}

model Membership {
  id                                       String                             @id @default(cuid())
  organizationId                           String
  officeId                                 String?
  userId                                   String
  role                                     UserRole
  status                                   MembershipStatus                   @default(active)
  title                                    String?
  permissions                              Json?
  createdAt                                DateTime                           @default(now())
  updatedAt                                DateTime                           @updatedAt
  organization                             Organization                       @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                                   Office?                            @relation(fields: [officeId], references: [id], onDelete: SetNull)
  user                                     User                               @relation(fields: [userId], references: [id], onDelete: Cascade)
  invitations                              Invitation[]                       @relation("InvitationMembership")
  sentInvitations                          Invitation[]                       @relation("InvitationInvitedByMembership")
  notificationPreference                   MembershipNotificationPreference?
  listingsOwned                            Listing[]
  clientsOwned                             Client[]
  tasksAssigned                            FollowUpTask[]
  notifications                            Notification[]
  eventsCreated                            Event[]
  eventRsvps                               EventRsvp[]
  auditLogs                                AuditLog[]
  transactionsOwned                        Transaction[]
  transactionTasksAssigned                 TransactionTask[]                  @relation("TransactionTaskAssignee")
  transactionTasksCompleted                TransactionTask[]                  @relation("TransactionTaskCompletedBy")
  transactionTasksSubmittedForReview       TransactionTask[]                  @relation("TransactionTaskSubmittedForReviewBy")
  transactionTasksFirstApproved            TransactionTask[]                  @relation("TransactionTaskFirstApprovedBy")
  transactionTasksSecondApproved           TransactionTask[]                  @relation("TransactionTaskSecondApprovedBy")
  transactionTasksRejected                 TransactionTask[]                  @relation("TransactionTaskRejectedBy")
  taskListViews                            TaskListView[]
  accountingTransactionsCreated            AccountingTransaction[]            @relation("AccountingTransactionCreatedBy")
  accountingTransactionsRelated            AccountingTransaction[]            @relation("AccountingTransactionRelatedMembership")
  accountingTransactionApplicationsCreated AccountingTransactionApplication[] @relation("AccountingTransactionApplicationCreatedBy")
  earnestMoneyRecordsCreated               EarnestMoneyRecord[]               @relation("EarnestMoneyCreatedBy")
  agentRecurringChargeRules                AgentRecurringChargeRule[]         @relation("AgentRecurringChargeRuleMembership")
  agentPaymentMethods                      AgentPaymentMethod[]
  createdLibraryFolders                    LibraryFolder[]                    @relation("LibraryFolderCreatedBy")
  uploadedTransactionDocuments             TransactionDocument[]              @relation("TransactionDocumentUploadedBy")
  uploadedLibraryDocuments                 LibraryDocument[]                  @relation("LibraryDocumentUploadedBy")
  createdTransactionForms                  TransactionForm[]                  @relation("TransactionFormCreatedBy")
  requestedSignatureRequests               SignatureRequest[]                 @relation("SignatureRequestRequestedBy")
  reviewedIncomingUpdates                  IncomingUpdate[]                   @relation("IncomingUpdateReviewedBy")
  createdOffers                            Offer[]                            @relation("OfferCreatedBy")
  offerComments                            OfferComment[]
  agentProfile                             AgentProfile?
  teamMemberships                          TeamMembership[]
  agentOnboardingItems                     AgentOnboardingItem[]
  completedAgentOnboardingItems            AgentOnboardingItem[]              @relation("AgentOnboardingCompletedBy")
  agentGoals                               AgentGoal[]
  commissionPlanAssignments                CommissionPlanAssignment[]
  commissionCalculations                   CommissionCalculation[]            @relation("CommissionCalculationMembership")
  calculatedCommissionCalculations         CommissionCalculation[]            @relation("CommissionCalculationCalculatedBy")
  checklistTemplatesCreated                ChecklistTemplate[]                @relation("ChecklistTemplateCreatedBy")
  checklistTemplatesUpdated                ChecklistTemplate[]                @relation("ChecklistTemplateUpdatedBy")

  @@unique([organizationId, userId])
}

model UserCredential {
  id                String   @id @default(cuid())
  userId            String   @unique
  passwordHash      String
  mustChangePassword Boolean @default(false)
  failedLoginCount  Int      @default(0)
  lockedUntil       DateTime?
  lastLoginAt       DateTime?
  lastFailedLoginAt DateTime?
  passwordChangedAt DateTime?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Invitation {
  id                   String      @id @default(cuid())
  organizationId       String
  membershipId         String
  email                String
  tokenHash            String      @unique
  expiresAt            DateTime
  acceptedAt           DateTime?
  revokedAt            DateTime?
  invitedByMembershipId String?
  createdAt            DateTime    @default(now())
  updatedAt            DateTime    @updatedAt
  organization         Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  membership           Membership   @relation("InvitationMembership", fields: [membershipId], references: [id], onDelete: Cascade)
  invitedByMembership  Membership?  @relation("InvitationInvitedByMembership", fields: [invitedByMembershipId], references: [id], onDelete: SetNull)

  @@index([organizationId, membershipId])
  @@index([organizationId, email])
  @@index([organizationId, expiresAt])
}

model MembershipNotificationPreference {
  id                    String       @id @default(cuid())
  organizationId        String
  officeId              String?
  membershipId          String       @unique
  inAppEnabled          Boolean      @default(true)
  approvalAlertsEnabled Boolean      @default(true)
  taskRemindersEnabled  Boolean      @default(true)
  offerAlertsEnabled    Boolean      @default(true)
  createdAt             DateTime     @default(now())
  updatedAt             DateTime     @updatedAt
  organization          Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                Office?      @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership            Membership   @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@index([organizationId, officeId])
  @@index([organizationId, membershipId])
}

model Transaction {
  id                             String                          @id @default(cuid())
  organizationId                 String
  officeId                       String?
  ownerMembershipId              String?
  primaryClientId                String?
  type                           TransactionType
  status                         TransactionStatus               @default(opportunity)
  representing                   TransactionRepresenting
  title                          String
  address                        String
  city                           String
  state                          String
  zipCode                        String
  price                          Decimal?                        @db.Decimal(12, 2)
  importantDate                  DateTime?
  buyerAgreementDate             DateTime?
  buyerExpirationDate            DateTime?
  acceptanceDate                 DateTime?
  listingDate                    DateTime?
  listingExpirationDate          DateTime?
  closingDate                    DateTime?
  companyReferral                Boolean                         @default(false)
  companyReferralEmployeeName    String?
  referralContext                Json?
  grossCommission                Decimal?                        @db.Decimal(12, 2)
  referralFee                    Decimal?                        @db.Decimal(12, 2)
  officeNet                      Decimal?                        @db.Decimal(12, 2)
  agentNet                       Decimal?                        @db.Decimal(12, 2)
  financeNotes                   String?
  commissionContext              Json?
  additionalFields               Json?
  createdAt                      DateTime                        @default(now())
  updatedAt                      DateTime                        @updatedAt
  organization                   Organization                    @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                         Office?                         @relation(fields: [officeId], references: [id], onDelete: SetNull)
  ownerMembership                Membership?                     @relation(fields: [ownerMembershipId], references: [id], onDelete: SetNull)
  primaryClient                  Client?                         @relation("TransactionPrimaryClient", fields: [primaryClientId], references: [id], onDelete: SetNull)
  transactionContacts            TransactionContact[]
  tasks                          TransactionTask[]
  accountingTransactions         AccountingTransaction[]
  accountingTransactionLineItems AccountingTransactionLineItem[]
  generalLedgerEntries           GeneralLedgerEntry[]
  earnestMoneyRecords            EarnestMoneyRecord[]
  documents                      TransactionDocument[]
  forms                          TransactionForm[]
  signatureRequests              SignatureRequest[]
  incomingUpdates                IncomingUpdate[]
  offers                         Offer[]
  commissionCalculations         CommissionCalculation[]

  @@index([organizationId, status])
  @@index([organizationId, officeId])
  @@index([organizationId, ownerMembershipId])
  @@index([organizationId, primaryClientId])
}

model Listing {
  id                String             @id @default(cuid())
  organizationId    String
  officeId          String?
  ownerMembershipId String?
  title             String
  slug              String
  sourceUrl         String?
  status            ListingStatus      @default(draft)
  price             Decimal?           @db.Decimal(12, 2)
  city              String
  neighborhood      String
  bedrooms          Int?
  bathrooms         Decimal?           @db.Decimal(4, 1)
  isPublic          Boolean            @default(false)
  heroFacts         Json?
  seoKeywords       String[]
  aiSummary         String?
  parsedJson        Json?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  organization      Organization       @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office            Office?            @relation(fields: [officeId], references: [id], onDelete: SetNull)
  ownerMembership   Membership?        @relation(fields: [ownerMembershipId], references: [id], onDelete: SetNull)
  assets            ListingAsset[]
  shareLinks        ListingShareLink[]

  @@unique([organizationId, slug])
  @@index([organizationId, status])
  @@index([organizationId, isPublic])
}

model ListingAsset {
  id        String   @id @default(cuid())
  listingId String
  assetType String
  label     String
  url       String
  sortOrder Int      @default(0)
  createdAt DateTime @default(now())
  listing   Listing  @relation(fields: [listingId], references: [id], onDelete: Cascade)
}

model ListingShareLink {
  id           String   @id @default(cuid())
  listingId    String
  membershipId String?
  channel      String
  code         String   @unique
  targetUrl    String
  clickCount   Int      @default(0)
  qrAssetUrl   String?
  createdAt    DateTime @default(now())
  listing      Listing  @relation(fields: [listingId], references: [id], onDelete: Cascade)
}

model Client {
  id                  String               @id @default(cuid())
  organizationId      String
  ownerMembershipId   String?
  fullName            String
  email               String?
  phone               String?
  contactType         String?
  source              String
  stage               String
  intent              String
  budgetMin           Decimal?             @db.Decimal(12, 2)
  budgetMax           Decimal?             @db.Decimal(12, 2)
  preferredAreas      String[]
  needsSummary        Json?
  notes               String?
  lastContactAt       DateTime?
  nextFollowUpAt      DateTime?
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
  organization        Organization         @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  ownerMembership     Membership?          @relation(fields: [ownerMembershipId], references: [id], onDelete: SetNull)
  followUpTasks       FollowUpTask[]
  primaryTransactions Transaction[]        @relation("TransactionPrimaryClient")
  transactionContacts TransactionContact[]

  @@index([organizationId, stage])
}

model TransactionContact {
  id             String                 @id @default(cuid())
  organizationId String
  transactionId  String
  clientId       String
  role           TransactionContactRole @default(other)
  isPrimary      Boolean                @default(false)
  notes          String?
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  organization   Organization           @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  transaction    Transaction            @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  client         Client                 @relation(fields: [clientId], references: [id], onDelete: Cascade)

  @@unique([transactionId, clientId])
  @@index([organizationId, transactionId])
  @@index([organizationId, clientId])
  @@index([organizationId, transactionId, isPrimary])
}

model TransactionTask {
  id                               String                          @id @default(cuid())
  organizationId                   String
  transactionId                    String
  checklistGroup                   String                          @default("General")
  title                            String
  description                      String?
  assigneeMembershipId             String?
  dueAt                            DateTime?
  status                           TransactionTaskStatus           @default(todo)
  requiresDocument                 Boolean                         @default(false)
  requiresDocumentApproval         Boolean                         @default(false)
  requiresSecondaryApproval        Boolean                         @default(false)
  reviewStatus                     TransactionTaskReviewStatus     @default(not_required)
  complianceStatus                 TransactionTaskComplianceStatus @default(not_applicable)
  sortOrder                        Int                             @default(0)
  completedAt                      DateTime?
  completedByMembershipId          String?
  submittedForReviewAt             DateTime?
  submittedForReviewByMembershipId String?
  firstApprovedAt                  DateTime?
  firstApprovedByMembershipId      String?
  secondApprovedAt                 DateTime?
  secondApprovedByMembershipId     String?
  rejectedAt                       DateTime?
  rejectedByMembershipId           String?
  rejectionReason                  String?
  reopenedAt                       DateTime?
  createdAt                        DateTime                        @default(now())
  updatedAt                        DateTime                        @updatedAt
  organization                     Organization                    @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  transaction                      Transaction                     @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  assigneeMembership               Membership?                     @relation("TransactionTaskAssignee", fields: [assigneeMembershipId], references: [id], onDelete: SetNull)
  completedByMembership            Membership?                     @relation("TransactionTaskCompletedBy", fields: [completedByMembershipId], references: [id], onDelete: SetNull)
  submittedForReviewByMembership   Membership?                     @relation("TransactionTaskSubmittedForReviewBy", fields: [submittedForReviewByMembershipId], references: [id], onDelete: SetNull)
  firstApprovedByMembership        Membership?                     @relation("TransactionTaskFirstApprovedBy", fields: [firstApprovedByMembershipId], references: [id], onDelete: SetNull)
  secondApprovedByMembership       Membership?                     @relation("TransactionTaskSecondApprovedBy", fields: [secondApprovedByMembershipId], references: [id], onDelete: SetNull)
  rejectedByMembership             Membership?                     @relation("TransactionTaskRejectedBy", fields: [rejectedByMembershipId], references: [id], onDelete: SetNull)
  documents                        TransactionDocument[]
  forms                            TransactionForm[]

  @@index([organizationId, transactionId])
  @@index([organizationId, assigneeMembershipId])
  @@index([organizationId, transactionId, status])
  @@index([organizationId, transactionId, sortOrder])
  @@index([organizationId, dueAt])
  @@index([organizationId, complianceStatus])
  @@index([organizationId, reviewStatus])
}

model TaskListView {
  id             String       @id @default(cuid())
  organizationId String
  officeId       String?
  membershipId   String
  name           String
  isSystem       Boolean      @default(false)
  isShared       Boolean      @default(false)
  filters        Json
  visibleColumns Json?
  sort           Json?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office         Office?      @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership     Membership   @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@unique([organizationId, membershipId, name])
  @@index([organizationId, membershipId, officeId])
}

model FollowUpTask {
  id                 String         @id @default(cuid())
  organizationId     String
  clientId           String?
  assigneeMemberId   String?
  title              String
  status             TaskStatus     @default(queued)
  dueAt              DateTime?
  metadata           Json?
  createdAt          DateTime       @default(now())
  updatedAt          DateTime       @updatedAt
  organization       Organization   @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  client             Client?        @relation(fields: [clientId], references: [id], onDelete: SetNull)
  assigneeMembership Membership?    @relation(fields: [assigneeMemberId], references: [id], onDelete: SetNull)
  notifications      Notification[]

  @@index([organizationId, status])
}

model Notification {
  id             String           @id @default(cuid())
  organizationId String
  officeId       String?
  membershipId   String?
  followUpTaskId String?
  eventId        String?
  type           NotificationType
  category       NotificationCategory?
  severity       NotificationSeverity?
  entityType     NotificationEntityType?
  entityId       String?
  metadata       Json?
  title          String
  body           String
  actionUrl      String?
  readAt         DateTime?
  createdAt      DateTime         @default(now())
  organization   Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office         Office?          @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership     Membership?      @relation(fields: [membershipId], references: [id], onDelete: SetNull)
  followUpTask   FollowUpTask?    @relation(fields: [followUpTaskId], references: [id], onDelete: SetNull)
  event          Event?           @relation(fields: [eventId], references: [id], onDelete: SetNull)

  @@index([organizationId, type])
  @@index([organizationId, membershipId, officeId, createdAt])
  @@index([organizationId, membershipId, readAt, createdAt])
  @@index([organizationId, membershipId, category])
  @@index([organizationId, membershipId, entityType, entityId])
}

model Event {
  id                  String          @id @default(cuid())
  organizationId      String
  officeId            String?
  createdByMemberId   String?
  title               String
  description         String
  visibility          EventVisibility @default(all_agents)
  startsAt            DateTime
  endsAt              DateTime?
  location            String?
  meetingUrl          String?
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt
  organization        Organization    @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office              Office?         @relation(fields: [officeId], references: [id], onDelete: SetNull)
  createdByMembership Membership?     @relation(fields: [createdByMemberId], references: [id], onDelete: SetNull)
  rsvps               EventRsvp[]
  notifications       Notification[]

  @@index([organizationId, startsAt])
}

model EventRsvp {
  id           String     @id @default(cuid())
  eventId      String
  membershipId String
  status       RsvpStatus
  respondedAt  DateTime   @default(now())
  event        Event      @relation(fields: [eventId], references: [id], onDelete: Cascade)
  membership   Membership @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@unique([eventId, membershipId])
}

model Resource {
  id             String       @id @default(cuid())
  organizationId String
  officeId       String?
  type           ResourceType
  title          String
  slug           String
  summary        String
  url            String
  tags           String[]
  isPublished    Boolean      @default(true)
  searchText     String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office         Office?      @relation(fields: [officeId], references: [id], onDelete: SetNull)

  @@unique([organizationId, slug])
}

model Vendor {
  id             String       @id @default(cuid())
  organizationId String
  officeId       String?
  category       String
  name           String
  headline       String
  phone          String?
  email          String?
  website        String?
  neighborhoods  String[]
  notes          String?
  isFeatured     Boolean      @default(false)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office         Office?      @relation(fields: [officeId], references: [id], onDelete: SetNull)
}

model LibraryFolder {
  id                    String            @id @default(cuid())
  organizationId        String
  officeId              String?
  parentFolderId        String?
  createdByMembershipId String?
  name                  String
  description           String?
  sortOrder             Int               @default(0)
  isActive              Boolean           @default(true)
  createdAt             DateTime          @default(now())
  updatedAt             DateTime          @updatedAt
  organization          Organization      @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                Office?           @relation(fields: [officeId], references: [id], onDelete: SetNull)
  parentFolder          LibraryFolder?    @relation("LibraryFolderParent", fields: [parentFolderId], references: [id], onDelete: SetNull)
  childFolders          LibraryFolder[]   @relation("LibraryFolderParent")
  createdByMembership   Membership?       @relation("LibraryFolderCreatedBy", fields: [createdByMembershipId], references: [id], onDelete: SetNull)
  documents             LibraryDocument[]

  @@index([organizationId, officeId, isActive, sortOrder])
  @@index([organizationId, parentFolderId, isActive, sortOrder])
}

model LibraryDocument {
  id                     String                    @id @default(cuid())
  organizationId         String
  officeId               String?
  folderId               String?
  uploadedByMembershipId String?
  title                  String
  originalFileName       String
  mimeType               String
  fileSizeBytes          Int
  storageKey             String
  pageCount              Int?
  summary                String?
  tags                   String[]
  category               String?
  visibility             LibraryDocumentVisibility @default(company_wide)
  sortOrder              Int                       @default(0)
  createdAt              DateTime                  @default(now())
  updatedAt              DateTime                  @updatedAt
  organization           Organization              @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                 Office?                   @relation(fields: [officeId], references: [id], onDelete: SetNull)
  folder                 LibraryFolder?            @relation(fields: [folderId], references: [id], onDelete: SetNull)
  uploadedByMembership   Membership?               @relation("LibraryDocumentUploadedBy", fields: [uploadedByMembershipId], references: [id], onDelete: SetNull)

  @@index([organizationId, officeId, folderId, createdAt])
  @@index([organizationId, officeId, category])
  @@index([organizationId, officeId, visibility])
  @@index([organizationId, officeId, updatedAt])
}

model AuditLog {
  id             String       @id @default(cuid())
  organizationId String
  membershipId   String?
  entityType     String
  entityId       String
  action         String
  payload        Json?
  createdAt      DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  membership     Membership?  @relation(fields: [membershipId], references: [id], onDelete: SetNull)

  @@index([organizationId, entityType])
}

model TransactionDocument {
  id                     String                    @id @default(cuid())
  organizationId         String
  officeId               String?
  transactionId          String
  offerId                String?
  uploadedByMembershipId String?
  linkedTaskId           String?
  title                  String
  fileName               String
  mimeType               String
  fileSizeBytes          Int
  storageKey             String
  storageUrl             String?
  documentType           String
  status                 TransactionDocumentStatus @default(uploaded)
  source                 TransactionDocumentSource @default(manual_upload)
  isRequired             Boolean                   @default(false)
  isSigned               Boolean                   @default(false)
  isUnsorted             Boolean                   @default(false)
  signedAt               DateTime?
  createdAt              DateTime                  @default(now())
  updatedAt              DateTime                  @updatedAt
  organization           Organization              @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                 Office?                   @relation(fields: [officeId], references: [id], onDelete: SetNull)
  transaction            Transaction               @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  offer                  Offer?                    @relation(fields: [offerId], references: [id], onDelete: SetNull)
  uploadedByMembership   Membership?               @relation("TransactionDocumentUploadedBy", fields: [uploadedByMembershipId], references: [id], onDelete: SetNull)
  linkedTask             TransactionTask?          @relation(fields: [linkedTaskId], references: [id], onDelete: SetNull)
  forms                  TransactionForm[]         @relation("TransactionFormDocument")
  signatureRequests      SignatureRequest[]        @relation("SignatureRequestDocument")

  @@index([organizationId, transactionId, status])
  @@index([organizationId, transactionId, isUnsorted])
  @@index([organizationId, linkedTaskId])
  @@index([organizationId, officeId])
  @@index([organizationId, offerId])
}

model FormTemplate {
  id               String            @id @default(cuid())
  organizationId   String?
  officeId         String?
  key              String            @unique
  name             String
  description      String?
  documentType     String
  mergeFields      Json
  isSystem         Boolean           @default(true)
  isActive         Boolean           @default(true)
  createdAt        DateTime          @default(now())
  updatedAt        DateTime          @updatedAt
  organization     Organization?     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office           Office?           @relation(fields: [officeId], references: [id], onDelete: SetNull)
  transactionForms TransactionForm[]

  @@index([organizationId, officeId, isActive])
}

model TransactionForm {
  id                    String                @id @default(cuid())
  organizationId        String
  officeId              String?
  transactionId         String
  offerId               String?
  templateId            String?
  linkedTaskId          String?
  documentId            String?
  name                  String
  status                TransactionFormStatus @default(draft)
  generatedPayload      Json
  createdByMembershipId String
  createdAt             DateTime              @default(now())
  updatedAt             DateTime              @updatedAt
  organization          Organization          @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                Office?               @relation(fields: [officeId], references: [id], onDelete: SetNull)
  transaction           Transaction           @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  offer                 Offer?                @relation(fields: [offerId], references: [id], onDelete: SetNull)
  template              FormTemplate?         @relation(fields: [templateId], references: [id], onDelete: SetNull)
  linkedTask            TransactionTask?      @relation(fields: [linkedTaskId], references: [id], onDelete: SetNull)
  document              TransactionDocument?  @relation("TransactionFormDocument", fields: [documentId], references: [id], onDelete: SetNull)
  createdByMembership   Membership            @relation("TransactionFormCreatedBy", fields: [createdByMembershipId], references: [id], onDelete: Cascade)
  signatureRequests     SignatureRequest[]    @relation("SignatureRequestForm")

  @@index([organizationId, transactionId, status])
  @@index([organizationId, linkedTaskId])
  @@index([organizationId, officeId])
  @@index([organizationId, offerId])
}

model SignatureRequest {
  id                      String                 @id @default(cuid())
  organizationId          String
  officeId                String?
  transactionId           String
  offerId                 String?
  formId                  String?
  documentId              String?
  requestedByMembershipId String
  recipientName           String
  recipientEmail          String
  recipientRole           String
  signingOrder            Int?
  status                  SignatureRequestStatus @default(draft)
  sentAt                  DateTime?
  viewedAt                DateTime?
  completedAt             DateTime?
  declinedAt              DateTime?
  createdAt               DateTime               @default(now())
  updatedAt               DateTime               @updatedAt
  organization            Organization           @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                  Office?                @relation(fields: [officeId], references: [id], onDelete: SetNull)
  transaction             Transaction            @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  offer                   Offer?                 @relation(fields: [offerId], references: [id], onDelete: SetNull)
  form                    TransactionForm?       @relation("SignatureRequestForm", fields: [formId], references: [id], onDelete: SetNull)
  document                TransactionDocument?   @relation("SignatureRequestDocument", fields: [documentId], references: [id], onDelete: SetNull)
  requestedByMembership   Membership             @relation("SignatureRequestRequestedBy", fields: [requestedByMembershipId], references: [id], onDelete: Cascade)

  @@index([organizationId, transactionId, status])
  @@index([organizationId, officeId])
  @@index([organizationId, formId])
  @@index([organizationId, documentId])
  @@index([organizationId, offerId])
}

model IncomingUpdate {
  id                     String               @id @default(cuid())
  organizationId         String
  officeId               String?
  transactionId          String?
  sourceSystem           String
  sourceReference        String
  status                 IncomingUpdateStatus @default(pending_review)
  summary                String
  payload                Json
  receivedAt             DateTime             @default(now())
  reviewedAt             DateTime?
  reviewedByMembershipId String?
  acceptedAt             DateTime?
  rejectedAt             DateTime?
  createdAt              DateTime             @default(now())
  updatedAt              DateTime             @updatedAt
  organization           Organization         @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                 Office?              @relation(fields: [officeId], references: [id], onDelete: SetNull)
  transaction            Transaction?         @relation(fields: [transactionId], references: [id], onDelete: SetNull)
  reviewedByMembership   Membership?          @relation("IncomingUpdateReviewedBy", fields: [reviewedByMembershipId], references: [id], onDelete: SetNull)

  @@unique([organizationId, sourceSystem, sourceReference])
  @@index([organizationId, status])
  @@index([organizationId, transactionId])
  @@index([organizationId, officeId, receivedAt])
}

model Offer {
  id                    String                @id @default(cuid())
  organizationId        String
  officeId              String?
  transactionId         String
  createdByMembershipId String
  title                 String
  offeringPartyName     String
  buyerName             String?
  status                OfferStatus           @default(draft)
  price                 Decimal?              @db.Decimal(12, 2)
  earnestMoneyAmount    Decimal?              @db.Decimal(12, 2)
  financingType         String?
  closingDateOffered    DateTime?
  expirationAt          DateTime?
  isPrimaryOffer        Boolean               @default(false)
  notes                 String?
  submittedAt           DateTime?
  acceptedAt            DateTime?
  rejectedAt            DateTime?
  withdrawnAt           DateTime?
  createdAt             DateTime              @default(now())
  updatedAt             DateTime              @updatedAt
  organization          Organization          @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                Office?               @relation(fields: [officeId], references: [id], onDelete: SetNull)
  transaction           Transaction           @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  createdByMembership   Membership            @relation("OfferCreatedBy", fields: [createdByMembershipId], references: [id], onDelete: Cascade)
  documents             TransactionDocument[]
  forms                 TransactionForm[]
  signatureRequests     SignatureRequest[]
  comments              OfferComment[]

  @@index([organizationId, transactionId, status])
  @@index([organizationId, officeId, createdAt])
  @@index([organizationId, expirationAt])
  @@index([organizationId, createdByMembershipId])
}

model OfferComment {
  id             String       @id @default(cuid())
  organizationId String
  officeId       String?
  offerId        String
  membershipId   String
  body           String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office         Office?      @relation(fields: [officeId], references: [id], onDelete: SetNull)
  offer          Offer        @relation(fields: [offerId], references: [id], onDelete: Cascade)
  membership     Membership   @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@index([organizationId, offerId, createdAt])
  @@index([organizationId, membershipId])
}

model LedgerAccount {
  id                   String                          @id @default(cuid())
  organizationId       String
  officeId             String?
  code                 String?
  name                 String
  accountType          LedgerAccountType
  isSystem             Boolean                         @default(false)
  isActive             Boolean                         @default(true)
  createdAt            DateTime                        @default(now())
  updatedAt            DateTime                        @updatedAt
  organization         Organization                    @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office               Office?                         @relation(fields: [officeId], references: [id], onDelete: SetNull)
  lineItems            AccountingTransactionLineItem[]
  generalLedgerEntries GeneralLedgerEntry[]

  @@unique([organizationId, code])
  @@index([organizationId, officeId, accountType])
  @@index([organizationId, isActive])
}

model AccountingTransaction {
  id                          String                             @id @default(cuid())
  organizationId              String
  officeId                    String?
  relatedTransactionId        String?
  relatedMembershipId         String?
  type                        AccountingTransactionType
  status                      AccountingTransactionStatus        @default(draft)
  accountingDate              DateTime
  dueDate                     DateTime?
  paymentMethod               AccountingPaymentMethod?
  referenceNumber             String?
  counterpartyName            String?
  memo                        String?
  notes                       String?
  isAgentBilling              Boolean                            @default(false)
  billingCategory             String?
  originRecurringChargeRuleId String?
  totalAmount                 Decimal                            @db.Decimal(12, 2)
  createdByMembershipId       String
  postedAt                    DateTime?
  externalSyncStatus          String?
  externalReferenceId         String?
  createdAt                   DateTime                           @default(now())
  updatedAt                   DateTime                           @updatedAt
  organization                Organization                       @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                      Office?                            @relation(fields: [officeId], references: [id], onDelete: SetNull)
  relatedTransaction          Transaction?                       @relation(fields: [relatedTransactionId], references: [id], onDelete: SetNull)
  relatedMembership           Membership?                        @relation("AccountingTransactionRelatedMembership", fields: [relatedMembershipId], references: [id], onDelete: SetNull)
  createdByMembership         Membership                         @relation("AccountingTransactionCreatedBy", fields: [createdByMembershipId], references: [id], onDelete: Cascade)
  originRecurringChargeRule   AgentRecurringChargeRule?          @relation(fields: [originRecurringChargeRuleId], references: [id], onDelete: SetNull)
  commissionCalculations      CommissionCalculation[]
  lineItems                   AccountingTransactionLineItem[]
  applicationsFrom            AccountingTransactionApplication[] @relation("AccountingTransactionApplicationSource")
  applicationsTo              AccountingTransactionApplication[] @relation("AccountingTransactionApplicationTarget")
  ledgerEntries               GeneralLedgerEntry[]

  @@index([organizationId, officeId, type])
  @@index([organizationId, officeId, status])
  @@index([organizationId, relatedTransactionId])
  @@index([organizationId, relatedMembershipId])
  @@index([organizationId, isAgentBilling, relatedMembershipId])
  @@index([organizationId, accountingDate])
}

model AccountingTransactionLineItem {
  id                      String                  @id @default(cuid())
  organizationId          String
  officeId                String?
  accountingTransactionId String
  relatedTransactionId    String?
  ledgerAccountId         String
  description             String?
  entrySide               AccountingLineEntrySide
  amount                  Decimal                 @db.Decimal(12, 2)
  sortOrder               Int                     @default(0)
  createdAt               DateTime                @default(now())
  updatedAt               DateTime                @updatedAt
  organization            Organization            @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                  Office?                 @relation(fields: [officeId], references: [id], onDelete: SetNull)
  accountingTransaction   AccountingTransaction   @relation(fields: [accountingTransactionId], references: [id], onDelete: Cascade)
  relatedTransaction      Transaction?            @relation(fields: [relatedTransactionId], references: [id], onDelete: SetNull)
  ledgerAccount           LedgerAccount           @relation(fields: [ledgerAccountId], references: [id], onDelete: Restrict)

  @@index([organizationId, accountingTransactionId])
  @@index([organizationId, ledgerAccountId])
  @@index([organizationId, relatedTransactionId])
}

model AccountingTransactionApplication {
  id                            String                @id @default(cuid())
  organizationId                String
  officeId                      String?
  sourceAccountingTransactionId String
  targetAccountingTransactionId String
  createdByMembershipId         String?
  amount                        Decimal               @db.Decimal(12, 2)
  memo                          String?
  appliedAt                     DateTime              @default(now())
  createdAt                     DateTime              @default(now())
  updatedAt                     DateTime              @updatedAt
  organization                  Organization          @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                        Office?               @relation(fields: [officeId], references: [id], onDelete: SetNull)
  sourceAccountingTransaction   AccountingTransaction @relation("AccountingTransactionApplicationSource", fields: [sourceAccountingTransactionId], references: [id], onDelete: Cascade)
  targetAccountingTransaction   AccountingTransaction @relation("AccountingTransactionApplicationTarget", fields: [targetAccountingTransactionId], references: [id], onDelete: Cascade)
  createdByMembership           Membership?           @relation("AccountingTransactionApplicationCreatedBy", fields: [createdByMembershipId], references: [id], onDelete: SetNull)

  @@index([organizationId, sourceAccountingTransactionId])
  @@index([organizationId, targetAccountingTransactionId])
  @@index([organizationId, officeId])
}

model GeneralLedgerEntry {
  id                      String                @id @default(cuid())
  organizationId          String
  officeId                String?
  accountingTransactionId String
  relatedTransactionId    String?
  accountId               String
  entryDate               DateTime
  debitAmount             Decimal               @default(0) @db.Decimal(12, 2)
  creditAmount            Decimal               @default(0) @db.Decimal(12, 2)
  memo                    String?
  createdAt               DateTime              @default(now())
  organization            Organization          @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                  Office?               @relation(fields: [officeId], references: [id], onDelete: SetNull)
  accountingTransaction   AccountingTransaction @relation(fields: [accountingTransactionId], references: [id], onDelete: Cascade)
  relatedTransaction      Transaction?          @relation(fields: [relatedTransactionId], references: [id], onDelete: SetNull)
  account                 LedgerAccount         @relation(fields: [accountId], references: [id], onDelete: Restrict)

  @@index([organizationId, officeId, entryDate])
  @@index([organizationId, accountingTransactionId])
  @@index([organizationId, relatedTransactionId])
  @@index([organizationId, accountId])
}

model AgentRecurringChargeRule {
  id                    String                  @id @default(cuid())
  organizationId        String
  officeId              String?
  membershipId          String
  name                  String
  chargeType            String
  description           String?
  amount                Decimal                 @db.Decimal(12, 2)
  frequency             AgentBillingFrequency
  customIntervalDays    Int?
  startDate             DateTime
  nextDueDate           DateTime
  endDate               DateTime?
  lastGeneratedAt       DateTime?
  autoGenerateInvoice   Boolean                 @default(false)
  isActive              Boolean                 @default(true)
  createdAt             DateTime                @default(now())
  updatedAt             DateTime                @updatedAt
  organization          Organization            @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                Office?                 @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership            Membership              @relation("AgentRecurringChargeRuleMembership", fields: [membershipId], references: [id], onDelete: Cascade)
  generatedTransactions AccountingTransaction[]

  @@unique([organizationId, membershipId, name])
  @@index([organizationId, officeId, isActive])
  @@index([organizationId, membershipId, isActive])
  @@index([organizationId, nextDueDate])
}

model AgentPaymentMethod {
  id                  String                   @id @default(cuid())
  organizationId      String
  officeId            String?
  membershipId        String
  type                AgentPaymentMethodType
  label               String
  provider            String
  last4               String?
  isDefault           Boolean                  @default(false)
  autoPayEnabled      Boolean                  @default(false)
  externalReferenceId String?
  status              AgentPaymentMethodStatus @default(active)
  createdAt           DateTime                 @default(now())
  updatedAt           DateTime                 @updatedAt
  organization        Organization             @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office              Office?                  @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership          Membership               @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@index([organizationId, officeId])
  @@index([organizationId, membershipId, status])
}

model AgentProfile {
  id                 String                @id @default(cuid())
  organizationId     String
  officeId           String?
  membershipId       String                @unique
  displayName        String?
  bio                String?
  notes              String?
  licenseNumber      String?
  licenseState       String?
  startDate          DateTime?
  onboardingStatus   AgentOnboardingStatus @default(not_started)
  commissionPlanName String?
  avatarUrl          String?
  internalExtension  String?
  createdAt          DateTime              @default(now())
  updatedAt          DateTime              @updatedAt
  organization       Organization          @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office             Office?               @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership         Membership            @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@index([organizationId, officeId])
  @@index([organizationId, onboardingStatus])
}

model Team {
  id                        String                     @id @default(cuid())
  organizationId            String
  officeId                  String?
  name                      String
  slug                      String
  isActive                  Boolean                    @default(true)
  createdAt                 DateTime                   @default(now())
  updatedAt                 DateTime                   @updatedAt
  organization              Organization               @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                    Office?                    @relation(fields: [officeId], references: [id], onDelete: SetNull)
  memberships               TeamMembership[]
  commissionPlanAssignments CommissionPlanAssignment[]

  @@unique([organizationId, slug])
  @@index([organizationId, officeId, isActive])
}

model TeamMembership {
  id             String             @id @default(cuid())
  organizationId String
  officeId       String?
  teamId         String
  membershipId   String
  role           TeamMembershipRole @default(member)
  createdAt      DateTime           @default(now())
  organization   Organization       @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office         Office?            @relation(fields: [officeId], references: [id], onDelete: SetNull)
  team           Team               @relation(fields: [teamId], references: [id], onDelete: Cascade)
  membership     Membership         @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@unique([teamId, membershipId])
  @@index([organizationId, officeId])
  @@index([organizationId, membershipId])
}

model AgentOnboardingItem {
  id                      String                       @id @default(cuid())
  organizationId          String
  officeId                String?
  membershipId            String
  templateItemId          String?
  title                   String
  description             String?
  category                String                       @default("General")
  dueAt                   DateTime?
  status                  AgentOnboardingItemStatus    @default(pending)
  sortOrder               Int                          @default(0)
  completedAt             DateTime?
  completedByMembershipId String?
  createdAt               DateTime                     @default(now())
  updatedAt               DateTime                     @updatedAt
  organization            Organization                 @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                  Office?                      @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership              Membership                   @relation(fields: [membershipId], references: [id], onDelete: Cascade)
  templateItem            AgentOnboardingTemplateItem? @relation(fields: [templateItemId], references: [id], onDelete: SetNull)
  completedByMembership   Membership?                  @relation("AgentOnboardingCompletedBy", fields: [completedByMembershipId], references: [id], onDelete: SetNull)

  @@index([organizationId, officeId, membershipId])
  @@index([organizationId, membershipId, status])
  @@index([organizationId, dueAt])
}

model AgentOnboardingTemplateItem {
  id              String                @id @default(cuid())
  organizationId  String
  officeId        String?
  title           String
  description     String?
  category        String                @default("General")
  dueDaysOffset   Int?
  sortOrder       Int                   @default(0)
  isActive        Boolean               @default(true)
  createdAt       DateTime              @default(now())
  updatedAt       DateTime              @updatedAt
  organization    Organization          @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office          Office?               @relation(fields: [officeId], references: [id], onDelete: SetNull)
  onboardingItems AgentOnboardingItem[]

  @@index([organizationId, officeId, isActive])
  @@index([organizationId, sortOrder])
}

model AgentGoal {
  id                     String              @id @default(cuid())
  organizationId         String
  officeId               String?
  membershipId           String
  periodType             AgentGoalPeriodType
  startsAt               DateTime
  endsAt                 DateTime
  targetTransactionCount Int?
  targetClosedVolume     Decimal?            @db.Decimal(12, 2)
  targetOfficeNet        Decimal?            @db.Decimal(12, 2)
  targetAgentNet         Decimal?            @db.Decimal(12, 2)
  notes                  String?
  createdAt              DateTime            @default(now())
  updatedAt              DateTime            @updatedAt
  organization           Organization        @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                 Office?             @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership             Membership          @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@index([organizationId, officeId, membershipId])
  @@index([organizationId, startsAt, endsAt])
}

model RequiredContactRoleSetting {
  id             String                 @id @default(cuid())
  organizationId String
  officeId       String?
  role           TransactionContactRole
  isRequired     Boolean                @default(true)
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  organization   Organization           @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office         Office?                @relation(fields: [officeId], references: [id], onDelete: SetNull)

  @@unique([organizationId, officeId, role])
  @@index([organizationId, officeId, isRequired])
}

model TransactionFieldSetting {
  id             String              @id @default(cuid())
  organizationId String
  officeId       String?
  fieldKey       TransactionFieldKey
  isRequired     Boolean             @default(false)
  isVisible      Boolean             @default(true)
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  organization   Organization        @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office         Office?             @relation(fields: [officeId], references: [id], onDelete: SetNull)

  @@unique([organizationId, officeId, fieldKey])
  @@index([organizationId, officeId, isVisible])
}

model ChecklistTemplate {
  id                    String                  @id @default(cuid())
  organizationId        String
  officeId              String?
  name                  String
  description           String?
  transactionType       TransactionType?
  isActive              Boolean                 @default(true)
  createdByMembershipId String?
  updatedByMembershipId String?
  createdAt             DateTime                @default(now())
  updatedAt             DateTime                @updatedAt
  organization          Organization            @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                Office?                 @relation(fields: [officeId], references: [id], onDelete: SetNull)
  createdByMembership   Membership?             @relation("ChecklistTemplateCreatedBy", fields: [createdByMembershipId], references: [id], onDelete: SetNull)
  updatedByMembership   Membership?             @relation("ChecklistTemplateUpdatedBy", fields: [updatedByMembershipId], references: [id], onDelete: SetNull)
  items                 ChecklistTemplateItem[]

  @@index([organizationId, officeId, isActive])
  @@index([organizationId, transactionType])
}

model ChecklistTemplateItem {
  id                        String            @id @default(cuid())
  organizationId            String
  officeId                  String?
  checklistTemplateId       String
  checklistGroup            String            @default("General")
  title                     String
  description               String?
  dueDaysOffset             Int?
  sortOrder                 Int               @default(0)
  requiresDocument          Boolean           @default(false)
  requiresDocumentApproval  Boolean           @default(false)
  requiresSecondaryApproval Boolean           @default(false)
  createdAt                 DateTime          @default(now())
  updatedAt                 DateTime          @updatedAt
  organization              Organization      @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                    Office?           @relation(fields: [officeId], references: [id], onDelete: SetNull)
  checklistTemplate         ChecklistTemplate @relation(fields: [checklistTemplateId], references: [id], onDelete: Cascade)

  @@index([organizationId, officeId, checklistTemplateId])
  @@index([organizationId, sortOrder])
}

model CommissionPlan {
  id              String                     @id @default(cuid())
  organizationId  String
  officeId        String?
  name            String
  description     String?
  isActive        Boolean                    @default(true)
  calculationMode CommissionCalculationMode  @default(split_and_fees)
  defaultCurrency String?
  createdAt       DateTime                   @default(now())
  updatedAt       DateTime                   @updatedAt
  organization    Organization               @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office          Office?                    @relation(fields: [officeId], references: [id], onDelete: SetNull)
  assignments     CommissionPlanAssignment[]
  rules           CommissionPlanRule[]
  calculations    CommissionCalculation[]

  @@unique([organizationId, officeId, name])
  @@index([organizationId, officeId, isActive])
}

model CommissionPlanAssignment {
  id               String         @id @default(cuid())
  organizationId   String
  officeId         String?
  membershipId     String?
  teamId           String?
  commissionPlanId String
  effectiveFrom    DateTime
  effectiveTo      DateTime?
  createdAt        DateTime       @default(now())
  updatedAt        DateTime       @updatedAt
  organization     Organization   @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office           Office?        @relation(fields: [officeId], references: [id], onDelete: SetNull)
  membership       Membership?    @relation(fields: [membershipId], references: [id], onDelete: Cascade)
  team             Team?          @relation(fields: [teamId], references: [id], onDelete: Cascade)
  commissionPlan   CommissionPlan @relation(fields: [commissionPlanId], references: [id], onDelete: Cascade)

  @@index([organizationId, officeId, membershipId])
  @@index([organizationId, officeId, teamId])
  @@index([organizationId, commissionPlanId])
  @@index([organizationId, effectiveFrom, effectiveTo])
}

model CommissionPlanRule {
  id               String                   @id @default(cuid())
  organizationId   String
  commissionPlanId String
  ruleType         CommissionPlanRuleType
  ruleName         String
  sortOrder        Int                      @default(0)
  splitPercent     Decimal?                 @db.Decimal(7, 4)
  flatAmount       Decimal?                 @db.Decimal(12, 2)
  feeType          CommissionRuleFeeType?
  feeAmount        Decimal?                 @db.Decimal(12, 2)
  thresholdStart   Decimal?                 @db.Decimal(12, 2)
  thresholdEnd     Decimal?                 @db.Decimal(12, 2)
  appliesToRole    String?
  recipientType    CommissionRecipientType?
  isActive         Boolean                  @default(true)
  createdAt        DateTime                 @default(now())
  updatedAt        DateTime                 @updatedAt
  organization     Organization             @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  commissionPlan   CommissionPlan           @relation(fields: [commissionPlanId], references: [id], onDelete: Cascade)

  @@index([organizationId, commissionPlanId, sortOrder])
  @@index([organizationId, ruleType, isActive])
}

model CommissionCalculation {
  id                       String                      @id @default(cuid())
  organizationId           String
  officeId                 String?
  transactionId            String
  membershipId             String?
  commissionPlanId         String?
  accountingTransactionId  String?
  recipientType            CommissionRecipientType
  recipientRole            String?
  recipientName            String?
  grossCommission          Decimal                     @default(0) @db.Decimal(12, 2)
  referralFee              Decimal                     @default(0) @db.Decimal(12, 2)
  fees                     Decimal                     @default(0) @db.Decimal(12, 2)
  officeNet                Decimal                     @default(0) @db.Decimal(12, 2)
  agentNet                 Decimal                     @default(0) @db.Decimal(12, 2)
  statementAmount          Decimal                     @default(0) @db.Decimal(12, 2)
  status                   CommissionCalculationStatus @default(draft)
  notes                    String?
  calculatedAt             DateTime                    @default(now())
  calculatedByMembershipId String?
  createdAt                DateTime                    @default(now())
  updatedAt                DateTime                    @updatedAt
  organization             Organization                @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                   Office?                     @relation(fields: [officeId], references: [id], onDelete: SetNull)
  transaction              Transaction                 @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  membership               Membership?                 @relation("CommissionCalculationMembership", fields: [membershipId], references: [id], onDelete: SetNull)
  commissionPlan           CommissionPlan?             @relation(fields: [commissionPlanId], references: [id], onDelete: SetNull)
  accountingTransaction    AccountingTransaction?      @relation(fields: [accountingTransactionId], references: [id], onDelete: SetNull)
  calculatedByMembership   Membership?                 @relation("CommissionCalculationCalculatedBy", fields: [calculatedByMembershipId], references: [id], onDelete: SetNull)

  @@index([organizationId, officeId, status])
  @@index([organizationId, transactionId])
  @@index([organizationId, membershipId])
  @@index([organizationId, commissionPlanId])
  @@index([organizationId, calculatedAt])
}

model EarnestMoneyRecord {
  id                    String             @id @default(cuid())
  organizationId        String
  officeId              String?
  transactionId         String
  expectedAmount        Decimal            @db.Decimal(12, 2)
  dueAt                 DateTime
  receivedAmount        Decimal            @default(0) @db.Decimal(12, 2)
  refundedAmount        Decimal            @default(0) @db.Decimal(12, 2)
  paymentDate           DateTime?
  depositDate           DateTime?
  heldByOffice          Boolean            @default(true)
  heldExternally        Boolean            @default(false)
  trackInLedger         Boolean            @default(true)
  status                EarnestMoneyStatus @default(not_received)
  notes                 String?
  createdByMembershipId String?
  createdAt             DateTime           @default(now())
  updatedAt             DateTime           @updatedAt
  organization          Organization       @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  office                Office?            @relation(fields: [officeId], references: [id], onDelete: SetNull)
  transaction           Transaction        @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  createdByMembership   Membership?        @relation("EarnestMoneyCreatedBy", fields: [createdByMembershipId], references: [id], onDelete: SetNull)

  @@unique([transactionId])
  @@index([organizationId, officeId, status])
  @@index([organizationId, dueAt])
}
