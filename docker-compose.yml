services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: acre
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d acre"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - acre-postgres-data:/var/lib/postgresql/data

  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    stdin_open: true
    tty: true
    working_dir: /app
    environment:
      ACRE_DEV_HOST: 0.0.0.0
      DATABASE_URL: ${ACRE_DOCKER_DATABASE_URL:-postgresql://postgres:postgres@db:5432/acre}
      ACRE_SESSION_SECRET: ${ACRE_SESSION_SECRET:-docker-local-session-secret}
      ACRE_SECURE_COOKIES: ${ACRE_SECURE_COOKIES:-false}
      ACRE_REMOTE_DOCUMENTS_SSH_TARGET: ${ACRE_REMOTE_DOCUMENTS_SSH_TARGET:-}
      ACRE_REMOTE_DOCUMENTS_STORAGE_ROOT: ${ACRE_REMOTE_DOCUMENTS_STORAGE_ROOT:-}
      ACRE_REMOTE_DOCUMENTS_SSH_KEY: ${ACRE_REMOTE_DOCUMENTS_SSH_KEY:-/run/acre/remote-documents-ssh-key}
      ACRE_REMOTE_DOCUMENTS_SSH_KNOWN_HOSTS: ${ACRE_REMOTE_DOCUMENTS_SSH_KNOWN_HOSTS:-/run/acre/remote-documents-known_hosts}
      ACRE_DOCUMENTS_STORAGE_DIR: /app/.local-storage/documents
      WATCHPACK_POLLING: "true"
    ports:
      - "3105:3105"
    volumes:
      - .:/app
      - acre-node-modules:/app/node_modules
      - acre-next-cache:/app/apps/web/.next
      - acre-documents:/app/.local-storage/documents
      - ${ACRE_REMOTE_DOCUMENTS_SSH_KEY_MOUNT:-./docker/dev-empty-secret}:/run/acre/remote-documents-ssh-key:ro
      - ${ACRE_REMOTE_DOCUMENTS_SSH_KNOWN_HOSTS_MOUNT:-./docker/dev-empty-secret}:/run/acre/remote-documents-known_hosts:ro
    command: ["npm", "run", "dev"]

volumes:
  acre-postgres-data:
  acre-node-modules:
  acre-next-cache:
  acre-documents:
