#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

bash ./scripts/run-gitleaks.sh protect --staged --source . --config .gitleaks.toml --redact --no-banner
