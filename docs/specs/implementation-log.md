# Back Office Implementation Log

## Current milestone

- `Back Office MVP+` on one active repo/deployment line
- Core operational modules already exist as real product surfaces, not just placeholder navigation
- Current priority is cleanup, normalization, and workflow hardening instead of blindly adding more modules
- `visible-scope CRM cleanup`, the first FO `activity + cleanup center`, the first browser-side `OCR / transcript intake assist`, the first logged `appointment bridge writeback` layer, the first agent-managed `appointment external follow-up writeback state`, a route-persistent `/agent/clients` workbench contract, a route-persistent `/agent/calendar` workbench contract, a route-persistent `/agent/listings` focused-lane contract, and dashboard-to-workbench re-entry shortcuts are now live, so the next FO work should stay focused on deeper external writeback, stronger provider-backed intake acceleration, and broader office-wide cleanup depth before any heavier background automation

## Recently completed major work

- 2026-04-24: `Back Office transaction overdue reminders` now surface post move-in / closing collection pressure as critical in-app work:
  - updated [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma), added [packages/db/prisma/migrations/20260424153000_bo_transaction_overdue_notifications/migration.sql](../../packages/db/prisma/migrations/20260424153000_bo_transaction_overdue_notifications/migration.sql), and updated [packages/db/src/notifications.ts](../../packages/db/src/notifications.ts) so transactions whose `moveInDate ?? closingDate` is more than three calendar months old and still not `Closed` or `Cancelled` create `transaction_overdue` notifications for the transaction owner plus active `owner / office_admin / accountant` recipients
  - updated [packages/db/src/dashboard.ts](../../packages/db/src/dashboard.ts), [apps/web/app/office/dashboard/page.tsx](../../apps/web/app/office/dashboard/page.tsx), [apps/web/app/office/notifications/notifications-client.tsx](../../apps/web/app/office/notifications/notifications-client.tsx), and [apps/web/app/globals.css](../../apps/web/app/globals.css) so the Dashboard and Notifications center both pin critical overdue transaction queues instead of burying them in ordinary lists

- 2026-04-24: `Listing Studio public collection maps` now give shared collection visitors a bottom map view for the saved homes:
  - added [apps/web/app/share/collections/[code]/listing-studio-public-collection-map.tsx](<../../apps/web/app/share/collections/[code]/listing-studio-public-collection-map.tsx>) and updated [apps/web/app/share/collections/[code]/listing-studio-public-collection-client.tsx](<../../apps/web/app/share/collections/[code]/listing-studio-public-collection-client.tsx>) plus [apps/web/app/globals.css](../../apps/web/app/globals.css) so `/share/collections/[code]` renders a client-facing map section with numbered listing pins, marker detail popovers for Google Maps, a no-coordinate fallback, and a keyless public tile-map preview when Google Maps is not configured
  - updated [packages/db/src/studio-listings.ts](../../packages/db/src/studio-listings.ts), [packages/db/src/studio-listings.test.ts](../../packages/db/src/studio-listings.test.ts), [docs/env.md](../env.md), and [docs/specs/listing-studio-spec.md](listing-studio-spec.md) so public collection snapshots carry listing coordinates and the Google Maps env contract covers the shared public route

- 2026-04-24: `Listing Studio collection share dialog` now separates normal link sharing from WeChat card sharing:
  - updated [apps/web/app/listing-studio/collections/[collectionId]/listing-studio-collection-detail-client.tsx](<../../apps/web/app/listing-studio/collections/[collectionId]/listing-studio-collection-detail-client.tsx>) and [apps/web/app/globals.css](../../apps/web/app/globals.css) so the collection detail `Share` dialog now offers `Copy with message` and `WeChat card` actions, both copying the public collection link while WeChat mode keeps the dialog open with in-WeChat sharing instructions
  - updated [docs/specs/listing-studio-spec.md](listing-studio-spec.md) so the collection detail share contract names both share methods

- 2026-04-24: `Listing Studio collection shares` now has a real activity list instead of a placeholder route:
  - updated [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma), added [packages/db/prisma/migrations/20260424110000_add_studio_collection_share_events/migration.sql](../../packages/db/prisma/migrations/20260424110000_add_studio_collection_share_events/migration.sql), and updated [packages/db/src/studio-listings.ts](../../packages/db/src/studio-listings.ts) so collection share-link copies write `shared` events and public collection page opens write `opened` events with the same privacy-preserving request metadata pattern used by pack shares
  - updated [apps/web/app/listing-studio/shares/page.tsx](../../apps/web/app/listing-studio/shares/page.tsx), [apps/web/app/share/collections/[code]/page.tsx](<../../apps/web/app/share/collections/[code]/page.tsx>), and [apps/web/app/globals.css](../../apps/web/app/globals.css) so `/listing-studio/shares` now lists collection share counts, view counts, latest activity, link status, and collection/public-link entry points
  - updated [packages/db/src/studio-listings.test.ts](../../packages/db/src/studio-listings.test.ts), [README.md](../../README.md), [docs/architecture.md](../architecture.md), and [docs/specs/listing-studio-spec.md](listing-studio-spec.md) so the collection share tracker has regression coverage and the docs no longer describe `Shares` as only a placeholder

- 2026-04-22: `Front Office restricted-module warning flow` now keeps users inside Front Office when a module is unavailable instead of bouncing them into Back Office:
  - added [apps/web/app/agent/\_components/front-office-access-notice.tsx](../../apps/web/app/agent/_components/front-office-access-notice.tsx) and updated [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx), [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx), [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx), and [apps/web/app/listing-studio/layout.tsx](../../apps/web/app/listing-studio/layout.tsx) so FO routes now render an in-workspace access warning with alternative FO destinations instead of calling `redirect(getDefaultAppPath(...))` when the current account lacks that module permission
  - updated [apps/web/app/agent/agent-nav.tsx](../../apps/web/app/agent/agent-nav.tsx), [apps/web/app/\_components/workspace-nav.tsx](../../apps/web/app/_components/workspace-nav.tsx), [apps/web/lib/i18n/messages/en-US.ts](../../apps/web/lib/i18n/messages/en-US.ts), [apps/web/lib/i18n/messages/zh-CN.ts](../../apps/web/lib/i18n/messages/zh-CN.ts), and [apps/web/app/globals.css](../../apps/web/app/globals.css) so restricted FO nav items now stay visible with a `Restricted / 受限` badge and open an inline warning dialog on click instead of looking like a broken jump into Back Office
  - updated [apps/web/app/agent/notifications/[notificationId]/open/page.tsx](../../apps/web/app/agent/notifications/[notificationId]/open/page.tsx) so notification open helpers now route back into `/agent/notifications` for no-access cases instead of falling through the older default-app redirect path

- 2026-04-22: `recursive Teams admin expansion` now opens the Back Office agent hierarchy beyond the earlier two-level admin cap instead of stopping at `Team -> Junior Team`:
  - updated [packages/db/src/agents/team-management.ts](../../packages/db/src/agents/team-management.ts) so team creation/update no longer passes the old `maxDepth: 1` cap into parent validation, child-team lifecycle errors now use depth-agnostic `child team` language, and the existing recursive team / reporting / commission chain model stays intact without any Prisma schema change
  - updated [apps/web/app/office/settings/teams/team-directory-shared.ts](../../apps/web/app/office/settings/teams/team-directory-shared.ts), [apps/web/app/office/settings/teams/teams-client.tsx](../../apps/web/app/office/settings/teams/teams-client.tsx), [apps/web/app/office/settings/teams/team-detail-client.tsx](../../apps/web/app/office/settings/teams/team-detail-client.tsx), [apps/web/app/office/settings/teams/teams-manage-client.tsx](../../apps/web/app/office/settings/teams/teams-manage-client.tsx), and [apps/web/app/office/settings/teams/page.tsx](../../apps/web/app/office/settings/teams/page.tsx) so `Settings > Teams` now names branches by depth (`Team`, `Junior Team 1`, `Junior Team 2`, `Junior Team 3`...), allows creating child teams from any current branch, filters parent-team pickers against the current branch and its descendants, and keeps badges, owner labels, manager placeholders, and block messages aligned to the current depth
  - added [apps/web/app/office/settings/teams/team-directory-shared.test.ts](../../apps/web/app/office/settings/teams/team-directory-shared.test.ts) and updated [packages/db/src/settings.test.ts](../../packages/db/src/settings.test.ts) so both the UI label helpers and the recursive team-management flow now have regression coverage for deeper branch naming, legal parent selection, cycle prevention, descendant-scope visibility, and `Team -> Junior Team 1 -> Junior Team 2 -> Junior Team 3` creation
  - updated [docs/specs/agent-management-spec.md](agent-management-spec.md) so the agent-management spec now describes the recursive admin flow, depth-aware branch naming, and the fact that all non-root owners still reuse the same `junior_team_leader` database role

- 2026-04-21: `Listing Studio share poster studio` now routes `share` into a dedicated owner-facing poster workspace instead of jumping straight to the public packet page:
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx), added [apps/web/app/(share-studio)/listing-studio/listings/[packId]/share/page.tsx](<../../apps/web/app/(share-studio)/listing-studio/listings/[packId]/share/page.tsx>) and [apps/web/app/listing-studio/listings/[packId]/share/listing-studio-share-studio-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/share/listing-studio-share-studio-client.tsx), and updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so clicking `share` from the listing detail stage now enters a standalone `/listing-studio/listings/[packId]/share` studio without the Acre workspace shell, shows a left template rail, a centered live poster preview, and a right control rail with `Listing status`, main-photo, public-share, and export controls
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts) and [apps/web/app/api/listing-studio/listings/[packId]/poster/route.ts](../../apps/web/app/api/listing-studio/listings/[packId]/poster/route.ts) so the poster pipeline now renders five brand-free vertical templates (`hero / editorial / card / cinematic / grid`), removes poster QR blocks, pulls avatar / company / phone / email from the current logged-in agent snapshot for the footer contact block, and serves matching `svg / html / png` output with real `2160 x 2880` PNG export
  - updated [README.md](../../README.md), [docs/architecture.md](../architecture.md), and [docs/specs/listing-studio-spec.md](listing-studio-spec.md) so the documented Listing Studio route map and export contract now include the new internal share studio, status-driven poster variants, and PNG export path

- 2026-04-20: `Listing Studio standalone edit route` now follows the normal page-scroll model instead of trapping the editor inside a fixed internal scroller:
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx), added [apps/web/app/listing-studio/listings/[packId]/edit/page.tsx](../../apps/web/app/listing-studio/listings/[packId]/edit/page.tsx), and updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so `Edit listing` now routes to `/listing-studio/listings/[packId]/edit`, `Cancel / Save Changes` return to the saved-listing detail route, and the editor itself scrolls as a normal standalone page instead of a fixed fullscreen dialog with an internal scroll area
  - updated [README.md](../../README.md) and [docs/architecture.md](../architecture.md) so the documented `Listing Studio` route map now includes the new dedicated edit page

- 2026-04-19: `Prisma pool tuning env contract` now gives Phase 1 a safe, reversible connection-pool control point instead of forcing operators to hand-edit `DATABASE_URL` query strings:
  - updated [packages/db/src/client.ts](../../packages/db/src/client.ts) so Prisma now derives its datasource URL through a small builder that conditionally injects `connection_limit` and `pool_timeout` only when `PRISMA_CONNECTION_LIMIT` / `PRISMA_POOL_TIMEOUT` are explicitly set; leaving those env vars empty preserves the prior runtime behavior
  - added [packages/db/src/client.test.ts](../../packages/db/src/client.test.ts) and updated [package.json](../../package.json) so the pool-tuning URL rewrite logic is regression-tested in the standard hardening suite, including preserving existing query params and ignoring invalid values
  - updated [.env.example](../../.env.example), [docs/env.md](../env.md), and [docs/OBSERVABILITY.md](../OBSERVABILITY.md) so operators now have one documented place for the new pool env knobs, an explicit starting recommendation for the current single-process DO deployment, and clearer guidance that `/api/health` reports database `max_connections`, not Prisma `connection_limit`

- 2026-04-19: `Session membership request memoization` now removes repeated auth-context database reads from hot-path renders and route chains instead of resolving the same session membership multiple times per request:
  - updated [apps/web/lib/auth-session.ts](../../apps/web/lib/auth-session.ts) so current-render session lookups now reuse a cached membership-context resolver keyed by `membershipId + activeOfficeId`, while route handlers now reuse a per-request `WeakMap` cache keyed to the incoming `NextRequest`; password-change gating behavior stays unchanged because it still runs after the shared context is resolved
  - updated [apps/web/lib/auth-session.test.ts](../../apps/web/lib/auth-session.test.ts) so auth-session coverage now includes request-scope dedupe behavior and office-selection isolation, guarding against regressions where repeated lookups within one request re-open duplicate `@acre/db` reads or accidentally blur separate office selections together

- 2026-04-16: `Back Office / Resources simplified publisher reset + Front Office three-lane parity` now turns office-side resource management into a minimal FO publishing surface instead of a directory-operations console:
  - updated [apps/web/app/office/resources/page.tsx](../../apps/web/app/office/resources/page.tsx) and fully rewrote [apps/web/app/office/resources/office-resources-client.tsx](../../apps/web/app/office/resources/office-resources-client.tsx) so `/office/resources` now exposes only `Documents / Vendors / Training` tabs with `add / edit / delete` flows, removing coverage cards, starter guidance, stale/top-opened modules, subtype controls, and other BO-only management copy that no longer maps cleanly to FO
  - updated [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma), [packages/db/src/office-resources.ts](../../packages/db/src/office-resources.ts), and [packages/db/src/office-resources.test.ts](../../packages/db/src/office-resources.test.ts) so office-managed `Resource` records now support uploaded PDF metadata, default-new resources to published, normalize legacy `playbook / template / document` records into one `document` lane, and return explicit stored-file/open-link information for both BO and FO consumers
  - updated [apps/web/lib/document-storage.ts](../../apps/web/lib/document-storage.ts), [apps/web/app/api/office/resources/route.ts](../../apps/web/app/api/office/resources/route.ts), [apps/web/app/api/office/resources/[resourceId]/route.ts](../../apps/web/app/api/office/resources/[resourceId]/route.ts), and added [apps/web/app/api/resources/[resourceId]/file/route.ts](../../apps/web/app/api/resources/[resourceId]/file/route.ts) so Documents now use authenticated PDF upload + streaming file delivery while Training stays JSON + YouTube-only and BO deletes replaced/removed stored files safely
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts), [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx), and [apps/web/app/agent/\_components/front-office-tracked-link.tsx](../../apps/web/app/agent/_components/front-office-tracked-link.tsx) so FO continues to show only the three approved lanes, treats all non-training resources as `Documents`, opens uploaded PDFs through the authenticated resource file route, and keeps existing interaction tracking without reintroducing the old BO complexity

- 2026-04-16: `Front Office / Resources passive-directory reset + office-admin management surface` now aligns the resource module to the PM-approved simpler directory model instead of continuing down the earlier execution-hub path:
  - updated [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx), [apps/web/app/agent/resources/front-office-resource-search-form.tsx](../../apps/web/app/agent/resources/front-office-resource-search-form.tsx), and [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so `/agent/resources` now behaves as a search-first passive directory with simple resource-type browsing and a separate vendor pool, while the underlying FO resource snapshot stops truncating the visible list to a small workbench subset
  - added [packages/db/src/office-resources.ts](../../packages/db/src/office-resources.ts), [packages/db/src/office-resources.test.ts](../../packages/db/src/office-resources.test.ts), and new `/api/office/resources*` routes under [apps/web/app/api/office/resources](../../apps/web/app/api/office/resources) so `office_admin` now has a real minimal CRUD layer for `Resource` and `Vendor` records plus lightweight `top opened / stale resources` signals without introducing approvals, workflow rules, or schema changes
  - replaced the old redirect-only [apps/web/app/office/resources/page.tsx](../../apps/web/app/office/resources/page.tsx), added [apps/web/app/office/resources/office-resources-client.tsx](../../apps/web/app/office/resources/office-resources-client.tsx), and updated [apps/web/app/office/office-nav.tsx](../../apps/web/app/office/office-nav.tsx) so Resources is now a real office-admin workspace rather than a jump to `/office/library`, while keeping `/office/library` as the separate Back Office document library
  - refined [apps/web/app/office/resources/page.tsx](../../apps/web/app/office/resources/page.tsx), [apps/web/app/office/resources/office-resources-client.tsx](../../apps/web/app/office/resources/office-resources-client.tsx), [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx), and [apps/web/app/agent/resources/front-office-resource-search-form.tsx](../../apps/web/app/agent/resources/front-office-resource-search-form.tsx) so the office side now shows directory coverage, starter guidance, and grouped management by resource type/vendor category, while the agent side now reads more like a stable searchable directory with common search examples, grouped browse-by-type sections, and clearer empty states instead of leftover hub-style copy

- 2026-04-16: `Listing Studio nested studio-nav + collections workspace` now turns Studio into a multi-surface FO module instead of one flat saved-listings entry:
  - updated [apps/web/app/\_components/workspace-nav.tsx](../../apps/web/app/_components/workspace-nav.tsx), [apps/web/app/agent/agent-nav.tsx](../../apps/web/app/agent/agent-nav.tsx), [apps/web/lib/i18n/messages/en-US.ts](../../apps/web/lib/i18n/messages/en-US.ts), and [apps/web/lib/i18n/messages/zh-CN.ts](../../apps/web/lib/i18n/messages/zh-CN.ts) so `Studio` now expands into `Dashboard / Listings / Collections / Shares`, stays auto-open on `/listing-studio/*`, and preserves the same branch behavior across desktop and mobile FO navigation
  - updated [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma), [packages/db/prisma/migrations/20260416113000_add_listing_studio_collections/migration.sql](../../packages/db/prisma/migrations/20260416113000_add_listing_studio_collections/migration.sql), [packages/db/src/studio-listings.ts](../../packages/db/src/studio-listings.ts), [packages/db/src/index.ts](../../packages/db/src/index.ts), and [packages/db/src/studio-listings.test.ts](../../packages/db/src/studio-listings.test.ts) so `Listing Studio` now has real Prisma-backed `StudioListingCollection / StudioListingCollectionItem` storage, current-membership-private collection scoping, normalized-name collision protection, item add/remove helpers, and database coverage tests instead of the old client-only prototype
  - added [apps/web/app/api/listing-studio/collections/route.ts](../../apps/web/app/api/listing-studio/collections/route.ts), [apps/web/app/api/listing-studio/collections/[collectionId]/route.ts](../../apps/web/app/api/listing-studio/collections/[collectionId]/route.ts), [apps/web/app/api/listing-studio/collections/[collectionId]/items/route.ts](../../apps/web/app/api/listing-studio/collections/[collectionId]/items/route.ts), and [apps/web/app/api/listing-studio/collections/[collectionId]/items/[packId]/route.ts](../../apps/web/app/api/listing-studio/collections/[collectionId]/items/[packId]/route.ts) so collections now have server-side CRUD and item membership routes instead of localStorage-only state
  - added [apps/web/app/listing-studio/studio-collection-picker.tsx](../../apps/web/app/listing-studio/studio-collection-picker.tsx), [apps/web/app/listing-studio/collections/page.tsx](../../apps/web/app/listing-studio/collections/page.tsx), [apps/web/app/listing-studio/collections/create-collection-form.tsx](../../apps/web/app/listing-studio/collections/create-collection-form.tsx), [apps/web/app/listing-studio/collections/[collectionId]/page.tsx](../../apps/web/app/listing-studio/collections/[collectionId]/page.tsx), [apps/web/app/listing-studio/collections/[collectionId]/listing-studio-collection-detail-client.tsx](../../apps/web/app/listing-studio/collections/[collectionId]/listing-studio-collection-detail-client.tsx), [apps/web/app/listing-studio/collections/[collectionId]/listing-studio-collection-map.tsx](../../apps/web/app/listing-studio/collections/[collectionId]/listing-studio-collection-map.tsx), [apps/web/app/listing-studio/shares/page.tsx](../../apps/web/app/listing-studio/shares/page.tsx), [apps/web/app/listing-studio/listings/page.tsx](../../apps/web/app/listing-studio/listings/page.tsx), [apps/web/app/listing-studio/listing-studio-card.tsx](../../apps/web/app/listing-studio/listing-studio-card.tsx), [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx), and [apps/web/app/globals.css](../../apps/web/app/globals.css) so Listing Studio now has a listed-style listings grid, a real collections index/detail flow, a shared collection picker reused by both list cards and detail view, a Google Maps / Places-powered collection map with `Supermarket / Subway / Restaurant / Coffee / Nightlife / All` pills, and a placeholder `Shares` page for the future share center

- 2026-04-15: `Listing Studio detail page listed-app reconstruction` now treats the saved-listing detail page as an information-first property view instead of an internal editorial workbench:
  - updated [packages/db/src/studio-listings.ts](../../packages/db/src/studio-listings.ts) so `StudioListingDetailSnapshot` now exposes stored `latitude / longitude` plus richer asset metadata to the detail page without changing Prisma schema, letting the page render in-stage map and floor-plan views from existing StreetEasy/Zillow import data
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) and [apps/web/app/listing-studio/listings/[packId]/page.tsx](../../apps/web/app/listing-studio/listings/[packId]/page.tsx) so the internal listing detail route now uses a listed-app-style information layout with a photo stage, in-place `FLOOR PLAN / MAP` switching, price/address/facts ordering, amenity and transit sections, and a top-right Acre action menu plus edit modal instead of the previous full-page editorial stack
  - updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so Listing Studio now has dedicated information-view styling for the rebuilt detail page, including the stage carousel, mode buttons, compact summary cards, transit/amenity sections, action menu, and internal edit modal

- 2026-04-15: `Listing Studio detail page default-state compression pass` now pushes the saved-listing workspace toward a shorter editorial default instead of leaving source, media, poster, and marketing tools fully open on first load:
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) so the detail page now keeps only essential packet-edit fields visible by default, moves secondary contact/copy blocks behind disclosures, collapses full source facts, full media curation, poster draft editing, poster preview, and deeper marketing bundles into explicit expand-on-demand sections, and compresses the right rail into cleaner `publish/export + reference` groups
  - updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so the Listing Studio detail shell now uses a narrower fixed-width right rail, clamps long hero copy, and supports the denser rail status stack needed by the shorter default layout

- 2026-04-14: `Listing Studio detail page simplification pass` now gives the saved-listing workspace a cleaner editing spine instead of forcing every packet task through one extra-long, rail-heavy scroll:
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) so the detail page now separates `hero + quick jump`, `curated page editor`, `imported snapshot`, `media library`, `poster studio`, and `marketing workspace` into clearer working zones, while moving save/share/export controls into a tighter right rail and collapsing lower-priority source / marketing subsections behind manual disclosures
  - updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so the Listing Studio detail layout now avoids the previous grid stretch / blank-space issue, uses denser form and summary grids, restores normal readable body copy inside key-value cards, and adds disclosure / sticky-rail styling that leaves more room for future modules without creating another wall of cards

- 2026-04-14: `Front Office intake OpenAI extraction bridge` now lets the existing review-first intake assist use server-side GPT extraction without replacing the live-form guardrails:
  - updated [apps/web/lib/front-office-intake-openai.ts](../../apps/web/lib/front-office-intake-openai.ts), [apps/web/lib/front-office-intake-ai.ts](../../apps/web/lib/front-office-intake-ai.ts), and [apps/web/lib/front-office-intake-assist-server.ts](../../apps/web/lib/front-office-intake-assist-server.ts) so the intake-assist route can now optionally call OpenAI with `OPENAI_API_KEY`, request strict JSON-schema lead suggestions from `gpt-5.4-mini`, and still fall back to the existing local OCR / local parser chain when the API is unavailable or returns nothing usable
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts) and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) so AI-returned field suggestions flow back through the same unresolved-first review/apply surface instead of introducing a second intake UI or bypassing live-form confirmation
  - updated [docs/env.md](../env.md) so the new optional `OPENAI_API_KEY` / `OPENAI_INTAKE_ASSIST_MODEL` contract is documented explicitly, including the current fallback and review-first boundary

- 2026-04-14: `Front Office page simplification playbook` now gives follow-on threads a stable rulebook for page cleanup instead of forcing every simplification pass to re-derive the same heuristics from chat:
  - added [docs/specs/frontoffice-page-simplification-playbook.md](frontoffice-page-simplification-playbook.md) so future threads have one repo-local source of truth for removing duplicate blocks, hiding empty/healthy states, suppressing technical implementation detail in default success paths, and keeping OCR / AI extraction surfaces focused on field values plus direct actions

- 2026-04-13: `Front Office red-spec wave 8 showcase-brief / template-flight / AI review-pack batch` now deepens the remaining redline surfaces without pretending Acre already owns public profile hosting or external marketing automation:
  - updated [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx) so the listings-side material center now exposes a `landing page brief`, `case-study reel`, and copyable `showcase bundle`, letting operators prepare a fuller public-facing identity package from the same manual showcase window instead of stopping at only intro-poster / closing-history snippets
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts) and [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) so `Listing Studio` now carries copyable `template briefs` for every poster mode plus reusable manual `campaign flights` for launch / open-house / evergreen cadence planning, pushing the template generator closer to a true marketing workbench while keeping it manual and Acre-owned
  - updated [packages/db/src/front-office-ai.ts](../../packages/db/src/front-office-ai.ts), [apps/web/app/agent/\_components/front-office-ai-explainability-surface.tsx](../../apps/web/app/agent/_components/front-office-ai-explainability-surface.tsx), [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx), and [apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx) so the rule-layer AI surface now ships a first `review pack` with per-rule checklist guidance and copy-ready drafts across dashboard and dossier instead of stopping at explanation-only playbook cards

- 2026-04-13: `Front Office red-spec wave 7 showcase-asset batch` now pushes the listings-side material center one step closer to the profile-window vision instead of keeping it only as a route-bound packet helper:
  - updated [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx) so the material center now exposes outward-facing `profile showcase assets`, including an `intro poster` and `closing history` strip beside the existing packet, readiness, send-plan, and launchpad surfaces, letting agents prepare a more public-facing identity package without leaving the same manual FO workbench

- 2026-04-13: `Front Office red-spec wave 6 delivery-plan batch` now turns the current manual material + marketing surfaces into clearer send-prep operators instead of only richer copy blocks:
  - updated [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx) so the listings-side material center now exposes an `asset readiness board` plus a route-aware `send plan`, letting the operator check portrait / contact / proof / route readiness and copy a recommended manual execution order before launching the next outbound touch
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts) and [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) so `Listing Studio` now arranges the existing marketing kit into a first manual `campaign delivery plan` with send-ready package sets, a delivery sequence, and a readiness checklist while staying on the same saved-packet / poster-draft content source

- 2026-04-13: `Front Office red-spec wave 5 draft-launch + campaign-bundle batch` now turns the current outbound helpers into tighter manual execution shortcuts instead of only richer copy surfaces:
  - updated [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx) so the listings-side material center now exposes direct `SMS / email draft launch` links that reopen the same focused lane with the companion package already loaded into manual draft assist, rather than making the operator copy support text first and rebuild the route by hand
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts) and [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) so `Listing Studio` now exposes reusable `campaign bundles` plus a full-kit copy action on top of the existing marketing-kit sections, keeping social / listing / follow-up copy grouped into one manual, review-first export stack

- 2026-04-13: `Front Office red-spec wave 4 launchpad + marketing-kit batch` now turns the newly added material / template surfaces into more actionable manual outbound tools instead of leaving them at preview-only depth:
  - merged [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx) so the listings-side material center now behaves more like a manual launchpad, with stable-route reopen, context-route reopen, clean-workspace reset, companion-package copy actions, and a more execution-oriented packet-mode surface instead of keeping route actions and packet framing separate
  - merged [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) and [apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts) so `Listing Studio` now exposes a first manual `marketing kit` with copy-ready social captions, listing blurbs, and follow-up notes derived from the same saved packet and current poster draft instead of forcing operators to rewrite campaign copy after every export

- 2026-04-13: `Front Office red-spec wave 3 packet workflow batch` now pushes the redline material + template surfaces from “clearer MVP” into a more reusable manual outbound workflow:
  - merged [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx) so the listings-side material center now includes an explicit `route block`, route-aware outbound packet framing, and a `recommended packet mode` layer that organizes `profile / contact / proof / route` together instead of leaving route context outside the package
  - merged [apps/web/app/api/listing-studio/listings/[packId]/route.ts](../../apps/web/app/api/listing-studio/listings/[packId]/route.ts), [packages/db/src/studio-listings.ts](../../packages/db/src/studio-listings.ts), and [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) so `Listing Studio` packets can now edit and persist `contact name / title / phone / email` inside the saved packet instead of treating the agent block as a fixed read-only import
  - merged [apps/web/app/api/listing-studio/listings/[packId]/poster/route.ts](../../apps/web/app/api/listing-studio/listings/[packId]/poster/route.ts), [apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts), and [apps/web/app/listing-studio/listings/[packId]/page.tsx](../../apps/web/app/listing-studio/listings/[packId]/page.tsx) so the poster / share / PDF side now stays aligned to that one saved contact block and the detail view explains packet distribution as one manual, review-first output chain instead of isolated export buttons

- 2026-04-13: `Front Office red-spec wave 2 material / poster / AI playbook batch` now deepens the first redline rollout instead of leaving the newly added modules at a shallow MVP edge:
  - updated [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx), [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx), and [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so the listings-side agent material rail now behaves more like a reusable material center with profile-sheet, contact-block, proof, and outbound packet framing instead of a thinner copy-helper strip
  - updated [apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts), [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx), [apps/web/app/listing-studio/listings/[packId]/page.tsx](../../apps/web/app/listing-studio/listings/[packId]/page.tsx), and [apps/web/app/api/listing-studio/listings/[packId]/poster/route.ts](../../apps/web/app/api/listing-studio/listings/[packId]/poster/route.ts) so `Listing Studio` posters now carry agent info and a scan-ready packet path directly inside preview / print / download output, preferring the public Acre packet when available and otherwise falling back to the original source listing
  - updated [packages/db/src/front-office-ai.ts](../../packages/db/src/front-office-ai.ts), [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [apps/web/app/agent/\_components/front-office-ai-explainability-surface.tsx](../../apps/web/app/agent/_components/front-office-ai-explainability-surface.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), and [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx) so the rule-layer AI contract now renders as a shared `playbook` with explicit do-now, prepare, and watch cues across dashboard and dossier while preserving the same review-first, no-auto-send FO boundary

- 2026-04-13: `Front Office red-spec calendar/material/template/AI batch` now closes the first implementation pass on the newly marked PRD additions instead of leaving them only as redline intent:
  - updated [apps/web/app/agent/calendar/calendar-view.ts](../../apps/web/app/agent/calendar/calendar-view.ts), [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx), and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) so `/agent/calendar` now supports true `day` and `week` agenda views grouped by date and time on top of the existing writeback lanes, rather than forcing every appointment review back through one coordination-only slice
  - updated [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx), [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx), [apps/web/app/listing-studio/listings/[packId]/page.tsx](../../apps/web/app/listing-studio/listings/[packId]/page.tsx), [apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-poster.ts), and [apps/web/app/api/listing-studio/listings/[packId]/poster/route.ts](../../apps/web/app/api/listing-studio/listings/[packId]/poster/route.ts) so the FO material rail now exposes a clearer `profile / contact / proof` package and `Listing Studio` now carries a first manual poster / template generator with editorial, open-house, social-square, and factsheet layouts plus preview, copy, print, and downloadable HTML poster output
  - updated [packages/db/src/front-office-ai.ts](../../packages/db/src/front-office-ai.ts), [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [apps/web/app/agent/\_components/front-office-ai-explainability-surface.tsx](../../apps/web/app/agent/_components/front-office-ai-explainability-surface.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx), and [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) so dossier and dashboard suggestions now share a first rule-layer strategy contract for `follow-up`, `silent period`, `holiday`, and `lease` reminders while keeping the same review-first, agent-approved, no-auto-send FO safety boundary

- 2026-04-09: `Front Office wave 4 cleanup/writeback/listings batch` now deepens three live operator lanes in parallel instead of only polishing one surface at a time:
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), [apps/web/app/agent/notifications/agent-notifications-config.ts](../../apps/web/app/agent/notifications/agent-notifications-config.ts), and [apps/web/app/agent/notifications/agent-notifications.module.css](../../apps/web/app/agent/notifications/agent-notifications.module.css) so `/agent/notifications` now reads more explicitly like a cleanup/operator workbench, with stronger focus-area framing, tighter lane cues, and clearer next-move language for personal cleanup, team cleanup, reminder pressure, and broader notices
  - updated [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) so `/agent/calendar` now treats `confirmation pending`, `touch scheduled`, `reschedule requested`, and `bridge opened / writeback pending` lanes as explicit coordination workbench states with clearer bridge-shell, writeback-shell, and next-touch guidance instead of flatter status summaries
  - updated [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts), [packages/db/src/front-office-listing-output.test.ts](../../packages/db/src/front-office-listing-output.test.ts), [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx), and [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) so `/agent/listings` now sharpens the tracked-share follow-through pulse, quiet-trail framing, and next-step storytelling while keeping automated regression coverage on the shared listing-output contract
- 2026-04-09: `Front Office resource adoption trend` now compares the current shared pulse with the prior window instead of showing only a raw 14-day snapshot:
  - updated [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts) so the shared FO resource interaction contract now carries `prior 14 days` comparison metadata and deltas for tracked actions, active operators, resource opens, and vendor clicks without changing schema
  - updated [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) and [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) so both the resource workbench and the FO dashboard now show compact momentum chips for the shared adoption pulse instead of forcing leadership viewers to infer trend from one flat snapshot
  - updated [packages/db/src/front-office-resources.test.ts](../../packages/db/src/front-office-resources.test.ts) and [packages/db/src/front-office-dashboard.test.ts](../../packages/db/src/front-office-dashboard.test.ts) so the shared pulse tests now verify prior-window comparison behavior with relative timestamps instead of fixed calendar dates
- 2026-04-09: `Front Office dashboard resource pulse` now brings visible team / office resource adoption back to the home workbench instead of hiding it only inside `/agent/resources`:
  - updated [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) so the dashboard snapshot now carries the same shared resource interaction pulse that already powers the FO resources hub, including visible-scope action counts, active-member counts, and hottest targets for leadership viewers without any schema change
  - updated [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) and [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) so `/agent/dashboard` now surfaces a compact shared adoption rail with a direct jump to the deeper `/agent/resources#shared-adoption-pulse` workbench section instead of keeping the homepage resource block static
  - added [packages/db/src/front-office-dashboard.test.ts](../../packages/db/src/front-office-dashboard.test.ts) so office-lead visibility and self-scope hiding for the dashboard resource pulse are both covered automatically
- 2026-04-08: `Front Office resource hub shared adoption pulse` now shows whether the visible team or office is actually using the published hub instead of stopping at one viewer’s recent clicks:
  - updated [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts) so the existing 14-day tracked interaction pipeline now also aggregates a visible-scope `team / office adoption pulse`, including shared action counts, active-member counts, top operators, and hottest resource/vendor targets without introducing new schema
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) and [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) so `/agent/resources` and the shared resources API snapshot now expose that adoption pulse to lead/admin viewers in the same workbench instead of hiding it inside audit-only data
  - added [packages/db/src/front-office-resources.test.ts](../../packages/db/src/front-office-resources.test.ts) so shared-adoption visibility is covered for both office-admin and self-scope agent viewers
- 2026-04-08: `/agent/notifications` now keeps unauthenticated behavior aligned with the rest of the Front Office workbench instead of streaming a loading shell before redirect:
  - removed the route-level loading boundary from [apps/web/app/agent/notifications/loading.tsx](../../apps/web/app/agent/notifications/loading.tsx) so signed-out requests now resolve directly through the existing server-side session redirect path
  - this keeps `Activity` aligned with the other `/agent/*` routes that send anonymous traffic straight to `/login` instead of returning a `200 + meta refresh` shell
- 2026-04-08: `/api/health` is public again so deployment checks and anonymous smoke tests can probe the live app without a session:
  - updated [apps/web/app/api/health/route.ts](../../apps/web/app/api/health/route.ts) so the route now returns the same service/organization/catalog payload without requiring an authenticated cookie
  - corrected the implementation log to keep `/api/health` out of the session-gated mock/API route list while protected feeds such as `/api/resources` and `/api/agent/dashboard` stay behind authentication
- 2026-04-08: `Front Office wave 3 writeback/intake/cleanup depth batch` now keeps the next FO move grounded in the same workbench instead of leaving writeback, intake review, and activity focus as looser copy-only hints:
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts) and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) so the intake assist summary now distinguishes `review-first`, `preview-only`, and `safe after review` more explicitly, while the manual-confirmation count now only tracks truly review-blocking fields instead of every softer preview row
  - updated [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), [apps/web/app/api/agent/appointments/[appointmentId]/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/route.ts), and [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) so appointment external coordination now reads as a saved checkpoint / promised next touch instead of a generic writeback note, including clearer bridge-follow-up guidance and validation hints on the update route
  - updated [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-client.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx), and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) so dossier section-focus copy now talks in clearer workbench-return terms for calendar, listing-output, cleanup, and FO/BO boundary re-entry instead of treating the anchored return point as a generic section jump
  - updated [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), and [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx) so dashboard shortcuts and AI suggestions now talk about `grounded next touch`, `calendar writeback`, and dossier-first rescue / warm-signal / boundary moves instead of broader generic workbench wording
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), [apps/web/app/agent/notifications/agent-notifications-config.ts](../../apps/web/app/agent/notifications/agent-notifications-config.ts), [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), and [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) so activity-center lanes now expose explicit focus labels, next-move guidance, stream-aware notice rails, and per-notice `next step` semantics instead of collapsing reminder and notice follow-through into one generic action label

- 2026-04-08: `Front Office wave 2 listings/material/share depth batch` now deepens the FO listings-side execution surface instead of stopping at stable route slices:
  - updated [apps/web/app/agent/listings/front-office-listings-route-state.ts](../../apps/web/app/agent/listings/front-office-listings-route-state.ts), [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx), and [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx) so `/agent/listings` now surfaces clearer re-entry guidance, tighter lane success feedback, and stronger send-lane next-move framing instead of leaving focused workbench routes as URL state plus raw counts
  - updated [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx), [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx), and [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so the supporting material / resource lanes now talk like active FO execution aids with recommended materials, focus-lane framing, and a clearer strongest-start path instead of static side reference copy
  - updated [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts), [apps/web/app/api/agent/listings/[listingId]/share-links/route.ts](../../apps/web/app/api/agent/listings/[listingId]/share-links/route.ts), and [apps/web/app/share/listings/[code]/page.tsx](../../apps/web/app/share/listings/[code]/page.tsx) so listing-share generation now carries an explicit FO execution contract into the API and public share surface, with clearer tracking / next-step / follow-up language instead of a thinner share-only payload
  - updated [apps/web/app/agent/agent-nav.tsx](../../apps/web/app/agent/agent-nav.tsx), [apps/web/app/\_components/workspace-nav.tsx](../../apps/web/app/_components/workspace-nav.tsx), and [apps/web/app/agent/layout.tsx](../../apps/web/app/agent/layout.tsx) so the FO shell now names `Execution` and `Handoff` more explicitly, labels the workspace switch as `Active workspace`, and keeps FO/BO boundary language visible at the shell level instead of only inside page-local copy
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), and [apps/web/app/agent/notifications/agent-notifications-config.ts](../../apps/web/app/agent/notifications/agent-notifications-config.ts) so `/agent/notifications` now reads more like a routed cleanup workbench with stronger route summaries, shareable cleanup framing, and next-move guidance instead of a generic mixed activity stack

- 2026-04-08: `Front Office wave 1 execution-depth batch` now makes the current FO workbench routes behave more like focused operating lanes instead of broad summaries:
  - updated [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) so dashboard launchpad copy now keeps the next move framed as a tighter FO workbench action instead of a broad summary jump
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts), [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx), and [apps/web/app/agent/\_components/front-office-lead-intake-review.ts](../../apps/web/app/agent/_components/front-office-lead-intake-review.ts) so quick intake now recognizes weekday-style timing cues more reliably and keeps unresolved-section review order visible while duplicate warnings can better distinguish contact matches from softer area / source overlap
  - updated [apps/web/app/agent/clients/[clientId]/front-office-client-chat-list-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-chat-list-client.tsx), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), and [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) so the dossier chat / phone strategy block now starts from the strongest live contact path and pushes the operator toward a dated next step instead of a generic script dump
  - updated [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), [apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts), [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts), [packages/db/src/front-office-calendar-links.ts](../../packages/db/src/front-office-calendar-links.ts), and [packages/db/src/front-office-calendar-links.test.ts](../../packages/db/src/front-office-calendar-links.test.ts) so bridge results now carry an explicit follow-up cadence into exported calendar / email payloads instead of leaving the outside action with only a status note
  - updated [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx), [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), and [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) so FO workspace routes now expose a clearer current-focus lane, route-slice guidance, and strongest-start resource/vendor guidance instead of only static page summaries

- 2026-04-08: `Front Office intake unresolved-first review pass` now pushes quick intake toward the riskiest review blocks before calmer recognized fields:
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) so grouped assist sections now carry a `priorityRank` that lifts unresolved and manual-confirmation sections ahead of already-recognized ones, keeping the first batch-review pass focused on what still needs operator judgment
  - that same intake surface now exposes explicit `Review unresolved sections first` and `Apply reviewed unresolved fields` actions, so the operator can clear the high-friction blocks as one coherent pass instead of scanning the whole assist stack from top to bottom every time

- 2026-04-08: `Front Office dossier section-focus re-entry` now explains which client rail is active when another FO workbench jumps back into the record:
  - updated [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx) so dossier section ids now resolve to shared labels and descriptions instead of leaving hash-based re-entry as a hidden implementation detail
  - updated [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-client.tsx) so the live dossier tracks `window.location.hash`, renders a visible `Re-entry focus` guidance item, and keeps the focused section explanation in sync as the user moves between anchored rails
  - updated [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) so the overview now surfaces the current section label / description and offers a direct jump back into that focused rail instead of treating anchored re-entry as invisible context

- 2026-04-08: `Front Office dashboard workbench route refinement` now makes launchpad and commitment shortcuts choose the tighter next workbench instead of broad summaries:
  - updated [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) so appointment commitments now derive a tighter `calendarView` from bridge activity, external workflow state, saved next action timing, and appointment metadata before reopening the calendar workbench
  - that same dashboard snapshot now falls back to `/agent/listings?lane=draft-lane` when the next move is general outbound prep rather than a client- or appointment-bound send trail, keeping listing launchers in a stable route contract instead of a broad desk summary
  - updated [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) so launchpad / commitment copy now reflects those tighter workbench routes directly in the user-facing CTA labels and supporting descriptions

- 2026-04-08: `Front Office notifications section-label drill-down` now makes cleanup cards explain which workbench rail they belong to before click-through:
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so cleanup items and notification items now derive a shared `sectionLabel` from their pressure type instead of relying only on `why now` and next-step text
  - updated [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) so personal cleanup, team cleanup, reminder, and notice cards now surface `Section` metadata beside `why now` / `next step`, making the activity center read more like a routed workbench than a generic inbox

- 2026-04-08: `Front Office calendar writeback lanes` now hardens future-touch and bridge-open slices into stable calendar aliases:
  - updated [apps/web/app/agent/calendar/calendar-view.ts](../../apps/web/app/agent/calendar/calendar-view.ts) so the shared `calendarView` contract now includes `touch_scheduled` and `writeback_pending` plus a reusable `getCalendarViewForWritebackReentry(...)` helper for save-flow routing
  - updated [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) so bridge actions, quick-status saves, preset saves, badges, and quick filters can reopen the same `Touch scheduled` or `Writeback pending` lane when Acre can prove that state
  - updated [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) so appointment summary counts and labels now expose those same workbench lanes instead of reducing them back into broader coordination buckets

- 2026-04-08: `Front Office client dossier lane re-entry alignment` now makes the dossier reopen the right FO workbench instead of falling back to broad summary routes:
  - updated [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) so dossier appointment cards, send records, AI accepted actions, and the next-step rail now all reuse shared lane-aware calendar / listings href builders instead of hardcoded broad `/agent/calendar?clientId=...` and `/agent/listings?clientId=...` links
  - that same dossier routing layer now intentionally keeps appointments with a future saved next touch in focused calendar view instead of falsely forcing them into `touch due` or `missing next touch`, while still preserving route-stable re-entry when Acre can prove a stronger writeback lane
  - updated [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) so the live appointment queue, send-record block, and empty-state actions now consume those lane-aware hrefs directly from the snapshot instead of reconstructing route intent in the page layer

- 2026-04-08: `Front Office calendar lane-aware writeback re-entry` now preserves the active writeback lane through save flows instead of dropping the operator back into a generic focused record:
  - updated [apps/web/app/agent/calendar/calendar-view.ts](../../apps/web/app/agent/calendar/calendar-view.ts) so external workflow state can resolve back into the shared `calendarView` aliases from one place instead of relying on page-local switch logic
  - updated [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) so bridge actions, quick-status saves, and touch-preset saves now refresh back into the owning `reply due`, `confirmation pending`, `externally confirmed`, `reschedule requested`, or `bridge logged` workbench when Acre can prove that lane

- 2026-04-08: `Front Office notifications section-aware cleanup re-entry` now sends cleanup pressure into the right dossier rail instead of a generic detail-page top:
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so cleanup items can target explicit client-section anchors such as the appointments / follow-up rail, listing-output rail, and next-step rail while preserving the existing duplicate-review and client-aware appointment-writeback re-entry paths
  - updated [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) so cleanup CTA copy now matches those anchored re-entry targets and reads like a workbench move instead of a generic queue jump

- 2026-04-08: `Front Office listings lane execution checklist` now turns the focused send lane into an explicit operator plan instead of URL state alone:
  - updated [apps/web/app/agent/listings/front-office-listings-route-state.ts](../../apps/web/app/agent/listings/front-office-listings-route-state.ts) so each focused `lane` now carries a label, description, and execution-step payload instead of only a stable href / diagnostics contract
  - updated [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx), [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx), and [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx) so the live send workbench and supporting rail now surface a lane-execution checklist beside the existing route / mode / package context

- 2026-04-08: `Front Office intake grouped review sections` now makes OCR / transcript assist review batchable instead of one flat list of suggested fields:
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) so assist suggestions are grouped into `identity`, `qualification`, `context`, `timing`, and `notes` sections with per-section review/apply actions on top of the existing field-level controls

- 2026-04-14: `Front Office intake assist` now renders as a lighter key-field extractor instead of a verbose review console:
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) so the assist entry copy, result header, duplicate hint, and field cards now focus on extracted values and direct apply actions instead of long queue/order/operator explanations
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts) so extraction summaries now describe the result as `key fields extracted` rather than exposing internal queue jargon
  - updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so extracted values read like compact field cards with short evidence text instead of a second audit-style console
  - this pass keeps the same safety model: fields still require review before apply, live-form replacement still needs explicit confirmation, and nothing auto-creates or auto-sends

- 2026-04-08: `Front Office listings focused route lanes` now turns `/agent/listings` into a stable send-lane workbench instead of only a generic tracked-link desk:
  - updated [apps/web/app/agent/listings/front-office-listings-route-state.ts](../../apps/web/app/agent/listings/front-office-listings-route-state.ts) so the listings route now carries a persistent `lane=send-rescue|follow-through|draft-lane` contract, stable lane-aware href builders, lane diagnostics, and focused-lane labels / descriptions on top of the existing `tracked-link / client-linked / appointment-linked` mode contract
  - updated [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) so workspace headings, recommended send lanes, share-lane plans, and stable re-entry links now read from the focused route lane instead of inferring everything from generic route mode alone
  - updated [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx) so the live rail now surfaces the active `Lane` alongside route, mode, draft, and package state, and the workspace-context stack now reopens the exact rescue / follow-through / draft lane instead of a broad generic listings route

- 2026-04-08: `Front Office dashboard shortcut hardening` now makes dashboard launch cards talk in stable workbench terms instead of broad summary jumps:
  - updated [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) so appointment commitments now reopen `/agent/calendar?calendarView=bridge_logged...`, tracked-send pressure now points to the client `next-step rail` or send-risk workbench, and leadership cleanup now exposes a cleaner `Open cleanup re-entry` next step
  - updated [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) so the launchpad now labels those shortcuts as `appointment workbench`, `send-risk workbench`, and `cleanup re-entry`, keeping route intent visible before the user clicks through

- 2026-04-08: `Front Office confirmed calendar lane` now hardens the last major appointment writeback slice into a stable route alias instead of a filter-only state:
  - updated [apps/web/app/agent/calendar/calendar-view.ts](../../apps/web/app/agent/calendar/calendar-view.ts) so `/agent/calendar` now also supports `calendarView=confirmed`, mapping confirmed outside responses into one stable workbench lane instead of forcing the operator to reconstruct that slice from `followUp=confirmed`
  - updated [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) so the quick filters, route normalization, and appointment cues now expose an explicit `Externally confirmed` lane alongside the existing reply / confirmation / bridge pressure lanes
  - updated [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) so the confirmed writeback summary now stays inside FO appointment language as `Externally confirmed` instead of drifting into FO -> BO handoff terminology

- 2026-04-08: `Front Office notifications cleanup re-entry copy` now pushes cleanup lanes toward explicit workbench actions instead of generic queue labels:
  - updated [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) so duplicate-review cleanup and personal/team cleanup CTAs now consistently say `workbench` / `re-entry` and keep duplicate merge work pointed at the dedicated clients lane
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so cleanup items now expose clearer next-step labels such as `Open follow-up workbench`, `Open next-touch workbench`, `Open send-risk workbench`, `Open recovery workbench`, and a client-aware calendar writeback workbench action

- 2026-04-08: `Front Office intake review clarity` now deepens the quick lead-intake assist instead of leaving OCR / transcript suggestions as one flat preview list:
  - extended [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts) so the assist result now emits explicit `recognized`, `manual confirmation`, and `not extracted` field groupings plus a readable reason for why the remaining fields stayed out of the live form
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) so `/agent/dashboard` and `/agent/clients` now show those review layers directly above the field-by-field assist actions, making it clearer what Acre found versus what still needs human confirmation
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-review.ts](../../apps/web/app/agent/_components/front-office-lead-intake-review.ts) so duplicate warnings now push the operator through `open existing record -> compare contact / stage / next touch -> use duplicate review or create separately` instead of a generic stop message

- 2026-04-08: `Front Office calendar bridge follow-up clarity` now turns bridge exports into a more explicit next-step workflow instead of leaving Google / Outlook / ICS / email jumps as the last visible action:
  - updated [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) so the appointment follow-up rhythm and bridge guidance now call out `Bridge opened, writeback pending`, stronger manual-only language, and a clearer saved-next-touch expectation after the outside action
  - updated [packages/db/src/front-office-calendar-links.ts](../../packages/db/src/front-office-calendar-links.ts) so exported calendar / email drafts now explicitly remind the agent to save the resulting confirmation / reschedule / follow-up writeback back into Acre
  - updated [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) and [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx) so the live FO calendar now exposes an `After the bridge` checkpoint, a `Jump to writeback` action, a `Load suggested next touch` action, and clearer coordination-pressure lane names such as `Reply due`, `Confirmation pending`, `Missing next touch`, and `Bridge logged`

- 2026-04-08: `Front Office activity cleanup workbench` now surfaces more explicit next-step guidance instead of stopping at counts and focus slices:
  - extended [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) and [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) so cleanup items and leadership activity-center rows now carry a dedicated `nextStepLabel` alongside the existing pressure, scope, and why-now copy
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), and [apps/web/app/agent/notifications/agent-notifications-config.ts](../../apps/web/app/agent/notifications/agent-notifications-config.ts) so `/agent/notifications` now leans harder into `workbench` language and exposes lane-level next-step cues for personal cleanup, team cleanup, appointment reminders, and notice lanes without changing the existing query-param contract

- 2026-04-08: `Front Office dossier appointment writeback clarity` now carries the bridge workflow back into the client record instead of making the agent bounce through the calendar route every time:
  - extended [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) so dossier appointment items now include `calendarWritebackHref`, `bridgeNextStepLabel`, and `bridgeNextStepDetail` alongside the existing external-status and bridge-status fields
  - updated [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) so appointment cards now expose an `Open calendar writeback` action plus clearer `next touch`, bridge status, and bridge-next-step copy directly in the dossier queue

- 2026-04-08: `Front Office clients workbench route focus` now makes `/agent/clients` reopen into explicit execution lanes instead of one generic queue shell:
  - updated [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx) so the route now accepts `clientView=all|follow_first|anchor_now|viewing_lane|boundary_review|duplicate_review`, adjusts page copy to the active lane, and keeps quick links anchored to the matching workbench section
  - updated [apps/web/app/agent/clients/front-office-client-duplicates-card.tsx](../../apps/web/app/agent/clients/front-office-client-duplicates-card.tsx) and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) so duplicate-review feedback and intake duplicate warnings now reopen the dedicated duplicate-review lane instead of dropping the agent into an unfocused client list

- 2026-04-08: `Front Office dashboard workbench shortcuts` now align dashboard launch actions with those focused FO routes instead of broad summary links:
  - extended [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) so dashboard queue items now carry `whyNowLabel` and `nextStepLabel`, plus stronger action labels and focused client-workbench hrefs for follow-up, lease timing, formal handoff, and leadership cleanup
  - updated [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) so launchpad cards, lane shortcuts, and duplicate-review re-entry now open stable `/agent/clients?clientView=...` targets or focused cleanup routes instead of broad `/agent/clients` summary jumps

- 2026-04-08: `Front Office calendar view route contract` now turns the main writeback lanes into stable route aliases instead of ad-hoc filter combinations:
  - added [apps/web/app/agent/calendar/calendar-view.ts](../../apps/web/app/agent/calendar/calendar-view.ts) so the calendar route now has reusable `calendarView` aliases for `reply_due`, `confirmation_pending`, `touch_due`, `missing_next_touch`, `reschedule_requested`, and `bridge_logged`
  - updated [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) so queue filters, quick lane buttons, and route normalization now consume `calendarView` while preserving the existing `appointmentId` focus lock behavior
  - updated [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx) so page-level copy and snapshot loading now follow the active calendar workbench lane instead of a generic calendar shell

- 2026-04-08: `Front Office cleanup re-entry hardening` now routes duplicate review and appointment writeback back into the right focused workbench instead of broad holding routes:
  - extended [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so appointment cleanup targets now preserve `clientId + appointmentId` when available, and the target appointment in listings-side snapshots uses the same tighter calendar href shape
  - updated [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) so duplicate-review cleanup now re-enters through `/agent/clients?clientView=duplicate_review#duplicate-review` and the personal cleanup copy explicitly points there when duplicate review is the active pass

- 2026-04-08: `Front Office dashboard appointment writeback shortcuts` now send appointment commitments back into exact calendar focus instead of a broad summary route:
  - updated [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) so appointment commitments now link to `clientId + appointmentId` calendar writeback URLs when possible, and the dashboard copy now talks about opening the writeback directly instead of a general calendar focus

- 2026-04-07: `Front Office activity center drill-down workbench` now deepens the existing `/agent/notifications` cleanup route instead of leaving office-level cleanup capped at preview-only leadership rows:
  - extended [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) so leadership cleanup now keeps its existing dashboard preview rows while also returning a deeper notifications-only workbench item set, plus more explicit overdue-task age copy and a focus-preserving team-cleanup launch URL
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), and [apps/web/app/agent/notifications/agent-notifications-config.ts](../../apps/web/app/agent/notifications/agent-notifications-config.ts) so personal cleanup, team cleanup, appointment reminders, and general notices now each expose focused drill-down cards inside their dedicated lane view while still preserving the existing `activityView / cleanupFilter / teamCleanupFilter / appointmentFilter / readState` URL behavior
  - updated [docs/specs/frontoffice-data-contract.md](frontoffice-data-contract.md) and [docs/specs/notifications-spec.md](notifications-spec.md) so the FO activity docs now describe the route as a deeper drill-down workbench, with preview-sized dashboard leadership rows and a fuller notifications-side office cleanup pass

- 2026-04-03: `appointment external touch due` notifications are now live on top of the earlier appointment writeback layer instead of leaving that promised outside follow-up window as cleanup-only pressure:
  - updated [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma) plus a new Prisma migration so the notification model now includes a dedicated `appointment_external_touch_due` family instead of overloading generic appointment reminders
  - extended [packages/db/src/notifications.ts](../../packages/db/src/notifications.ts), [packages/db/src/activity.ts](../../packages/db/src/activity.ts), [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), and [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so appointment `next external touch` deadlines now reconcile into the shared inbox, show up with a readable `External touch due` label across FO notice surfaces, and stale appointment timing/writeback reminders are deleted when the appointment no longer matches the active reminder window
  - updated [apps/web/app/office/notifications/notifications-client.tsx](../../apps/web/app/office/notifications/notifications-client.tsx), [docs/specs/notifications-spec.md](notifications-spec.md), [docs/specs/frontoffice-data-contract.md](frontoffice-data-contract.md), [docs/specs/frontoffice-overview.md](frontoffice-overview.md), and [docs/specs/product-coverage-audit.md](product-coverage-audit.md) so the product docs and user-facing copy now describe appointment external touch deadlines as a real inbox-backed reminder, not only a cleanup metric
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) plus [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) again so the FO activity route now splits appointment reminder pressure into a dedicated queue with explicit `confirmation due`, `reschedule follow-up`, `external touch due`, and `appointment soon` groupings instead of leaving those notices inside one undifferentiated stream
  - added [apps/web/app/agent/notifications/[notificationId]/open/page.tsx](../../apps/web/app/agent/notifications/[notificationId]/open/page.tsx) and updated [packages/db/src/notifications.ts](../../packages/db/src/notifications.ts) plus [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so Agent-side activity notices now mark themselves read before redirecting into the live calendar / dossier target instead of leaving unread counts untouched after the user already acted on the reminder
  - added [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), [apps/web/app/api/agent/notifications/route.ts](../../apps/web/app/api/agent/notifications/route.ts), and [apps/web/app/api/agent/notifications/[notificationId]/route.ts](../../apps/web/app/api/agent/notifications/[notificationId]/route.ts) so the FO activity route now supports reminder-type filtering, per-notice read/unread toggles, and mark-all-read for the currently visible Agent-side notice stack instead of staying a read-only view
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) and [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) again so Agent-side reminder-type and read-state filters now persist in the route URL and survive refresh / reopen flows instead of resetting to `all` after every navigation
  - extended [packages/db/src/notifications.ts](../../packages/db/src/notifications.ts), [apps/web/app/api/agent/notifications/route.ts](../../apps/web/app/api/agent/notifications/route.ts), and [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) so the Agent-side activity center now supports `mark all in view as unread` alongside the earlier batch read action instead of leaving bulk state control one-directional
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) again so the existing dashboard leadership queue now also appears in the `/agent/notifications` rail for team leads and office admins, keeping team cleanup pressure beside the personal activity stack instead of isolating it on `/agent/dashboard`
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) plus [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) again so that same leadership queue now also renders as a dedicated main-stack team cleanup section with scope stats, while the rail stays as a lighter summary instead of carrying the only visible copy of that queue
  - extended [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), and [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) again so leadership cleanup items now carry an explicit pressure type and `/agent/notifications` can persist a focused team-cleanup filter for overdue tasks, stale dossiers, or send-trail risk in the route URL instead of forcing leaders back into the full mixed queue
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) plus [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) again so the FO activity center now persists a higher-level `activityView` focus state in the route URL and only renders the relevant main-stack sections for personal cleanup, team cleanup, appointment reminders, or broader notices instead of always dropping the user back into the full mixed page
  - this pass still stays honest about scope: Acre now reconciles a stronger manual inbox/writeback loop for outside appointment follow-up, but it still does not claim true two-way sync or external delivery ownership

- 2026-04-03: `appointment next external touch writeback` is now live on top of the earlier bridge + status layer instead of leaving the promised outside follow-up window in an agent's head:
  - extended [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) plus [packages/db/src/date-time.ts](../../packages/db/src/date-time.ts), so FO appointment writeback metadata can now persist an optional `next external touch` timestamp alongside the current external workflow state and note, and the appointment snapshot now returns that timing back to the UI as both label and form value
  - updated [apps/web/app/api/agent/appointments/[appointmentId]/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/route.ts), [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx), and [apps/web/app/globals.css](../../apps/web/app/globals.css) so agents can now save `status + next external touch + note` directly from the live appointment card instead of only flipping the external state dropdown
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [docs/specs/frontoffice-data-contract.md](frontoffice-data-contract.md), [docs/specs/frontoffice-overview.md](frontoffice-overview.md), and [docs/specs/product-coverage-audit.md](product-coverage-audit.md) so the FO cleanup center and product docs now treat appointment cleanup as driven by either the near-term meeting window or the promised external confirmation / reschedule deadline
  - this pass still stays honest about scope: Acre now tracks a deeper manual writeback contract for outside calendar / inbox work, but it still does not claim full two-way sync or outbound delivery guarantees from external systems

- 2026-04-02: `appointment external follow-up writeback` is now live on top of the earlier logged bridge layer instead of leaving confirmation and reschedule state outside Acre:
  - extended [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) so scheduled FO appointments now carry a typed external workflow state in appointment metadata, including `idle`, `needs follow-up`, `awaiting confirmation`, `confirmed`, and `reschedule requested`, while still reusing the existing appointment update activity contract instead of inventing a second event store
  - updated [apps/web/app/api/agent/appointments/[appointmentId]/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/route.ts), [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx), and [apps/web/app/globals.css](../../apps/web/app/globals.css) so agents can update that writeback state directly from the live FO calendar without leaving the appointment queue
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) so appointment follow-up, confirmation, and reschedule pressure now show up in the unified FO cleanup center and the client dossier instead of staying hidden behind the last bridge click
  - this pass still stays honest about scope: Acre now records agent-managed external follow-up state alongside bridge actions, but it still does not claim true outbound email delivery or two-way calendar sync with outside systems

- 2026-04-02: the first `appointment bridge writeback` layer is now live on top of the earlier external calendar / email bridge instead of leaving those actions as invisible exports:
  - updated [packages/db/src/front-office-calendar-links.ts](../../packages/db/src/front-office-calendar-links.ts) so Google Calendar, Outlook, ICS, and email-brief actions now route through internal Acre bridge URLs first, while still resolving to the same external destination or downloadable ICS payload
  - extended [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts), [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), and [packages/db/src/activity-log.ts](../../packages/db/src/activity-log.ts) so each bridge action is written into the shared `AuditLog` as appointment activity and the latest logged bridge status now comes back in both calendar and dossier appointment snapshots
  - added [apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts), and updated [apps/web/app/api/agent/appointments/[appointmentId]/ics/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/ics/route.ts), [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx), and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) so the live FO surfaces can both open those bridges and show the latest external action status without inventing a second appointment-sync database
  - this pass still stays honest about scope: Acre now records the bridge action and reflects it back in FO, but it still does not claim true two-way calendar or inbox sync with outside systems

- 2026-04-02: `Front Office OCR / transcript intake assist` is now live inside the quick lead-intake card instead of remaining only a follow-up roadmap item:
  - added [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts) so Acre can turn pasted chat text or OCR output into structured lead-intake suggestions for name, phone, email, source, stage, intent, budget, areas, next follow-up date, and notes
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) so `/agent/dashboard` and `/agent/clients` now expose a `Screenshot OCR / transcript assist` block directly above the quick lead form, using browser-side extraction and conservative fill rules that only apply to empty/default intake fields
  - updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so the new assist surface stays inside the existing FO lead-intake visual language instead of introducing a one-off import widget
  - this first pass stays honest about scope: it is a browser-side beta for faster first-call capture, not a provider-backed OCR ingestion pipeline, not a WeChat integration, and not an auto-create lead flow

- 2026-04-02: `Front Office activity + cleanup center` is now live on top of the reminder / duplicate foundation instead of keeping activity, cleanup, and notices spread across separate FO surfaces:
  - extended [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so `getFrontOfficeActivitySnapshot(...)` now emits one FO cleanup queue combining due follow-up work, stale clients, tracked-send risk, near-term appointments, visible-scope duplicate review, and the existing notice / event stream instead of only returning notifications and events
  - updated [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) so `/agent/notifications` now behaves as a true `Activity + Cleanup Center`, with the cleanup queue shown before the notice stream and the existing duplicate-merge surface mounted directly inside the same FO center
  - this first pass stays honest about scope: it unifies the active FO execution pressure into one center, but it does not yet claim full office-wide cleanup management, OCR-assisted intake, or two-way external calendar / inbox sync

- 2026-04-02: `Front Office visible-scope duplicate governance` is now live on top of the first dedupe pass instead of stopping at the current owner's queue:
  - updated [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so `/agent/clients` duplicate candidates are now derived from the CRM records currently visible to the viewer through contact scope resolution, while the main FO list itself still stays intentionally self-scoped
  - extended [packages/db/src/contacts.ts](../../packages/db/src/contacts.ts), [apps/web/app/api/agent/clients/route.ts](../../apps/web/app/api/agent/clients/route.ts), and [apps/web/app/api/agent/clients/merge/route.ts](../../apps/web/app/api/agent/clients/merge/route.ts) so quick lead intake duplicate warnings and merge authorization both operate on that same visible CRM scope instead of only the current agent-owned queue
  - updated [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx), [apps/web/app/agent/clients/front-office-client-duplicates-card.tsx](../../apps/web/app/agent/clients/front-office-client-duplicates-card.tsx), and [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx) so the UI now tells the agent when a duplicate is in their own FO queue versus only visible through broader CRM permissions, and the review CTA routes to either the FO dossier or the Office contact workspace accordingly

- 2026-04-02: a first `external calendar / email bridge` is now live on FO appointment surfaces instead of remaining only a spec bullet:
  - added [packages/db/src/front-office-calendar-links.ts](../../packages/db/src/front-office-calendar-links.ts) so Google Calendar, Outlook, ICS, and client-email draft links all derive from the same FO appointment data instead of each UI surface composing its own export logic
  - extended [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) and [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) so both `/agent/calendar` and the client dossier expose those external bridge actions directly on scheduled appointments
  - added [apps/web/app/api/agent/appointments/[appointmentId]/ics/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/ics/route.ts), so agents can download a real ICS file from the FO appointment record instead of re-entering event details by hand
  - updated [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx), and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), so agents can push scheduled appointments into Google Calendar / Outlook, download ICS, or send a client-facing schedule email without leaving the active FO shell
  - this first pass stays intentionally honest about scope: it is an action-first export bridge, not a claimed two-way sync with external systems

- 2026-04-02: `AI explainability + FO / BO boundary hardening` are now live across the dossier and dashboard instead of remaining only the next FO recommendation:
  - [packages/db/src/front-office-ai.ts](../../packages/db/src/front-office-ai.ts) now emits a shared execution-boundary contract, so grounded AI suggestions can explain whether the work should stay in Front Office, align to an already-live Back Office file, or move into a formal BO handoff next
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) and [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) now attach that contract to both dossier suggestions and dashboard queue items, including explicit reasoning for the primary CTA and why one-click follow-up is available or intentionally paused
  - added [apps/web/app/agent/\_components/front-office-ai-explainability-surface.tsx](../../apps/web/app/agent/_components/front-office-ai-explainability-surface.tsx), and updated [apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx) plus [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx), so both FO surfaces now render the same structured explanation instead of scattering rationale into ad hoc strings
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) now promotes context-aware primary actions such as `Open calendar` and `Open Back Office create flow`, and BO-ready handoff suggestions no longer expose one-click follow-up creation as a competing shortcut
  - updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so the new explainability surface stays inside the existing `Front Office` playbook visual language instead of introducing a third AI-specific shell

- 2026-04-02: `Front Office dedupe / merge` is now live inside `/agent/clients` instead of stopping at create-time warning only:
  - added [packages/db/src/contacts.ts](../../packages/db/src/contacts.ts) `mergeFrontOfficeClients(...)`, so the current agent-owned FO queue can safely reconcile duplicate client records by moving appointments, follow-up tasks, tracked send history, AI accepted actions, handoff drafts, transaction-contact links, and transaction primary-client pointers onto one surviving dossier before the duplicate record is deleted
  - extended [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) so the `/agent/clients` snapshot now surfaces exact same-email / same-phone / same-name duplicate pairs with a recommended keep record instead of leaving agents to discover duplicates only at create time
  - added [apps/web/app/api/agent/clients/merge/route.ts](../../apps/web/app/api/agent/clients/merge/route.ts) plus [apps/web/app/agent/clients/front-office-client-duplicates-card.tsx](../../apps/web/app/agent/clients/front-office-client-duplicates-card.tsx), and updated [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx), so agents can review both records, see why Acre recommends a survivor, and merge the duplicate directly from the live FO pipeline page
  - updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so the new duplicate-review surface keeps the same `Front Office` action-first visual language instead of introducing a one-off admin panel shell

- 2026-04-02: `quick lead intake + lightweight duplicate guard` is now live inside the active FO shell instead of remaining only a dashboard expectation:
  - added [apps/web/app/api/agent/clients/route.ts](../../apps/web/app/api/agent/clients/route.ts) so Front Office now has its own lightweight client-create entry point that writes directly into the existing shared `Client` / stage-history / handoff foundation instead of bouncing through the Back Office contacts workspace
  - added [packages/db/src/contacts.ts](../../packages/db/src/contacts.ts) duplicate-match helpers, so quick intake can warn on same-email / same-phone / same-name records already owned by the current agent before a second FO dossier is created
  - added [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) and mounted it on [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) plus [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx), so agents can capture a real FO lead record, first follow-up date, and stage context from the two highest-frequency FO surfaces
  - updated [apps/web/app/globals.css](../../apps/web/app/globals.css) so the new intake surface keeps the same `Front Office` action-first visual language instead of introducing a page-local form shell

- 2026-04-02: `outcome-informed AI ranking + safe escalation rules` are now live on top of the FO AI bridge instead of remaining only the next roadmap line:
  - [packages/db/src/front-office-ai.ts](../../packages/db/src/front-office-ai.ts) now centralizes accepted-action outcome mapping, recent history indexing, ranking insight, and safe escalation signals so dossier and dashboard stop maintaining two separate interpretations of AI results
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) now feeds recent accepted-action history back into dashboard queue ordering, promoting suggestion kinds with stronger recent outcomes while escalating stalled accepted actions to the top for review instead of leaving the queue purely static
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) now uses that same history index inside the dossier suggestion builder, so the active suggestion can carry measurable outcome signals and suppress one-click duplicate follow-up creation when a similar accepted follow-up is still unresolved
  - [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx) and [apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx) now expose that safer review behavior in the FO UI by hiding one-click follow-up creation when Acre detects a same-kind AI-created follow-up that still needs review
  - [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now also show a compact by-kind AI outcome summary, so agents can see which suggestion kinds are actually converting instead of only reading the top-line accepted / positive totals
  - [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx) and [apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx) now split `why now` from `what changed the priority`, so live dossier signals and history-driven ranking/escalation cues no longer appear as one mixed meta string
  - [packages/db/src/front-office-ai.ts](../../packages/db/src/front-office-ai.ts), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now also show compact `Last 7d / Last 90d` accepted-action windows, so agents can compare recent AI conversion by suggestion kind instead of reading only the lifetime-ish 90-day rollup
  - added [packages/db/src/front-office-ai.test.ts](../../packages/db/src/front-office-ai.test.ts) so the new ranking / escalation rules have direct service-level coverage without depending on Prisma fixtures

- 2026-04-02: `accepted-action history + AI outcome tracking` is now live on top of the FO AI bridge instead of remaining only the next roadmap line:
  - added `FrontOfficeAiAcceptedAction` to [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma) plus migration [packages/db/prisma/migrations/20260402103000_frontoffice_ai_accepted_actions/migration.sql](../../packages/db/prisma/migrations/20260402103000_frontoffice_ai_accepted_actions/migration.sql), so accepted AI follow-up creation and AI-assisted tracked sends now persist as first-class FO records tied back to the shared follow-up task or send record instead of disappearing after the click
  - [packages/db/src/front-office-ai.ts](../../packages/db/src/front-office-ai.ts), [packages/db/src/contacts.ts](../../packages/db/src/contacts.ts), [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts), [apps/web/app/api/agent/clients/[clientId]/follow-up-tasks/route.ts](../../apps/web/app/api/agent/clients/[clientId]/follow-up-tasks/route.ts), and [apps/web/app/api/agent/listings/[listingId]/share-links/route.ts](../../apps/web/app/api/agent/listings/[listingId]/share-links/route.ts) now carry structured accepted-action metadata through the same API path that creates the real shared task or tracked send
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now expose `Accepted AI actions & outcomes` inside the dossier, including acceptance counts, positive-outcome counts, and recent outcome rows tied to task completion or tracked opens
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx), [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx), and [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now complete the metadata chain from dashboard acceptance or dossier draft assist into visible outcome history

- 2026-04-02: `agent-approved one-click follow-up creation + outbound draft assist` are now live on top of the FO AI bridge instead of remaining only the next roadmap line:
  - added [packages/db/src/front-office-ai.ts](../../packages/db/src/front-office-ai.ts) so the grounded AI layer now has one shared follow-up-action contract for both dossier and dashboard acceptance flows, keeping due-date/title suggestions aligned instead of duplicating ad hoc task text
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) now emits a concrete `followUpSuggestion` inside the dossier AI snapshot, and [apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx) can now create that shared follow-up task directly or open tracked outbound draft assist into listing output
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx), and [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) now let the dashboard AI queue create shared follow-up tasks with one click instead of stopping at safe prefill only
  - [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx) and [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now accept AI outbound draft assist in search params, so matching SMS / email copy actions reuse grounded draft language while still appending a private tracked listing link and writing the normal send record trail

- 2026-04-02: `dashboard-level AI queue + safe automation actions` are now live on top of the dossier AI bridge instead of remaining only the next roadmap line:
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) now derives grounded AI queue items from the same FO execution trail, surfacing lease-timing, appointment-prep, tracked-send, BO-handoff, closing, and post-close opportunities directly on `/agent/dashboard`
  - [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) now renders an `AI next-touch queue` for memberships with `ai:use`, and each queue item offers a safe automation path into a prefilled FO follow-up instead of auto-creating or auto-sending in the background
  - [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) plus [front-office-client-dossier-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-client.tsx) now accept AI-prefilled follow-up query params, so dashboard actions can land agents inside the shared follow-up form with the suggested task title and due date already loaded

- 2026-04-02: `AI next-touch suggestions` are now live inside the FO client dossier instead of remaining only the next roadmap line:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) now turns the live dossier trail into a grounded AI suggestion snapshot, combining workflow pressure, lease timing, appointments, tracked send/open behavior, BO handoff readiness, and formal transaction milestones into one recommended next touch plus copyable call / text / email drafts
  - the first AI bridge intentionally stays agent-approved and non-destructive: the drafts are copyable and the primary CTA routes back into existing FO/BO actions, but Acre does not auto-send anything or invent a standalone model service
  - [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) and [front-office-client-ai-suggestions-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-ai-suggestions-client.tsx) now render an `AI next-touch suggestions` section for memberships with `ai:use`, so agents can copy grounded drafts straight from the same dossier that already carries follow-up, send history, negotiation, inspection, closing, and PDF context

- 2026-04-01: `closing / deal-win suggestions` are now live inside the FO client dossier instead of remaining only the next roadmap line:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) now bridges the live dossier into shared BO transaction outcome signals, so each client can be read as `Pre-win prep`, `Ready for deal wrap`, `Formal deal in flight`, `Closing soon`, `Fresh win`, or `Post-close nurture` without inventing a second “closed wins” store
  - that same FO snapshot now turns formal BO status plus acceptance / closing / move-in dates into actionable FO suggestions such as confirming the close date, placing the first post-close touch, downloading the recap PDF, asking for referrals / testimonials while the win is fresh, or routing the agent back to respectful nurture when a file is cancelled
  - [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now renders a `Closing & win suggestions` section with dossier-native CTA routing, so agents can act on close-stage guidance directly from the same FO record that already carries follow-up, send history, negotiation, inspection, and PDF export context

- 2026-04-01: `PDF export / client-facing report delivery` is now live off the FO client dossier instead of remaining only the next roadmap line:
  - added [front-office-client-summary-pdf.tsx](../../apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx) plus [route.ts](../../apps/web/app/api/agent/clients/[clientId]/pdf/route.ts), so `/api/agent/clients/[clientId]/pdf` now renders a client-ready summary PDF from the live FO dossier snapshot with upcoming schedule, shared materials, negotiation status, and contract-support summary
  - the export deliberately wraps the same live FO/BO execution trail instead of inventing a second reporting model, and it omits internal-only note content while keeping formal transaction, signature, and archival truth in the shared BO workflow
  - [page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now exposes a `Download client PDF` action in the dossier overview so agents can package the current state directly from the client record
- 2026-04-01: `inspection / contract-support bridge` is now live inside the FO client dossier instead of remaining only the next roadmap line:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) now bridges the live dossier into existing BO transaction foundations, so each client can be read as `Front Office prep`, `Ready for contract file`, `Contract file live`, or `Inspection-era live` without inventing a second inspection store
  - that same FO snapshot now reads the shared BO transaction task, signature-request, and incoming-update queues directly when a linked transaction exists, surfacing open-task pressure, pending signature work, review queue counts, and direct links back into `/office/transactions/[transactionId]#transaction-tasks`, `#transaction-forms-signatures`, and `#transaction-incoming-updates`
  - [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now renders an `Inspection & contract support` section with dossier-native CTA routing, so agents can see whether to stay in FO prep, open the BO create flow, or work directly from the formal BO contract workspace during the inspection-era window
- 2026-04-01: `offer / negotiation bridge` is now live inside the FO client dossier instead of remaining only a roadmap line:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) now bridges the live dossier to the existing BO handoff + offer foundations, so each client can be read as `Front Office prep`, `Ready for BO handoff`, or `BO workspace live` without inventing a second offer store
  - that same FO snapshot now reads the shared BO offers snapshot when a linked transaction already exists, surfacing offer count, expiring-soon count, accepted / primary state, and direct offer links back into `/office/transactions/[transactionId]#transaction-offers`
  - [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now renders an `Offer & negotiation` section with dossier-native CTA routing, so agents can see whether to keep prep in FO, open the BO create flow, or work directly from the formal BO offer workspace
- 2026-04-01: `leadership/team-level overdue engagement views` are now live on top of the existing FO send trail instead of remaining only a spec target:
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) now expands leadership scope from only overdue follow-up tasks and 15+ day stale clients into a broader execution-pressure queue, including tracked-send risk derived from the latest `FrontOfficeSendRecord` per client
  - that same leadership snapshot now flags `3+ day no tracked open` and `7+ day quiet after last tracked open` using the existing send trail, and surfaces stage + appointment context captured on the send record so management can see why the outreach happened before it went quiet
  - [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) now shows a dedicated `Send-trail risk` stat inside the leadership card and updates the section copy so team leads / office admins read the queue as execution oversight instead of only follow-up aging
- 2026-04-01: `deeper send records tied to appointments and client stage` is now live on top of FO listing output instead of remaining only a planning note:
  - added `appointmentId`, `clientStageLabel`, `appointmentTitle`, and `appointmentStartsAt` to [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma) plus migration [packages/db/prisma/migrations/20260401195500_frontoffice_send_context/migration.sql](../../packages/db/prisma/migrations/20260401195500_frontoffice_send_context/migration.sql), so send records now preserve execution context instead of only the raw share action
  - [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts) and [apps/web/app/api/agent/listings/[listingId]/share-links/route.ts](../../apps/web/app/api/agent/listings/[listingId]/share-links/route.ts) now accept optional appointment context, infer the right client when launching from a client-linked appointment, and snapshot the client stage at send time
  - [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx), and [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now support `appointmentId` send context alongside `clientId`, so the output terminal can be opened from a scheduled showing/consultation path instead of only from the generic dossier send button
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), and [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) now surface stage and appointment context directly in send history / engagement rows, so the FO trail explains why material was sent instead of showing an isolated open counter
- 2026-04-01: `appointment reminders / notification delivery` now closes the loop between FO calendar work and the shared reminder stream:
  - added `appointment_due_soon` / `appointment` notification enum values in [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma) plus migration [packages/db/prisma/migrations/20260401194000_frontoffice_appointment_due_soon_notifications/migration.sql](../../packages/db/prisma/migrations/20260401194000_frontoffice_appointment_due_soon_notifications/migration.sql), so appointment reminders now have a first-class notification contract instead of piggybacking on generic event copy
  - [packages/db/src/notifications.ts](../../packages/db/src/notifications.ts) now reconciles scheduled appointments that start within the next 24 hours into the shared in-app inbox, using the appointment record itself as the dedupe key and the existing notification preference pipeline as the delivery gate
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) and [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) now trigger that reconciliation before loading FO dashboard / activity notifications, so near-term appointments appear in the same `Resource & notice rail` and `Activity` stream agents already watch
  - [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts), [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), and [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) now surface reminder-specific copy and row badges, so agents can see whether a scheduled meeting is `Today`, `Starts within 2h`, or already past due without opening a second tool
- 2026-04-01: `lease-date reminders` are now a live FO execution surface instead of staying a roadmap bullet:
  - added `leaseEndDate` and `leaseReminderAt` to [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma), so renewal / remarketing timing now lives as first-class client data instead of drifting in notes or spreadsheets
  - [packages/db/src/contacts.ts](../../packages/db/src/contacts.ts), [packages/db/src/field-settings.ts](../../packages/db/src/field-settings.ts), [apps/web/app/api/office/contacts/route.ts](../../apps/web/app/api/office/contacts/route.ts), [apps/web/app/api/office/contacts/[contactId]/route.ts](../../apps/web/app/api/office/contacts/[contactId]/route.ts), and [apps/web/app/office/contacts/[contactId]/contact-detail-client.tsx](../../apps/web/app/office/contacts/[contactId]/contact-detail-client.tsx) now treat those lease dates as shared contact fields, with Acre auto-scheduling the reminder 45 days before lease end when the reminder date is left blank
  - added [apps/web/app/api/agent/clients/[clientId]/lease-reminder/route.ts](../../apps/web/app/api/agent/clients/[clientId]/lease-reminder/route.ts) plus [apps/web/app/agent/clients/[clientId]/front-office-client-lease-reminder-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-lease-reminder-client.tsx), and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now shows a dedicated `Lease-date reminder` card so agents can manage renewal / remarketing timing directly inside the FO dossier
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) now folds lease reminder pressure into the client workflow signal, so a due or overdue lease window can become the next recommended action instead of hiding behind generic follow-up text
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), and [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) now surface a `Lease-date reminders` queue plus lease-aware next-touch labels across FO dashboard / client list / listing-send context, so the reminder shows up in the same operational views agents already use
- 2026-04-01: `send record + client engagement` is now the live next layer on top of tracked listing links instead of remaining only a FO roadmap note:
  - added `FrontOfficeSendRecord` to [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma) with organization / office / sender / client / listing attribution plus `sentAt`, `firstOpenedAt`, `lastOpenedAt`, and `openCount`, so FO outreach is now a real execution record instead of only a link counter
  - [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts) now creates a send record whenever listing output is opened in a client-linked context, and public listing-share opens now write back into both the shared link click counter and the matching send record open/revisit fields
  - [apps/web/app/api/agent/listings/[listingId]/share-links/route.ts](../../apps/web/app/api/agent/listings/[listingId]/share-links/route.ts), [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx), and [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now support `/agent/listings?clientId=...`, making the listings page an explicit “send for this client” mode instead of only a generic tracked-link terminal
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) and [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now expose a dedicated `Send record & engagement` section in the client dossier, including send counts, opened sends, revisits, last engagement time, and recent send history that links back into client-scoped listing output
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) and [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) now surface `Client sends`, `Opened sends`, `Engaged clients`, and a `Recent client engagement` list so the FO dashboard finally closes the loop between content output and client response
- 2026-04-01: `Chat List / phone strategy` is now embedded inside the FO client dossier instead of living as a future-only note:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) now builds a stage-aware playbook alongside the dossier snapshot, including intro scripts, call checklists, qualification / showing / BO-ready prompts, objection handling, and copy-ready message templates derived from the current client context
  - added [apps/web/app/agent/clients/[clientId]/front-office-client-chat-list-client.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-chat-list-client.tsx) so agents can copy intro scripts and message templates directly from the live dossier while calling, texting, or emailing the client
  - [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) now renders that playbook as its own `Chat List & phone strategy` section, keeping the scripts in the same execution context as appointments, follow-ups, and BO handoff state
  - [apps/web/app/globals.css](../../apps/web/app/globals.css) now includes the canonical FO playbook cards/template styles needed for this stage-aware outreach surface
- 2026-04-01: FO listing output is now a real tracked-send surface instead of a read-only inventory list:
  - added [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts) so Front Office can create private tracked listing links from the shared `ListingShareLink` store and resolve those links into a public share snapshot that increments `clickCount` when opened
  - added [apps/web/app/api/agent/listings/[listingId]/share-links/route.ts](../../apps/web/app/api/agent/listings/[listingId]/share-links/route.ts) for authenticated FO share-link creation by `sms / email / direct` channel
  - added [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx), and [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx) now uses it so agents can copy tracked SMS templates, email templates, or raw links directly from the send-ready inventory surface
  - added [apps/web/app/share/listings/[code]/page.tsx](../../apps/web/app/share/listings/[code]/page.tsx) as the lightweight private public-facing landing page for those tracked links, with listing context and direct agent contact actions
- 2026-04-01: `Agent material window` now sits beside listing output instead of remaining a future-only FO spec bullet:
  - [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) now enriches the listings snapshot with a current-agent material block built from the shared membership, `AgentProfile`, and recent closed transactions, so FO can reuse the existing profile/transaction truth instead of inventing a parallel profile store
  - added [apps/web/app/agent/listings/front-office-agent-material-window.tsx](../../apps/web/app/agent/listings/front-office-agent-material-window.tsx) so agents can copy a business card, intro email, or intro text from the live FO listings rail while also surfacing portrait readiness, license info, and featured recent wins
  - [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx) now renders that `Agent material window` in the rail next to output signals and send modes, keeping agent identity material in the same execution context as tracked listing outreach
- 2026-04-01: the first real `Front Office` execution-to-handoff contract is now live instead of placeholder-only:
  - added new Prisma models and enums in [packages/db/prisma/schema.prisma](../../packages/db/prisma/schema.prisma) for `Appointment`, `ClientStageHistory`, and `FrontOfficeHandoffDraft`, establishing the first formal FO execution-state layer that does not duplicate `Transaction`
  - [packages/db/src/contacts.ts](../../packages/db/src/contacts.ts) now writes initial and changed `ClientStageHistory` rows and synchronizes BO-ready `FrontOfficeHandoffDraft` records whenever a client moves into or out of negotiation / offer / application / contract-style stages
  - added new [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) service so FO has a real appointment scheduling module with create/update flows, option sets, dashboard snapshot support, and client-touch writeback on completion
  - [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx) and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) now provide a real FO calendar surface for showings, consultations, and meetings, while the right rail exposes explicit BO handoff drafts
  - [apps/web/app/api/agent/appointments/route.ts](../../apps/web/app/api/agent/appointments/route.ts) and [apps/web/app/api/agent/appointments/[appointmentId]/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/route.ts) now back that FO calendar with authenticated create + status-update APIs
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) and [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx) no longer describe appointment scheduling as “not live yet”; the dashboard now merges live FO appointments with shared office events and reads the `Needs Back Office` queue from explicit handoff drafts instead of stage-text heuristics
  - documented the new FO/BO boundary in [docs/specs/frontoffice-data-contract.md](frontoffice-data-contract.md) so future work relies on a stable contract instead of chat history
- 2026-04-01: the first FO client dossier + BO prefill bridge is now live on top of that execution contract:
  - added [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts) so FO now has a dedicated dossier snapshot for one client, including stage history, appointments, follow-up tasks, linked transactions, and handoff state instead of only the aggregate pipeline list
  - [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx) is now the first real FO dossier page, and [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) plus [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts) now link the client list/dashboard into that detail route
  - [packages/db/src/front-office-contracts.ts](../../packages/db/src/front-office-contracts.ts), [apps/web/app/office/transactions/new/page.tsx](../../apps/web/app/office/transactions/new/page.tsx), [apps/web/app/office/transactions/new/transaction-create-page-client.tsx](../../apps/web/app/office/transactions/new/transaction-create-page-client.tsx), and [apps/web/app/office/transactions/transaction-intake-form.tsx](../../apps/web/app/office/transactions/transaction-intake-form.tsx) now let `/office/transactions/new?handoffId=...` prefill the BO intake form from a formal FO handoff draft
  - [apps/web/app/api/office/transactions/route.ts](../../apps/web/app/api/office/transactions/route.ts) now commits the handoff draft after a successful transaction create and links the originating FO client into the new transaction as the primary contact, so FO no longer leaves stale BO-ready queue items behind once the formal record exists
  - [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx) and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) now accept `?clientId=...` so the dossier can launch directly into a preselected appointment workflow for the same client
- 2026-04-01: the final active design-system unification pass closed the remaining FO/BO shell-behavior and legacy-compatibility gaps in one sweep:
  - added shared [apps/web/app/\_components/workspace-nav.tsx](../../apps/web/app/_components/workspace-nav.tsx) so [apps/web/app/office/office-nav.tsx](../../apps/web/app/office/office-nav.tsx) and [apps/web/app/agent/agent-nav.tsx](../../apps/web/app/agent/agent-nav.tsx) now share one navigation behavior implementation instead of drifting copies
  - [apps/web/app/office/contacts/[contactId]/page.tsx](../../apps/web/app/office/contacts/[contactId]/page.tsx) and [apps/web/app/office/payout-statements/[statementId]/page.tsx](../../apps/web/app/office/payout-statements/[statementId]/page.tsx) now own their route-level detail shell/header through `OfficeDetailPageShell + OfficeDetailPageHeader`, while their client bodies were reduced to canonical stacked sections
  - [apps/web/app/office/settings/users/[membershipId]/user-detail-client.tsx](../../apps/web/app/office/settings/users/[membershipId]/user-detail-client.tsx) replaced its page-local hero treatment with shared `SectionCard + office-detail-grid + badges` grammar
  - [apps/web/app/office/office-table-layout-bootstrap.ts](../../apps/web/app/office/office-table-layout-bootstrap.ts) and [apps/web/app/office/office-table-layout-runtime.tsx](../../apps/web/app/office/office-table-layout-runtime.tsx) no longer parse or scope `bm-*` table classes
  - [apps/web/app/globals.css](../../apps/web/app/globals.css) was pruned of dead `.bm-*`, `.office-bm-*`, retired `agent-nav-*`, and old settings-user hero selectors, leaving the live product with one active class language
  - removed the dead [apps/web/app/office/agents/[membershipId]/agent-profile-client.tsx](../../apps/web/app/office/agents/[membershipId]/agent-profile-client.tsx) file because `/office/agents/[membershipId]` now permanently redirects into the unified user-operations workspace
- 2026-04-01: inner BO detail-workspace normalization started after the shared route shells landed:
  - [apps/web/app/office/transactions/[transactionId]/transaction-detail-workspace.tsx](../../apps/web/app/office/transactions/[transactionId]/transaction-detail-workspace.tsx) now uses the shared BO detail shell/header family directly and promotes `Owner / Office / Status` into the route-level summary instead of keeping that summary in a route-local overview header split
  - [apps/web/app/office/settings/users/[membershipId]/permissions/page.tsx](../../apps/web/app/office/settings/users/[membershipId]/permissions/page.tsx) now renders a canonical detail header with summary chips and a back action
  - [apps/web/app/office/settings/users/[membershipId]/permissions/permissions-client.tsx](../../apps/web/app/office/settings/users/[membershipId]/permissions/permissions-client.tsx) now uses shared `SectionCard`, `ListPageStatsGrid`, and `StatCard` primitives for the core permissions workspace instead of a page-local hero composition
- 2026-04-01: special Office detail/create/preview routes now share one BO detail-shell family instead of hand-assembling their own `PageShell + PageHeader` stack:
  - added shared [apps/web/app/\_components/canonical-detail-page-template.tsx](../../apps/web/app/_components/canonical-detail-page-template.tsx) and [apps/web/app/office/\_components/office-detail-page-template.tsx](../../apps/web/app/office/_components/office-detail-page-template.tsx) as the canonical detail-route skeleton for BO special pages
  - [apps/web/app/office/transactions/new/page.tsx](../../apps/web/app/office/transactions/new/page.tsx), [apps/web/app/office/settings/users/[membershipId]/page.tsx](../../apps/web/app/office/settings/users/[membershipId]/page.tsx), [apps/web/app/office/settings/teams/[teamId]/page.tsx](../../apps/web/app/office/settings/teams/[teamId]/page.tsx), [apps/web/app/office/1099-tracker/preview/[membershipId]/page.tsx](../../apps/web/app/office/1099-tracker/preview/[membershipId]/page.tsx), and the transaction signature authoring routes now compose that shared detail shell
  - `/office/settings/users/[membershipId]/permissions` now also routes through the shared detail shell at the outer frame, while intentionally keeping its inner permission-management hero/content contract local for now
- 2026-04-01: shared BO route-template adoption now covers the remaining active Office list/workbench pages beyond the first shell batch:
  - [apps/web/app/office/pipeline/page.tsx](../../apps/web/app/office/pipeline/page.tsx), [apps/web/app/office/library/page.tsx](../../apps/web/app/office/library/page.tsx), [apps/web/app/office/reports/page.tsx](../../apps/web/app/office/reports/page.tsx), [apps/web/app/office/billing/page.tsx](../../apps/web/app/office/billing/page.tsx), [apps/web/app/office/approve-docs/page.tsx](../../apps/web/app/office/approve-docs/page.tsx), [apps/web/app/office/performance/page.tsx](../../apps/web/app/office/performance/page.tsx), [apps/web/app/office/account/page.tsx](../../apps/web/app/office/account/page.tsx), [apps/web/app/office/1099-tracker/page.tsx](../../apps/web/app/office/1099-tracker/page.tsx), [apps/web/app/office/signatures/page.tsx](../../apps/web/app/office/signatures/page.tsx), [apps/web/app/office/signatures/templates/page.tsx](../../apps/web/app/office/signatures/templates/page.tsx), and the settings list/workbench routes under [apps/web/app/office/settings](../../apps/web/app/office/settings) now start from `OfficeListPageShell + OfficeListPageHeader`
  - this means the active BO workbench/list routes no longer maintain route-local `PageShell + PageHeader + PageHeaderSummary` stacks; the remaining exceptions are special detail/create/preview pages that need a shared detail-shell story rather than another list-page migration
- 2026-04-01: shared BO route-template adoption advanced beyond class-language cleanup into the route shell/header layer:
  - [apps/web/app/office/accounting/page.tsx](../../apps/web/app/office/accounting/page.tsx), [apps/web/app/office/activity/page.tsx](../../apps/web/app/office/activity/page.tsx), [apps/web/app/office/dashboard/page.tsx](../../apps/web/app/office/dashboard/page.tsx), [apps/web/app/office/notifications/page.tsx](../../apps/web/app/office/notifications/page.tsx), and [apps/web/app/office/tasks/page.tsx](../../apps/web/app/office/tasks/page.tsx) now start from `OfficeListPageShell + OfficeListPageHeader` instead of hand-assembling `PageShell + PageHeader + PageHeaderSummary`
  - this keeps each page's existing workbench body intact, but standardizes the route-level page frame against the shared BO list-page family so those routes no longer maintain their own shell/header contract
- 2026-04-01: active Office module/page markup was consolidated onto one canonical `office-*` class language across the remaining high-debt workspaces:
  - accounting, billing, contacts, transaction list/create flow, transaction detail subcards, offer/documents/tasks/contacts/incoming-updates/signature editor, and related modals/cards under [apps/web/app/office](../../apps/web/app/office) no longer emit `bm-*` design-language classes in live TSX markup
  - [apps/web/app/globals.css](../../apps/web/app/globals.css) now exposes `office-*` compatibility selectors for the previously active `bm-*` modules, so the live product uses one class vocabulary while visual behavior stays stable during cleanup
  - remaining `bm-*` references are now limited to compatibility CSS and runtime helpers such as the legacy table-layout support path, not live page/component chrome
- 2026-04-01: `/office/dashboard` completed the next `bm-*` to canonical `office-*` migration batch for one of the highest-visibility BO pages:
  - [apps/web/app/office/dashboard/page.tsx](../../apps/web/app/office/dashboard/page.tsx) no longer emits `bm-*` markup for goal tracking, KPI strip, chart bars/month ticks, progress ring, or payout-statement row actions
  - new `office-dashboard-*` classes in [apps/web/app/globals.css](../../apps/web/app/globals.css) now define the canonical dashboard chrome for those surfaces, so the page aligns with the shared Office design language instead of depending on legacy `bm-*` dashboard vocabulary
  - this keeps the current dashboard data, role visibility, commission self-service card, and transaction table behavior intact while removing another major BO design-language fork
- 2026-04-01: `/office/activity` completed the first live `bm-*` to canonical `office-*` migration batch inside a high-visibility BO workspace:
  - [apps/web/app/office/activity/page.tsx](../../apps/web/app/office/activity/page.tsx) and [apps/web/app/office/activity/activity-alerts-layout.tsx](../../apps/web/app/office/activity/activity-alerts-layout.tsx) no longer emit `bm-*` activity shell/list markup for section rails, records, alert cards, or activity-specific filter layout
  - new `office-activity-*` classes in [apps/web/app/globals.css](../../apps/web/app/globals.css) now act as the canonical styling layer for the activity workspace, so the page sits inside the same visual language as the rest of the Office system instead of extending legacy `bm-*` chrome
  - this keeps the existing audit/event data flow and live-alert loading model intact while reducing one of the largest remaining visible BO design-language forks
- 2026-04-01: FO / BO route-level page templates now share one canonical list-page skeleton instead of maintaining separate shell logic:
  - added shared [apps/web/app/\_components/canonical-list-page-template.tsx](../../apps/web/app/_components/canonical-list-page-template.tsx) as the common route-template foundation for `PageShell`, canonical header summary, and canonical list-card composition
  - [apps/web/app/agent/\_components/front-office-page-template.tsx](../../apps/web/app/agent/_components/front-office-page-template.tsx) now composes that shared canonical skeleton instead of hand-assembling its own shell/header stack
  - [apps/web/app/office/\_components/office-list-page-template.tsx](../../apps/web/app/office/_components/office-list-page-template.tsx) now also delegates to the same shared canonical skeleton, so FO and BO list-page routes start from one route-level template family
  - this is the first implementation step of the “one Acre design system” rule: FO may still have its own action-first layout flavor, but it no longer owns a separate route-shell foundation
- 2026-03-31: agent payout review reminders are now treated as persistent high-priority in-product work instead of one easy-to-miss inbox row:
  - `sendAgentPayoutStatementToAgent(...)` now writes `payout_statement_ready` notifications with action-required copy and `critical` severity for the agent-facing send / resend flow
  - `/office/notifications` now derives a live `Needs your payout review` queue from current `AgentPayoutStatement.reviewStatus === awaiting_agent`, so statement review stays visible even if the user already opened or marked the original inbox item as read
  - `/office/dashboard` `My commissions` now shows an urgent payout reminder block whenever the current membership still has one or more `awaiting_agent` statements
  - the accounting send helper text now explicitly tells finance that agents will see the statement as a high-priority Acre task on both dashboard and notifications until they respond
- 2026-03-31: active `Front Office` pages and legacy FO feed APIs were rebuilt off real `@acre/db` services instead of `@acre/backoffice` mock feeds:
  - `/agent/clients`, `/agent/listings`, `/agent/resources`, and `/agent/notifications` now read organization/membership-scoped Prisma data through new FO workspace snapshot services
  - the legacy `GET /api/clients`, `GET /api/listings`, `GET /api/events`, and `GET /api/resources` endpoints now return real FO snapshot data instead of in-memory feed arrays
  - `/agent` navigation no longer imports `agentSections` from `@acre/backoffice`; the live FO shell now owns its own route map instead of borrowing legacy mock section config
  - this removes the last active `/agent` route dependency on `@acre/backoffice` feed helpers, leaving that package as legacy/demo compatibility only
- 2026-03-31: accounting payout statements now complete the agent review loop entirely inside Back Office instead of relying on email / WeChat follow-up:
  - `AgentPayoutStatement` now keeps a review lifecycle of `draft -> awaiting_agent -> revision_requested -> confirmed` plus a durable internal message timeline
  - `/office/accounting` now lets finance/admin send or resend a statement to the agent with an internal note and see the full review timeline on the saved statement detail
  - `/office/dashboard` now exposes only sent payout statements to the owning membership, and each row links into a dedicated `/office/payout-statements/[statementId]` self-service review page
  - agents can now confirm a statement or submit a revision request with a required message inside the system, and both actions emit in-app notifications plus accounting activity-log entries
  - editing statement-level manual adjustments after a send now resets the statement back to `draft`, forcing finance to resend the updated statement before the agent can review it again
- 2026-04-06: `/office/accounting` statement history status is now admin-editable instead of being locked after agent confirmation:
  - `AgentPayoutStatement.reviewStatus` now adds `paid` on top of the existing `draft / awaiting_agent / revision_requested / confirmed` states
  - finance/admin can change the saved statement review status inline from the history table, including rolling a previously `confirmed` statement back into another review state
  - member-facing payout statement lists and badges now surface the new `paid` label while keeping the review queue limited to `awaiting_agent`
- 2026-03-31: `/agent/dashboard` was rebuilt from the old `Agent OS` mock landing page into a real `Front Office` action homepage:
  - the route now reads from new `@acre/db` `getFrontOfficeDashboardSnapshot(...)` service instead of `@acre/backoffice` demo snapshot data
  - the new snapshot aggregates real `Client`, `FollowUpTask`, `Listing`, `ListingShareLink`, `Event`, `Notification`, `Resource`, `Vendor`, `Transaction`, and `SignatureRequest` data into one action-first FO surface
  - the page structure now follows the same shared design system as Back Office using `PageHeader`, `PageHeaderSummary`, `SectionCard`, `StatCard`, `SummaryChip`, `ListPageSplit`, and `ListPageStack`
  - the homepage explicitly separates `Front Office` work from `Back Office` formal workflow by keeping outreach / follow-up / commitments in the main queue and pushing transaction/signature follow-through into a `Needs Back Office` handoff section
  - the `/agent` shell copy and navigation were normalized away from the older standalone `Agent OS` voice so `Front Office` and `Back Office` now present as one product family instead of parallel brands
- 2026-03-31: eSignature moved from a transaction-only single-signer MVP toward a platform-level signature center MVP:
  - added new Prisma models and enums for `SignatureRecipient`, `SignatureTemplate`, `SignatureTemplateRecipient`, `SignatureTemplateField`, `SignatureArtifact`, `OrganizationSignatureDriveSetting`, richer request statuses, recipient roles, template categories, context types, and Drive sync state
  - `SignatureField` now stores `assignedRecipientId`, `fieldKey`, `isReadOnly`, `isSystemPrefilled`, `visibilityRule`, `mirrorGroup`, and `fieldOptions`, so each signing location can be bound to a specific signer and reused across mirrored fields
  - transaction signature request authoring now supports multiple signers / approvers / CC recipients, routing-step based serial/parallel/hybrid flows, template/category/context metadata, and recipient-token public signing instead of one shared envelope token
  - public `/sign/:token` and `/api/public/signatures/:token*` now enforce recipient-specific field ownership, overlay previously submitted values into the PDF preview, advance routing step by step, and keep backward compatibility for older single-recipient links
  - completed requests now create normalized signature artifacts for original + signed copy and synchronously attempt Google Drive archival using one organization-level service account + folder mapping stored under `Settings > Signature Drive`; failures stay visible and can be retried from the center page
  - `/office/signatures` now provides a unified signature operations workspace with filtering, recipient/requester visibility, Drive status, and CSV export, while `/office/signatures/templates` provides the reusable template library
  - role permissions now explicitly distinguish `signatures:view`, `signatures:manage`, `signatures:template_manage`, `signatures:report_view`, and `signatures:report_export`, with self-related visibility preserved for `agent / team_lead` users
  - current first-phase create flow is still transaction-first even though requests can already be tagged as `HR / Finance / Admin / Generic`; template management, subject membership linkage, and center-level tracking are live, but a fully generic non-transaction authoring surface remains follow-up work
- 2026-03-28: transaction documents now support an external eSignature MVP inside the existing `Documents / Forms & eSignature` workspace:
  - `SignatureRequest` now stores recipient email copy, sender metadata, expiry, public token hash, signed/completed timestamps, and signed-output linkage instead of stopping at the old internal status shell
  - new `SignatureField` and `SignatureAuditEntry` models hold PDF field placement plus signer-facing audit evidence for request create/send/view/submit/finalize/cancel/expire
  - transaction detail PDF documents now expose `Prepare signature`, a draggable field editor, signer setup, send/resend/cancel actions, and request status visibility inside the existing document/signature cards
  - public `/sign/:token` and `/api/public/signatures/:token*` routes let recipients review the PDF and sign without logging into CRM, using draw / typed / uploaded signature input modes
  - submit finalization now generates a signed PDF with `pdf-lib`, archives it back into transaction documents, keeps the original file intact, and preserves audit coverage for both `Activity Log` and signer timeline data
- 2026-03-28: `/office/1099-tracker` is now a real `office_admin`-only workflow for annual agent payout backup and 1099 prep:
  - added a durable `Agent1099PaymentRecord` Prisma model plus yearly payment-record save/update/delete flows with audit-log coverage
  - `Payment Record` now lets admins search `active` or `invited` agents, edit unlimited yearly line items, and save one replaceable annual batch per `Agent + Tax Year`
  - `1099 Summary / Preview` now aggregates totals by tax year from those saved payment records only, pulls payee identity data from current `AgentBankInformation`, and exposes per-agent preview + PDF export
  - agent `Bank information` now includes `payeeName`, and that field is reused by the new 1099 summary/export path instead of adding a second profile store
  - exported PDFs are internal `1099 Summary / Backup Document` files, not IRS-format forms
- 2026-03-28: local/dev Prisma client refresh is now self-healing across standard Acre dev flows:
  - root `npm run dev` now runs `db:generate` before starting `@acre/web`, so newly added Prisma models/relations stop depending on a manual "restart Next first" memory step
  - the dev wrapper now watches `packages/db/prisma/schema.prisma` and automatically regenerates Prisma Client plus restarts the Next dev child when the schema changes
  - this specifically hardens local `/office/accounting` and other Prisma-backed Office routes against `PrismaClientValidationError` / `Unknown field ... for include statement` failures caused by a long-running dev process still holding an old generated client
- 2026-03-26: shared `@acre/ui` `DataTable` was hardened and `/office/accounting` review behavior was simplified so local/dev Office pages stop thrashing during review flows:
  - `DataTable` now lives behind a client component boundary before it reads `HorizontalScrollArea` context, so server-rendered Office pages no longer crash with `Attempted to call useHorizontalScrollAreaContext() from the server`
  - this removes the dev-server restart loop that could temporarily make local Back Office routes appear unavailable whenever a server page compiled a shared table surface
  - `/office/accounting` candidate-row transaction links now open the full transaction workspace in a new browser tab instead of trying to hold an embedded review modal inside the statement workspace
- 2026-03-26: shared Office table resize behavior now uses one consistent leading-edge divider contract across Office tables:
  - resizable dividers now anchor to the left edge of each column except the first one, so the visual line always stays on the same side as the column label the user is aiming for
  - resize handles still start the gesture on the divider itself, but active drag motion now continues through window-level `pointermove / pointerup` listeners, so column motion keeps updating while the mouse button stays down even if the table rescans or the handle subtree rerenders
  - resize handles are now reused across rescans instead of being torn down and recreated mid-gesture, so an active drag no longer loses its event target before release
  - hover / drag highlighting now stays scoped to the active divider instead of lighting up every divider in the table during resize
  - `/office/accounting` statement candidate tables now keep header and row content left-aligned within that workspace, so the resize boundary no longer competes with right-aligned amount text in the same card
- 2026-03-26: shared Office tables are now the canonical template for future Back Office table work:
  - new `/office` tables should default to `@acre/ui` `DataTable` plus the shared `office-table-*` / `office-list-table-*` row contracts instead of inventing a page-local table shell
  - semantic native `<table>` remains allowed when it materially helps, but it should still stay inside the supported Office table contract so shared width persistence and resize behavior keep applying
  - future table changes should be made in the shared contract first whenever possible, so later tables automatically inherit the updated behavior instead of forking logic across routes
- 2026-03-26: `/office/accounting` statement generation UI now uses one combined candidate workspace instead of two stacked selection cards:
  - `Invoice selection` and `Candidate rows` are now rendered inside one `Statement candidates` card with `Invoices` and `Rows` sub-blocks, reducing duplicate context while keeping invoice-number generation semantics intact
  - the preview/uncheck flow still exists, but it now stays visually inside the same workspace the admin is already using to assemble the payout statement
- 2026-03-26: `/office/accounting` statement generation now supports repeat snapshot generation from the same invoice rows:
  - `payable / paid` candidate rows remain selectable for fresh statement snapshots instead of being blocked after the first generation
  - only `calculated / reviewed / statement_ready` rows are advanced to `payable` during generation; existing `paid` rows keep their `paid` status even when included in a regenerated statement
  - the summary action area now explains this rule directly so admins can tell the difference between a disabled empty selection and a valid repeat-generation flow
- 2026-03-26: `/office/accounting` candidate rows can now open source transactions inside the same statement workspace instead of forcing a full-page route change:
  - `Candidate rows` transaction titles now open the existing transaction detail workspace in a separate browser tab, leaving the accounting statement workflow untouched in the original tab
  - this avoids local review-modal state churn while still giving admins direct access to the same intake, finance, commission, task, document, and offer editing surfaces
- 2026-03-26: transaction finance calculator terminology and active fee set were tightened up for current operations:
  - user-facing `Client Referral` labels are now consistently shown as `Internal Referral` across create, detail finance, blockers, audit copy, and calculation helper text
  - `Channel Development Fee` has been removed from active calculator, calculation, and statement/detail post-split outputs, while the legacy storage enum remains tolerated for backward compatibility
- 2026-03-26: `/office/pipeline` personal `my_*` metrics now stay truly self-scoped for team leaders and junior team leaders:
  - `My net income` no longer rolls subordinate commission rows into branch-leader totals
  - selecting a personal pipeline metric now also hides subordinate-only deals where the current viewer has no direct participation
  - this change is limited to pipeline personal metrics; broader team-scope transaction/reporting surfaces keep their existing team visibility rules
- 2026-03-30: `/office/pipeline` snapshot loading no longer pulls the full visible transaction portfolio into one in-memory bucket pass:
  - pending summary, six-month closed history, and selected working-list rows now come from separate query paths
  - `my_*` metric scope is pushed into relation-aware database predicates instead of post-query membership filtering
  - page URL semantics and UI layout remain unchanged while the service path is materially lighter for larger datasets
- 2026-04-06: `/office/pipeline` closed-history rail now supports a year switch without losing the default recent view:
  - the sidebar still defaults to `Last 6 months`, but now adds a year dropdown so users can switch to a full `Jan-Dec` view for any available year
  - year mode keeps empty months visible, so annual review still shows all 12 buckets even when some months have `0` transactions
  - `Closed This Month` remains anchored to the actual current month through a separate current-month summary query, so the stage card still works even when the history rail is focused on an older year
- 2026-03-26: transaction commission detail and saved payout statements now surface named post-split fee detail instead of leaving `External Referral` and `Company Referral` hidden behind one merged company adjustment:
  - transaction `Commission` now shows one combined allocation table where those active fee rows appear as separate rows beneath the stakeholder rows, while saved statement detail / PDF still retain named fee lines
  - generated `AgentPayoutStatementLine` snapshots now persist fee-breakdown JSON so saved statement detail / PDF can keep showing the named post-split breakdown even after later recalculation
  - statement detail / PDF now render `Post split detail` as total + named fee lines, while payout ownership still follows the existing company-side default allocation logic
- 2026-03-26: `Create transaction` commission intake now uses a simplified calculator flow instead of a create-time fee-ledger editor:
  - the create page now keeps one left-to-right calculator for `Gross Commission / Rebate / Internal Referral / External Referral / Company Referral`, with `Gross Commission` as the only required input
  - the simplified calculator still exposes `Rate %` for each fee in both create and detail finance, so amount/rate entry matches the earlier editor without bringing back the full fee-ledger layout
  - create-time finance now exposes one shared `Note` field instead of multiple fee-level notes or company-referral-only helper fields
  - clicking `Calculate` in create mode now previews `Final Agent Net` using the current split-chain and fee-placement rules before the transaction is saved, while continuing to persist the same structured fee-ledger payload as before
  - transaction detail `Finance` now follows the same calculator layout and field order, so create/edit no longer diverge between a simplified calculator and the older fee-ledger editor
- 2026-03-26: `/office/performance` now provides a real role-scoped `Agent Performance` workspace on top of live transaction finance data:
  - the new page reuses Office report scope and reads directly from `Transaction + TransactionFinanceFee` instead of introducing a second aggregate store
  - performance is fixed to `Gross Commission - Rebate - Referral Fee - Reimbursement`
  - natural-period attribution uses `moveInDate ?? closingDate`, and only `Pending / Closed` rows contribute
  - agents now get self-only numeric visibility plus company Top 10 rank boards with peer amounts hidden, while team/company scope viewers get full numeric tables, rankings, and CSV export
  - current rollout only exposes the `NY` company bucket, while `Rental / NJ` stay reserved in the contract for future activation
- 2026-03-26: internal session/API hardening pass closed several review findings in one sweep:
  - server-side session decoding now rejects cookies older than the configured 30-day max age instead of trusting browser expiry alone
  - legacy/mock feed routes such as `/api/db/seeded-context`, `/api/agent/dashboard`, `/api/clients`, `/api/events`, `/api/listings`, and `/api/resources` now require an authenticated session, and the seeded workspace snapshot additionally requires office user-management access
  - `Contacts` list/detail now resolve the same office/team/self scope model as other office surfaces, and contact detail no longer leaks out-of-scope transaction links or available-transaction pickers
  - the remaining unguarded `request.json()` office routes touched in this pass now return `400` for malformed JSON instead of bubbling a `500`
  - recurring billing month/year roll-forward now clamps to the target month end, so Jan 31 / leap-day schedules no longer skip into the following month
- 2026-03-26: `/office/pipeline` now exposes `My gross commission` alongside the existing self-service metrics so agents and branch-scoped leads can review gross commission totals without unlocking office-wide metrics
- 2026-03-26: transaction visibility is now split between portfolio-style surfaces and full transaction workspace access:
  - manual/default commission participants can see shared deals and their own scoped income in list-style surfaces such as `Transactions`, `Pipeline`, and dashboard summaries without inheriting the full transaction detail/edit workspace
  - transaction detail, intake edits, finance edits, and other workspace-scoped operations stay limited to owners and non-commission collaborators instead of treating payout participation as a blanket transaction permission
  - `/office/transactions` summary now labels scoped income honestly as `My / Team / Office net income` based on the viewer's financial scope instead of always saying `My net income`
- 2026-03-26: design-system unification pass moved the live page shell / modal / action chrome further onto canonical `office-*` classes:
  - shared confirm dialogs, transaction create/detail shells, transaction intake modal chrome, contact create, users create, billing payment-method modal, library preview/create/upload modals, report/transaction search-layout editors, and agent-billing modals now use `office-modal-*` or `office-create-modal-*` instead of direct `bm-modal-*` page chrome
  - transaction/contact detail save errors and multiple transaction workflow cards now use shared `office-form-error`, `office-inline-error`, `office-button`, `office-toggle-link`, and `office-inline-link` contracts instead of old `bm-*` action/error chrome
  - `bm-*` still exists as a compatibility layer in CSS and in some deeper page-local workflow markup, but the live shell-level direction is now explicitly `office-*` only
- `Settings > Users > [membership]` Teams assignment card now keeps validation errors inside the card body and uses a wrapped content/action layout so branch labels, status badges, and remove actions stop colliding in narrow side-column widths
- permission resolution now treats active `Team Leader / Junior Team Leader` assignments as an effective `team_lead` template even when older data still stores the account role as `agent`, so legacy leader accounts keep team transaction/report visibility until the saved role is cleaned up
- `Settings > Users` and agent profile team-assignment forms no longer auto-select the first available Team / Junior Team; admins now have to make an explicit branch choice before `Add to team`, so the form stops implying the member already belongs to whichever option happens to sort first
- Admin-managed operational member pickers now follow one explicit long-term rule: `invited` memberships remain operationally usable alongside `active` ones, so office admins must not require invite acceptance/login before assigning transaction ownership, adding commission override participants, or generating payout statements for a member
- Working baseline normalized around:
  - local repo `<repo-root>`
  - GitHub remote `https://github.com/johnrebirth5-web/acre-office-warm-ui.git`
  - DigitalOcean default entry `http://45.55.247.137:3105/`
- Internal auth now moved past seeded-email login:
  - bootstrap admin enforcement
  - invitation onboarding + password setup
  - email + password login
  - forced first password change
  - 5-attempt account lockout + admin unlock
  - signed cookie session kept as the current session model
- `Office / Back Office` now has real route and workflow foundations across:
  - dashboard
  - pipeline
  - transactions
  - contacts
  - tasks and `Approve Docs`
  - reports
  - notifications
  - activity
  - accounting
  - billing and account
  - library
  - settings/admin
  - agent management
- Transaction detail now acts as a real workflow hub for:
  - contacts
  - finance
  - tasks and compliance
  - documents and forms / eSignature
  - offers
  - commissions
- Incoming update review foundation still exists in the data/API layer, but the section is now hidden from the default transaction detail UI to keep the workflow hub focused on active operations.
- Transaction intake is now office-scoped and schema-driven across the create modal, `/office/transactions/new`, and transaction detail:
  - `Settings > Fields` is now the single field-structure admin surface
  - first centralized modules are `transaction / contact / offer`
  - built-in fields can be sorted, marked visible/hidden, and required/optional
  - built-in `Type / Status / Representing` dropdowns still support office-admin option enable/disable and display-name overrides without changing stored enum values
  - `office_admin` can add custom `text / select / date` fields per module
  - `/office/transactions` search is now backed by an office-shared `TransactionSearchLayout`: users with `fields:manage` can open `Edit fields` to add or remove operational, built-in, and custom filters, while legacy `q / status / type / ownerMembershipId / teamId / startDate / endDate` params remain compatible and hidden/archived transaction fields are automatically stripped from saved search layouts
  - hidden address groups still force `Transaction Name` visible + required
  - contact and offer custom values now persist in `Client.additionalFields` and `Offer.additionalFields`
  - create-mode intake now routes legacy finance-style fields like `Commission($) / Rebate / Referral Fee / Reimbursement / Note` through a structured finance section so new transactions write real `grossCommission`, fee-ledger rows, and finance notes instead of disconnected text-only values
  - structured create-time finance now keeps untouched fee rows truly empty instead of auto-materializing placeholder `20%` rebate / client referral / company referral records, and clearing a fee in Finance now actually removes its stored rate/amount instead of restoring hidden defaults
  - transaction legacy compatibility placeholders and text-only finance bridge fields are now retired from the shared schema itself, so they no longer appear in `Settings > Fields`, create-time `Edit fields`, or default transaction search/report layouts
  - field-structure changes write to `Activity Log`
  - `/office/transactions` page load now reuses one shared server snapshot for both search-layout resolution and paginated list data, so the page no longer resolves the same scope/team/filter-option context twice before rendering the table
  - `/office/transactions` list summary now short-circuits empty result sets and only runs the net-income aggregate path required for the current viewer scope instead of always running office + scoped income aggregates together
- Reports and transaction pricing are now normalized around one transaction truth source:
  - `/office/reports` has been rebuilt into a transaction-centric workspace where filters, rows, summary cards, and CSV export all reuse the same predicate builder and column registry
  - `/office/reports` rows are now server-paginated via `page / pageSize`, while summary cards still calculate against the full filtered result set and CSV export continues to ignore pagination so it returns the full filtered result set
  - `/office/reports` now also has an office-shared `TransactionReportSearchLayout`: admins with `fields:manage` can add or remove visible report filters via `Edit fields`, while pinned `Sort By / Direction` stay outside the editable layout and old hidden filter params remain readable until the next Apply
  - transaction pricing is now split into typed `askingPrice + purchasedPrice + moveInDate`, while legacy `price` remains as a compatibility mirror of `purchasedPrice`
  - `Team Leader` reporting now comes only from current team hierarchy, not a retired transaction custom field
  - `agent` now has personal reports access, `team_lead` keeps team scope, and admin/review roles keep company scope
  - pipeline volume, dashboard recent transactions, agent goal volume, contact-linked transaction pricing, accepted-offer writeback, and incoming-update price sync now all use the new purchased-price bridge
- Shared `@acre/ui` and the Office design system are now the canonical UI layer for Back Office pages, with `/office/transactions` as the list-page composition reference
- Destructive Back Office actions now use a shared confirmation dialog before delete/remove/unlink-style execution, replacing one-click deletes and ad-hoc `window.confirm` prompts
- `/office/transactions` and `/office/contacts` now share one canonical Office list-page template, including the same page header, summary/action block, workbench card, table card, and footer/pagination rhythm
- Transaction detail now collapses the long operational stack below `Status`:
  - `Intake fields` moved directly under `Status` and defaults open
  - every section from `Intake fields` downward now uses a user-scoped remembered expand/collapse state in local browser storage
  - the remember key is isolated by `organizationId + membershipId`, so one user's section preference does not become the default for everyone else

## Current default baseline

- Product priority: `Back Office`, not the public site
- Local source of truth: `<repo-root>`
- Default local browser entry: `http://localhost:3105/`
- Root `npm run dev` is now expected to bind `@acre/web` to `3105` by default
- Local development now defaults `@acre/web` to `next dev --webpack` instead of Turbopack to avoid recurring false `Module not found` errors when Docker bind mounts add or rename files under `app/`
- Local Docker development is now a supported long-running baseline:
  - `npm run docker:dev:up` starts `web + db`
  - the `web` container bind-mounts the local repo, so Docker uses the same source tree instead of a second copied checkout
  - host-mode Prisma / `.env.local` now target `127.0.0.1:5433`, while Docker-internal traffic still uses `db:5432`, so local Homebrew/Postgres on `5432` no longer hijacks Acre's default dev database path
  - Docker volumes persist PostgreSQL data, `node_modules`, Next cache, and local document storage
  - local Docker runtime currently uses `colima` on macOS
  - seeing the bottom-left `N` dev tools badge locally is expected while the app runs under `next dev`, even inside Docker
- `/login` should never prefill demo credentials and now stays visually empty until the operator focuses the fields, reducing stale localhost/browser autofill from snapping back to a previously saved account like `office@acreny.us`
- Default Git remote: `https://github.com/johnrebirth5-web/acre-office-warm-ui.git`
- Default deployment line: `DigitalOcean :3105`
- Default public entry: `https://acresystem.us/`
- Default login entry: `https://acresystem.us/login`
- Direct fallback entry during DNS propagation: `http://45.55.247.137:3105/`
- Default production runtime:
  - service `acre-ui-rebuild-web.service`
  - WorkingDirectory `/opt/acre-ui-rebuild/app`
  - env `/etc/acre/acre-ui-rebuild.env`
  - nginx `/etc/nginx/sites-available/acre-ui-rebuild.conf`
  - upstream `127.0.0.1:3206`
  - TLS `certbot + nginx`
  - auto-renew `certbot.timer`
- `GitHub` sync and `DigitalOcean` deployment are separate actions
- For deployment truth, `docs/deployment.md` is the canonical reference and runtime truth comes from systemd `ExecStart` plus the active nginx upstream
- Preferred repo-root deployment command: `npm run deploy:digitalocean`
- UI glassmorphism has been reduced on long-lived shells, headers, badges, and overlays to lower GPU/compositor pressure after reports of prolonged-session screen artifacting
- `acresystem.us` / `www.acresystem.us` now terminate HTTPS at nginx with a Let's Encrypt certificate, and HTTP redirects to HTTPS
- Added recursive team hierarchy support so local and deployed databases upgrade legacy `lead / leader_i / leader_ii` memberships into `team_leader / junior_team_leader`, add `Team.parentTeamId`, and keep descendant-scope access working across `/office/settings/users`, `/office/transactions`, and `/office/reports`
- Back Office account access is now tiered as `owner / office_admin / accountant / human_resources / team_lead / agent`, with server-side scope enforcement and finance redaction applied to dashboard, transactions, reports, exports, and agent views
- Team hierarchy is now modeled explicitly with `TeamMembership.role + reportsToTeamMembershipId`, and the repo includes a one-off provisioning script for the initial `acreny.us` account batch plus invite-link output
- `Settings > Teams` now shows inherited parent-branch managers for leader rows and flags invalid root/child branch leader-role mismatches instead of silently displaying the first allowed option
- Team labels and branch summaries now treat branch ownership consistently:
  - assignable team dropdowns always render `Team path · Leader: ...` and show `Leader: Unassigned` when a branch exists without an active owner
  - only leader roles that match the current team shape count as branch owners, so invalid root/child mismatches no longer appear in branch-owner summaries or manager pickers
  - `Settings > Teams` now makes `Root team / Child branch / Branch owner` status explicit so an empty child branch is distinguishable from an invalid leader assignment on the parent team
- `Settings > Teams` now defaults to a directory-style hierarchy browser:
  - the landing page shows top-level Team cards only, with leader, direct-agent, and Junior Team summaries
  - opening `/office/settings/teams/[teamId]` shows Junior Team cards first and the selected Team's direct agents below, so Team and Junior Team structure no longer compete on the same page
  - the previous dense all-team editor remains available as an advanced manage view for deeper admin cleanup
- Team admin now treats hierarchy ownership as a required binding instead of optional copy:
  - current product language uses `Team / Junior Team / Team Leader / Junior Team Leader`
  - creating a Team or Junior Team requires selecting its owner in the same flow
  - the current owner cannot be removed or demoted without first transferring ownership
  - promoting another member into the owner role now transfers leadership instead of forcing admins through a temporary ownerless state
  - legacy `Junior Team Leader` assignments that were still sitting inside the parent Team now auto-create a leader-named Junior Team and move that leader plus direct reports into the correct child structure
  - the current Back Office UI intentionally opens only two levels today while the underlying recursive team model remains in place for future expansion
- Office table column widths can now be resized and saved at the organization level by `owner / office_admin`, with the shared layout applied across list/workspace tables for every user in the same org
- Commission V2 now uses membership-level default split settings plus reporting-line chain calculation:
  - new `CommissionSplitTemplate` + `MembershipCommissionSetting` models
  - create-user and profile editing now write structured default commission values
  - transaction commission defaults lock to `Transaction.createdAt`
  - accounting commission page now leads with split templates / member defaults and keeps legacy plan tools under advanced settings
  - team assignments now block multiple active reporting lines for one membership inside the same company scope, while still allowing one active branch in another company scope
  - saving `Finance > Gross commission` on transaction detail now auto-triggers commission recalculation when the current role can calculate commissions
- Agent profile team assignment now shows leader names inside team options and allows choosing the direct manager during the initial `Add to team` action, so admins can place agents directly under the right branch lead without a second pass in `Settings > Teams`
- Team hierarchy and commission chain derivation now honor explicit `reportsToTeamMembershipId` links even for leader roles, so same-branch `junior_team_leader -> team_leader` reporting lines propagate correctly into multi-level commission splits
- Assigning a `team_leader` or `junior_team_leader` team role now auto-upgrades `agent` accounts to the `team_lead` permission template, preventing leader-visibility mismatches on transactions and commission self-service pages
- `Settings > Users` now blocks downgrading an actively assigned `Team Leader / Junior Team Leader` back to `agent`, and roster/profile snapshot reads no longer auto-materialize legacy junior teams or write audit log side effects when someone merely opens the page
- Child-team members without a same-team leader now temporarily fall back to the parent team leader for hierarchy/commission-chain resolution, so ownerless junior branches do not silently collapse to `member + company` only while the branch leader assignment is being cleaned up
- Transaction and accounting commission tables now show each row's actual effective share in the `Plan` column detail instead of repeating the owner's default split label for every recipient
- Manual commission overrides now recompute stakeholder `Share` from the latest overridden `Final` amounts, so adding extra payout participants no longer leaves transaction tables or agent statement detail / PDF views stuck on the original split-chain percentages or `Manual`
- `New transaction` owner assignment now treats the `Agent Name` field as the real owner control:
  - sales roles can only create transactions for themselves and see a locked self owner label
  - admin/company-scope roles can search active agents or team leads by name and assign ownership before create
- Commission self-service visibility is now aligned to sales hierarchy expectations:
  - `agent` always keeps self commission visibility
  - `team_lead` always keeps self + downline commission visibility
  - `/office/dashboard` now shows the self-service commission summary only for sales roles, so admins and non-sales office roles do not get a redundant `My commissions` card on their dashboard
  - the dashboard keeps the current month expanded while older commission months now live inside a collapsible history block to save vertical space
  - `/office/transactions/[transactionId]` now respects scoped commission visibility even when older org role templates are missing the newer commission view keys
  - commission plan management and statement-generation surfaces stay restricted to admin/review roles instead of piggybacking on the new self-service visibility baseline
  - backend now enforces the same rule and persists `additionalFields.agentName` from the actual selected owner instead of arbitrary free text
- `Settings > Fields` custom field editor now supports `Protected from deletion`, and the transaction `Agent Name` field is hard-protected so admins must hide it instead of deleting the owner-linked schema row
- Transaction intake now retires the legacy `Team Leader` custom field from both `New Transaction` and `Settings > Fields`; team hierarchy should come only from membership/team assignment, not from a second dropdown on the transaction form
- Transaction owner selection for `New Transaction` now treats `invited` sales memberships the same as `active` ones, so admins can create transactions for agents/team leads who never log into the system themselves
- Admin-operated accounting now follows the same rule: invited agents stay selectable in `/office/accounting` statement generation, because invitation status should not block office admins from doing payroll/commission operations for agents who never log in personally
- `/office/accounting` payee selection now follows office/global sales-membership scope instead of office-local `agent` only, so admins can search `agent / team_lead` memberships in `active` or `invited` state, including company-level records with `officeId = null`
- `/office/accounting` statement generation now follows the invoice-number workflow: admins load an agent's eligible invoice candidates from the transaction `invoiceNumber` field, preview the rows under the selected invoice numbers if needed, optionally uncheck specific rows, and then generate the payout statement without relying on a date window
- `New Transaction > Agent Name` owner search now sources assignable owners from office/global sales memberships instead of the viewer's current transaction roster visibility, so admins can still find unassigned or not-yet-rostered agents and company-level members with `officeId = null`
- the transaction create flow now has an inline `Edit fields` modal for office admins in both `/office/transactions` create modal and `/office/transactions/new`:
  - it reuses the shared transaction field settings instead of creating a second page-only schema
  - after retiring the old compatibility / text-only finance bridge fields, the modal now shows the same active transaction schema that really drives create-time rendering and search/report defaults, while still allowing a new custom field to be created in place
  - schema changes made from that modal now update the live create form without clearing the draft values already typed into still-visible fields
  - `/office/transactions` now keeps those inline create-flow field edits in client state for the life of the page session, so closing and reopening `Create transaction` no longer snaps back to the stale schema from the original page load
- transaction field management is now fully operable from the create-flow `Edit fields` modal itself:
  - the embedded workspace reuses the full `Settings > Fields` transaction editor instead of a reduced checkbox-only version
  - built-in transaction field labels are now overrideable and persisted, so admins can rename display text such as `State` directly from the inline editor or the main settings page
  - report search fields, report columns, and CSV export headers now read those same shared labels instead of hardcoded report-only copies
  - field-setting mutation routes now read their updated transaction/contact/offer field snapshots inside the same Prisma transaction before responding, so inline editors no longer show the old field state on the first save attempt
  - custom fields that already have saved values now surface an explicit `Hide instead` path in the field editor, so admins see why delete is blocked and can remove the field from active forms without losing historical data
- Transaction status handling is now tightened around admin control:
  - `Create transaction` only allows `Pending / Closed / Cancelled`
  - non-admin creators are forced to `Pending`
  - non-admin users cannot change transaction status from either the standalone status card or the schema-driven intake editor
- `Create user` now supports immediate operational placement:
  - admins with team permissions can assign a new sales user into a top-level team or junior branch during invitation
  - the same create flow can optionally set the user's direct manager from current branch leaders
  - the unified user detail page continues to support later team reassignment or removal without a separate agent-only route
- `Settings > Users > [membership]` now keeps the `Teams` card available for admins with `teams:manage` even when the target member has no current branch visibility through the agent roster yet:
  - assignable team options come from the current office team-management scope instead of the viewer's agent roster visibility
  - members with `officeId = null` can still be assigned to the current office's teams from the user detail page
- Team / user hierarchy hardening now keeps agent roster/profile reads on `agents:view:*` scope instead of the transaction default, lets `Settings > Teams` honor `teams:view` directly without accidental agent-scope narrowing, and blocks non-`agent / team_lead` accounts from entering team hierarchy through create-user, team-owner, team-membership, or incompatible role-change writes
- User-access and accounting hardening now keeps admin-tier controls behind `owner / office_admin`, limits per-user permission overrides to `settings:manage`, removes sales-role access to `/office/accounting` while preserving `/office/billing`, and prevents transaction intake edits from silently overwriting finance columns without `transactions:finance`
- reports search-layout refinement now treats people-name filters as search-first pickers instead of raw dropdowns:
  - `Owner` and `Team Leader` use typed search and ranked matching so roster growth does not make filter selection unusable
  - this behavior is now documented as the default rule for future `owner / agent / member / assignee` selectors across Office pages
- existing role templates now receive a one-time signature-permission backfill migration so older organizations do not silently lose `Prepare signature` and signature-center access after the new e-signature permission tree shipped:
  - legacy templates that only carried `signatures:manage` now also receive the required `signatures:view` parent key plus the matching template/report keys for roles that should have them
  - this closes the upgrade gap where the new signature UI code could be deployed successfully while persisted organization role templates still hid the entry points
- transaction signature authoring is now a true two-step workflow instead of one mixed editor:
  - Step 1 saves `Recipients and delivery` first, including signers / approvers / CC recipients, routing steps, and invitation copy
  - Step 2 unlocks only after that save and focuses on PDF field placement, with each field explicitly bound to one signer or approver before send
- transaction detail now exposes an admin-only `Delete transaction` action:
  - `owner / office_admin` can remove a transaction directly from `/office/transactions/[transactionId]` with a confirmation dialog
  - the delete API now removes the transaction plus transaction-owned workspace rows, records a `transaction.deleted` audit event, cleans up stored transaction-document and signature-artifact files, and leaves any accounting rows in place with their `relatedTransactionId` cleared by the database
- typography baseline cleanup now routes legacy Office surfaces back through the shared `--office-font-sans` token:
  - body and older sidebar/panel/meta surfaces no longer hardcode `Inter / Helvetica Neue / Arial` locally
  - the shared font stack now includes explicit Chinese fallbacks to reduce cross-browser drift for mixed English/CJK pages
- office table-layout stability now applies saved column widths before first paint and scopes persisted widths by actual header signature instead of only the shared row class:
  - refreshes no longer need a client fetch to "snap back" from an incorrect default grid before saved widths apply
  - different tables that reused the same legacy row class no longer overwrite one another's saved widths when their columns differ
  - accounting statement candidate tables now use dedicated structural classes so first-time visitors also get a stable default grid before any manual resizing
- compact operational list cleanup now treats `QueueItem` as the shared replacement for old `office-note-item` rows on live pages:
  - `Front Office` rails now use the same compact operational row language instead of page-local left-tag cards
  - `Settings > Commission plans` and multiple agent/user detail sections now render compact queue-style rows for member defaults, split templates, recent calculations, recent transactions, and recent activity
  - `AGENTS.md` and the design-system rules now explicitly prohibit new `office-note-item` or `bm-*` expansion in touched UI surfaces
- FO dossier execution now starts to behave like a real CRM workbench instead of a read-only snapshot:
  - `/agent/clients/[clientId]` surfaces a workflow pressure signal plus a stage-derived suggested next step
  - agents can create shared follow-up tasks directly from the dossier through `/api/agent/clients/[clientId]/follow-up-tasks`
  - the task write reuses the existing `FollowUpTask` service and keeps `Client.nextFollowUpAt` synchronized when the new follow-up has a due date, so FO/BO reminder surfaces stay aligned
- FO follow-up execution now goes beyond creation:
  - `/api/agent/clients/[clientId]/follow-up-tasks/[taskId]` lets Front Office complete or push forward existing follow-up tasks without leaving the dossier
  - updating a follow-up now records audit activity and re-synchronizes `Client.nextFollowUpAt` when that task was driving the current next-touch signal
  - `/agent/dashboard` now shows overdue task counts, 15+ day stale counts, and a leadership pressure queue for `team_lead / office_admin / owner`, using team hierarchy for lead scope and office scope for admin/owner
- FO dossier return semantics now come from the shared data contract instead of page-local guesses:
  - `FrontOfficeClientDetailNextStepRailItem` now carries a `returnPoint + returnDescription` contract so the active workbench lane, dossier block jump target, and re-entry copy are all emitted from `@acre/db`
  - `/agent/clients/[clientId]` and its dossier client now consume that contract for current-section state, appointments/listing subtitles, and re-entry guidance instead of maintaining separate hardcoded appointment/listing return strings
- FO listing output now keeps the latest tracked-share execution checkpoint visible after refresh instead of collapsing back to aggregate counters only:
  - [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts) now exports a shared tracked-share execution summary builder so channel, binding mode, writeback scope, and next-step labels come from one contract
  - [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) now adds `latestTrackedShare` onto each listing card in the FO listings snapshot by resolving the viewer's most recent share link / send-record context for that listing
  - [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now shows that latest tracked-share checkpoint inline, including bound dossier / appointment jump-backs when the last share was client- or appointment-linked
- FO resource center now reads its execution-pressure summary from a shared snapshot contract instead of page-local heuristics:
  - [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) now emits `executionPulse` for `/agent/resources`, including resource-lane counts, strongest/thinnest lane, and vendor posture derived from the current resource/vendor set
  - [apps/web/app/api/resources/route.ts](../../apps/web/app/api/resources/route.ts) now returns the richer resource-center snapshot shape plus lightweight `type / vendorCategory / vendorMode` filtering so downstream consumers can request a narrowed FO resource payload without recreating summary logic
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now consumes that shared pulse contract for the command-deck rails instead of recomputing strongest/thinnest lane and vendor posture inside the page shell
- FO resource hub interactions are now auditable instead of disappearing into raw outbound links:
  - [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts) now validates office-scoped resource/vendor visibility and records `resource_opened / vendor_clicked` events into the shared audit log contract
  - [packages/db/src/activity-log.ts](../../packages/db/src/activity-log.ts) now recognizes Front Office resource/vendor interaction events, maps them into activity summaries, and links them back to `/agent/resources`
  - [apps/web/app/api/resources/interactions/route.ts](../../apps/web/app/api/resources/interactions/route.ts) plus [front-office-tracked-link.tsx](../../apps/web/app/agent/_components/front-office-tracked-link.tsx) now send lightweight tracked opens/clicks before the real resource or vendor action continues
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now routes resource opens and vendor call/email/site actions through that tracked interaction path while keeping the existing FO hub layout intact
- FO resource hub tracking now surfaces back into the hub instead of living only in the audit trail:
  - [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts) now aggregates the current viewer's recent 14-day resource opens and vendor clicks into a lightweight interaction snapshot
  - [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) now includes that `interactionTracking` block in the shared FO resources snapshot, and [apps/web/app/api/resources/route.ts](../../apps/web/app/api/resources/route.ts) now returns it to API consumers too
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now shows tracked opens/clicks in summary chips and a `Recent tracked use` rail so the resource hub reflects live usage rather than acting like a static library
- FO notifications now read more like an execution workbench than a passive inbox:
  - [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), [agent-notifications-config.ts](../../apps/web/app/agent/notifications/agent-notifications-config.ts), and [agent-notifications.module.css](../../apps/web/app/agent/notifications/agent-notifications.module.css) now foreground `Focus lane / Next move / Route contract` and tighten the language around personal cleanup, team cleanup, reminders, and notices
- FO listings now surface a stronger tracked-share usage pulse instead of relying on raw counts alone:
  - [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts) now builds a listings usage pulse summary for the current desk
  - [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx) and [front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now use that pulse in the rail and tracked-share cards so engaged trails, quiet trails, and strongest tracked lanes read like an operator signal instead of a sparse metric block
- FO resources now support tracked hub search in addition to opens and vendor clicks:
  - [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts) now records `front_office.resource_searched`, includes search counts in the 14-day interaction snapshot, and classifies recent tracked use as `Resource search / Resource open / Vendor click`
  - [packages/db/src/activity-log.ts](../../packages/db/src/activity-log.ts) now recognizes tracked FO hub searches in the shared audit summaries
  - [apps/web/app/api/resources/interactions/route.ts](../../apps/web/app/api/resources/interactions/route.ts), [apps/web/app/api/resources/route.ts](../../apps/web/app/api/resources/route.ts), [apps/web/app/agent/resources/front-office-resource-search-form.tsx](../../apps/web/app/agent/resources/front-office-resource-search-form.tsx), and [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now provide a real `Search this hub` entry, tracked search events, and a search-results section without collapsing the rest of the resource workbench into a generic filter page
- FO training resources now track progress instead of stopping at the first open:
  - [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts), [packages/db/src/activity-log.ts](../../packages/db/src/activity-log.ts), and [apps/web/app/api/resources/interactions/route.ts](../../apps/web/app/api/resources/interactions/route.ts) now support `front_office.resource_progress_logged` milestones for training videos, including `25% / 50% / complete` tracking without changing schema
  - [apps/web/app/agent/resources/front-office-resource-progress-actions.tsx](../../apps/web/app/agent/resources/front-office-resource-progress-actions.tsx) and [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now let agents log manual training progress from the same FO hub and surface those milestones back into `Recent tracked use` and the resource summary chips
- FO intake assist now behaves more like a real unresolved-first review bench instead of a loose OCR preview:
  - [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts) now tightens low-signal guidance, transcript coaching, and readiness summaries around unresolved-first review rather than generic safe-first copy
  - [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) and [apps/web/app/agent/\_components/front-office-lead-intake-review.ts](../../apps/web/app/agent/_components/front-office-lead-intake-review.ts) now reinforce section-by-section batch cues so identity clears before qualification, timing, and any safe-after-review apply pass
- FO dashboard now reads as a leadership command deck instead of a generic launchpad:
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [packages/db/src/front-office-dashboard.test.ts](../../packages/db/src/front-office-dashboard.test.ts), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), and [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx) now frame `/agent/dashboard` as the FO command deck, with stronger `lead with / command center / approve next move` language and regression coverage for office-admin leadership semantics
- FO dossier mid-funnel work now carries stronger operator framing all the way through PDF export:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx), and [apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx](../../apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx) now deepen the `offer / negotiation`, `inspection / contract support`, and `closing / win suggestion` sections so the live dossier and exported client summary both carry explicit next-move and operator-frame guidance without introducing new schema
- FO calendar coordination now reads more like a checkpoint workbench than a loose bridge log:
  - [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts) and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) now sharpen the `confirmation / reschedule / promised next touch / bridge-opened` language so the live calendar makes external coordination pressure and the next checkpoint easier to scan without implying provider-owned sync
- FO listings follow-through now makes the rescue lane and recent timeline more explicit:
  - [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts), [packages/db/src/front-office-listing-output.test.ts](../../packages/db/src/front-office-listing-output.test.ts), [apps/web/app/agent/listings/page.tsx](../../apps/web/app/agent/listings/page.tsx), and [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now push `quiet trail rescue`, `strongest trail`, and `recent tracked-share timeline` into a clearer operator-facing lane instead of leaving them as sparse metric copy
- FO resource adoption pulse now goes deeper than raw totals while staying inside the current hub:
  - [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts), [packages/db/src/front-office-resources.test.ts](../../packages/db/src/front-office-resources.test.ts), [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), and [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now deepen the shared adoption pulse with stronger `search / progress / open / click`, `top actors`, and `hottest targets` semantics without splitting off a separate analytics module
- FO notifications now present a clearer cleanup command surface:
  - [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), [apps/web/app/agent/notifications/agent-notifications-config.ts](../../apps/web/app/agent/notifications/agent-notifications-config.ts), and [apps/web/app/agent/notifications/agent-notifications.module.css](../../apps/web/app/agent/notifications/agent-notifications.module.css) now tighten the copy and lane framing around personal cleanup, team pressure, appointment reminders, and general notices so the page reads like a command surface instead of a mixed inbox
- FO dashboard command deck now has a stronger ordering contract:
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [packages/db/src/front-office-dashboard.test.ts](../../packages/db/src/front-office-dashboard.test.ts), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), and [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx) now sharpen the `command lead / leadership pressure / launchpad order / AI next-touch` language so the dashboard reads more like a sequenced FO command deck than a generic summary board
- FO clients index now behaves more like a workspace anchor than a plain client list:
  - [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts) and [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx) now deepen the workspace-anchor semantics around cleanup, next queue move, and stable return slices so `/agent/clients` can act as a more reliable FO re-entry surface
- FO dossier now draws a clearer BO source-of-truth boundary around closing and handoff:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx), and [apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx](../../apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx) now reinforce that formal closing, handoff, and milestone truth still lives in Back Office even while the FO dossier explains the next move and operator frame
- FO intake assist now gives operators clearer review guidance without promising provider-backed ingestion:
  - [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts), [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx), and [apps/web/app/agent/\_components/front-office-lead-intake-review.ts](../../apps/web/app/agent/_components/front-office-lead-intake-review.ts) now tighten unresolved-first review guidance, low-signal coaching, and operator-facing copy while still keeping the feature firmly browser-side and manual-review-first
- FO listing share creation and public share viewing now share one clearer client-facing promise:
  - [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts), [packages/db/src/front-office-listing-output.test.ts](../../packages/db/src/front-office-listing-output.test.ts), [apps/web/app/api/agent/listings/[listingId]/share-links/route.ts](../../apps/web/app/api/agent/listings/[listingId]/share-links/route.ts), and [apps/web/app/share/listings/[code]/page.tsx](../../apps/web/app/share/listings/[code]/page.tsx) now carry the same `private share / reply lane / privacy / next step` semantics, so tracked client shares, appointment-linked follow-through shares, and generic private links all explain the same client-facing promise without exposing Back Office language
- FO appointment bridge APIs now expose a tighter checkpoint contract without pretending there is provider-owned sync:
  - [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts), [apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts), and [apps/web/app/api/agent/appointments/[appointmentId]/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/route.ts) now return clearer `bridge / writeback / checkpoint / next touch` semantics so the API surface matches the FO calendar workbench language
- FO duplicate merge now gives the operator a more explicit way back into the correct clients workbench slice:
  - [apps/web/app/api/agent/clients/merge/route.ts](../../apps/web/app/api/agent/clients/merge/route.ts), [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), and [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx) now deepen duplicate-review return flow with explicit `returnToLabel / returnToHref` and stronger post-merge re-entry framing instead of dropping every merge back into the same generic clients list
- FO resource interaction tracking now speaks in richer audit summaries without changing the hub shell:
  - [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts) and [packages/db/src/activity-log.ts](../../packages/db/src/activity-log.ts) now deepen the event-summary language around resource search, progress, open, and vendor-click behavior so the underlying audit trail better matches the FO resource adoption pulse already visible in the hub
- FO intake assist now behaves more like a live intake review bench than a loose OCR preview:
  - [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts) and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) now tighten the unresolved-first bench with stronger low-signal rescue, review gating, and transcript/operator guidance while still keeping the feature firmly browser-side
- FO dossier mid-funnel return points now stay aligned between the live record and exported PDF:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx), and [apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx](../../apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx) now keep the `offer / inspection / closing` operator contract and return-point framing aligned across the dossier and client summary PDF instead of letting the export drift into weaker generic copy
- FO calendar workbench now carries a more explicit checkpoint-continuity contract:
  - [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts), [apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts), [apps/web/app/api/agent/appointments/[appointmentId]/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/route.ts), and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) now unify the language around bridge, writeback, promised next touch, and checkpoint continuity so the FO calendar reads like one manual coordination loop instead of separate bridge fragments
- FO listings now show the client-facing share promise before the operator copies anything:
  - [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now surfaces the `client-facing share contract` inside each listing card and in post-copy feedback, so the agent can see the public-share promise, reply lane, and privacy framing before and after creating a tracked share instead of discovering that continuity only on the client-facing page
- FO dashboard sequencing now reads more like a command deck than a flat pressure board:
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [packages/db/src/front-office-dashboard.test.ts](../../packages/db/src/front-office-dashboard.test.ts), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), and [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx) now add explicit sequencing labels so office-admin leaders can read the command deck in an ordered `what to lead with / what to sequence next` rhythm without implying automation
- FO notifications now behave more like a cleanup bench than a mixed feed:
  - [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) and [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx) now tighten the cleanup summary, reminder pressure, and lane language so the page reads like a focused operator bench instead of a loose inbox
- FO clients index now gives duplicate-review and cleanup work a steadier way back into the right slice:
  - [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx), and [apps/web/app/api/agent/clients/merge/route.ts](../../apps/web/app/api/agent/clients/merge/route.ts) now deepen clients-workbench re-entry with clearer cleanup anchors and stronger post-merge return semantics instead of dropping everything back into the same generic queue slice
- FO listings now expose send risk directly instead of hiding it inside tracked-share totals:
  - [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts), [packages/db/src/front-office-listing-output.test.ts](../../packages/db/src/front-office-listing-output.test.ts), and [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now surface `send-risk watch` semantics at both the desk pulse and per-listing level, making `unopened risk`, `quiet-after-open`, and `managed risk` visible before the operator chooses the next rescue move
- FO intake review now reads more like a bounded execution bench than a loose OCR preview:
  - [apps/web/app/agent/\_components/front-office-lead-intake-assist.ts](../../apps/web/app/agent/_components/front-office-lead-intake-assist.ts), [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx), and [apps/web/app/agent/\_components/front-office-lead-intake-review.ts](../../apps/web/app/agent/_components/front-office-lead-intake-review.ts) now tighten unresolved-first ordering, low-signal rescue copy, and transcript review guidance so the same browser-side assist behaves more like a real intake workbench without pretending Acre owns provider-backed ingestion
- FO dossier mid-funnel now keeps live record and PDF export on the same operator contract:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx), and [apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx](../../apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx) now align `offer / inspection / closing` around the same `source of truth + next move + return point` language, so the PDF no longer drifts behind the live dossier in the same mid-funnel sections
- FO calendar follow-through now reads as one tighter manual checkpoint loop:
  - [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts), [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), and [apps/web/app/api/agent/appointments/[appointmentId]/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/route.ts) now tighten the semantics around follow-through checkpoints, promised touches, and the next manual move after an outside coordination jump, while still clearly staying on the `bridge + writeback` side of the FO / provider boundary
- FO resources now surface a clearer operator signal without becoming a separate analytics product:
  - [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts), [packages/db/src/front-office-resources.test.ts](../../packages/db/src/front-office-resources.test.ts), [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx), and [apps/web/app/agent/resources/front-office-resource-progress-actions.tsx](../../apps/web/app/agent/resources/front-office-resource-progress-actions.tsx) now convert raw `search / progress / open / click` counts into `operator signal` language such as `Search-led`, `Training follow-through`, `Vendor-led`, and `Balanced`, so both personal and shared resource views read like actionable FO hub signals instead of a flat count board
- FO dashboard now makes safe actions and queue order more explicit:
  - [packages/db/src/front-office-dashboard.ts](../../packages/db/src/front-office-dashboard.ts), [packages/db/src/front-office-dashboard.test.ts](../../packages/db/src/front-office-dashboard.test.ts), [apps/web/app/agent/dashboard/page.tsx](../../apps/web/app/agent/dashboard/page.tsx), and [apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx](../../apps/web/app/agent/dashboard/front-office-dashboard-ai-queue-client.tsx) now tighten the `safe action / why now / sequence contract` framing, so command-deck priorities read like explicit next moves instead of a softer AI summary layer
- FO notifications now surface target semantics more clearly inside the cleanup center:
  - [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx), [apps/web/app/agent/notifications/agent-notifications-client.tsx](../../apps/web/app/agent/notifications/agent-notifications-client.tsx), and [apps/web/app/agent/notifications/agent-notifications-config.ts](../../apps/web/app/agent/notifications/agent-notifications-config.ts) now distinguish `section target`, `lane contract`, and `next move` more explicitly, so the route reads more like a command surface with visible destinations instead of a mixed feed of notices
- FO clients workspace now re-enters through section-aware anchors instead of lane-only hints:
  - [packages/db/src/front-office-workspaces.ts](../../packages/db/src/front-office-workspaces.ts), [apps/web/app/agent/clients/page.tsx](../../apps/web/app/agent/clients/page.tsx), and [apps/web/app/api/agent/clients/merge/route.ts](../../apps/web/app/api/agent/clients/merge/route.ts) now return and render `returnSectionLabel / returnSectionHref / returnSectionDescription`, so duplicate-review and cleanup flows can point back to a specific FO section as well as a lane
- FO listings now align the client-facing share promise with the public share page contract:
  - [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts), [packages/db/src/front-office-listing-output.test.ts](../../packages/db/src/front-office-listing-output.test.ts), and [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx) now keep agent-side share promise copy aligned with the `publicPage` contract returned by share creation, and the tests now assert that `share.snapshot.publicPage` stays in lockstep with the actual public-share semantics
- FO dossier export continuity now keeps the live record and client PDF on one clearer mid-funnel contract:
  - [packages/db/src/front-office-clients.ts](../../packages/db/src/front-office-clients.ts), [apps/web/app/agent/clients/[clientId]/page.tsx](../../apps/web/app/agent/clients/[clientId]/page.tsx), [apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx](../../apps/web/app/agent/clients/[clientId]/front-office-client-dossier-shared.tsx), and [apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx](../../apps/web/app/api/agent/clients/[clientId]/pdf/front-office-client-summary-pdf.tsx) now tighten the shared `offer / inspection / create / export / client-ready view` framing so the dossier and exported summary point to the same next move without pretending Back Office formal workflow already lives in Front Office
- FO calendar bridge continuity now carries a clearer manual checkpoint loop end to end:
  - [packages/db/src/front-office-appointments.ts](../../packages/db/src/front-office-appointments.ts), [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), [apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/bridge/route.ts), and [apps/web/app/api/agent/appointments/[appointmentId]/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/route.ts) now deepen `continuity / next-step / return-to-writeback` semantics after a Google / Outlook / ICS / email bridge so the operator can reopen the same manual loop without implying provider-owned sync
- FO listing-share promise now uses a client-safe single-source contract instead of importing the DB module into the browser bundle:
  - [packages/db/src/front-office-listing-share-promise.ts](../../packages/db/src/front-office-listing-share-promise.ts), [packages/db/src/front-office-listing-output.ts](../../packages/db/src/front-office-listing-output.ts), [apps/web/app/agent/listings/front-office-listings-output-client.tsx](../../apps/web/app/agent/listings/front-office-listings-output-client.tsx), [apps/web/app/share/listings/[code]/page.tsx](../../apps/web/app/share/listings/[code]/page.tsx), and [packages/db/src/front-office-listing-output.test.ts](../../packages/db/src/front-office-listing-output.test.ts) now share the same pure `public/share promise` helper, so agent preview, share creation, and public page language stay aligned without pulling Prisma/server-only dependencies into the Next client build
- FO intake assist now has a server-side OCR ingress path instead of forcing image extraction in the browser:
  - [apps/web/app/api/agent/clients/intake-assist/route.ts](../../apps/web/app/api/agent/clients/intake-assist/route.ts), [apps/web/lib/front-office-intake-assist-server.ts](../../apps/web/lib/front-office-intake-assist-server.ts), [apps/web/lib/front-office-intake-assist-server.test.ts](../../apps/web/lib/front-office-intake-assist-server.test.ts), and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) now move screenshot OCR submission onto a Node route, keep transcript+image normalization on the server, and return the same review-oriented raw text contract back to the existing FO intake bench without pretending a provider-backed ingest pipeline exists yet
- FO calendar now supports an internal Acre mail-thread continuity path for appointment briefs:
  - [packages/db/src/mail.ts](../../packages/db/src/mail.ts), [apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts), [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx), and [apps/web/app/agent/calendar/page.tsx](../../apps/web/app/agent/calendar/page.tsx) now let scheduled appointments open an internal Acre mail thread that preserves the same manual brief context and returns the operator back to writeback, while keeping the external email send explicitly manual instead of claiming true two-way email sync
- FO cleanup now has a reusable digest foundation and a manual runner entrypoint:
  - [packages/db/src/front-office-cleanup-digest.ts](../../packages/db/src/front-office-cleanup-digest.ts), [packages/db/src/front-office-cleanup-digest.test.ts](../../packages/db/src/front-office-cleanup-digest.test.ts), and [scripts/front-office-cleanup-digest.ts](../../scripts/front-office-cleanup-digest.ts) now build a 7-day FO cleanup summary across unread notifications, due follow-up tasks, client reminder pressure, and appointment continuity signals, giving future automation/job-runner work one shared digest contract instead of scattering cleanup logic across route shells
- FO notifications now expose the cleanup digest without collapsing the existing workbench:
  - [packages/db/src/index.ts](../../packages/db/src/index.ts), [apps/web/app/api/agent/notifications/cleanup-digest/route.ts](../../apps/web/app/api/agent/notifications/cleanup-digest/route.ts), and [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) now expose the phase11 cleanup digest through a protected `no-store` API route and a lightweight rail summary inside the existing FO activity command surface, so operators can read the live digest and its next move without replacing the current notifications workbench or pretending a scheduler already exists
- FO appointment mail continuity now uses a shared contract instead of route-local string assembly:
  - [packages/db/src/mail.ts](../../packages/db/src/mail.ts), [packages/db/src/mail.test.ts](../../packages/db/src/mail.test.ts), [apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts), and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) now centralize the manual-only continuity response, lock the guarded error mapping in tests, and keep the calendar client aligned with one stable `internal thread + return to writeback` contract without implying true two-way email sync
- FO intake server ingest now guards empty payloads, oversized screenshots, and transcript fallback explicitly:
  - [apps/web/lib/front-office-intake-assist-server.ts](../../apps/web/lib/front-office-intake-assist-server.ts), [apps/web/lib/front-office-intake-assist-server.test.ts](../../apps/web/lib/front-office-intake-assist-server.test.ts), [apps/web/app/api/agent/clients/intake-assist/route.ts](../../apps/web/app/api/agent/clients/intake-assist/route.ts), and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) now reject empty or oversized server-ingest requests earlier, preserve a clear transcript fallback signal when OCR fails on hybrid submissions, and surface the same honest server-boundary language back in the FO intake bench
- FO cleanup digest now has reusable render helpers instead of only raw snapshot data:
  - [packages/db/src/front-office-cleanup-digest.ts](../../packages/db/src/front-office-cleanup-digest.ts), [packages/db/src/front-office-cleanup-digest.test.ts](../../packages/db/src/front-office-cleanup-digest.test.ts), and [scripts/front-office-cleanup-digest.ts](../../scripts/front-office-cleanup-digest.ts) now expose stable operator-facing section rendering and summary helpers, so future manual runs or scheduled automation can reuse one digest presentation contract instead of rebuilding human-readable output in each surface
- FO notifications now expose the cleanup digest through a dedicated reusable card:
  - [apps/web/app/agent/notifications/front-office-cleanup-digest-card.tsx](../../apps/web/app/agent/notifications/front-office-cleanup-digest-card.tsx) and [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) now keep the existing notifications workbench intact while surfacing manual-mode cleanup digest stats, refresh, and JSON access in a tighter FO rail instead of leaving digest output as loose inline markup
- FO intake assist route now has first-class contract coverage instead of relying on the parser tests alone:
  - [apps/web/app/api/agent/clients/intake-assist/route.ts](../../apps/web/app/api/agent/clients/intake-assist/route.ts), [apps/web/app/api/agent/clients/intake-assist/route.test.ts](../../apps/web/app/api/agent/clients/intake-assist/route.test.ts), [apps/web/lib/front-office-intake-assist-server.ts](../../apps/web/lib/front-office-intake-assist-server.ts), and [apps/web/lib/front-office-intake-assist-server.test.ts](../../apps/web/lib/front-office-intake-assist-server.test.ts) now lock the server route behind one testable helper that covers `401 / 403 / 400 / 413 / 200` and transcript fallback behavior, so the FO ingest boundary can evolve without route-only regressions
- FO appointment mail continuity now carries a clearer action-target return contract:
  - [packages/db/src/mail.ts](../../packages/db/src/mail.ts), [packages/db/src/mail.test.ts](../../packages/db/src/mail.test.ts), [apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts), and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) now return explicit `actionTargetUrl`, `actionTargetLabel`, and `continuity.returnToUrl` values, so opening an internal mail thread keeps the operator anchored to the same appointment follow-through loop without implying true two-way mail sync
- FO cleanup digest now has a delivery-draft contract for future mail or automation surfaces:
  - [packages/db/src/front-office-cleanup-digest.ts](../../packages/db/src/front-office-cleanup-digest.ts), [packages/db/src/front-office-cleanup-digest.test.ts](../../packages/db/src/front-office-cleanup-digest.test.ts), [packages/db/src/index.ts](../../packages/db/src/index.ts), and [scripts/front-office-cleanup-digest.ts](../../scripts/front-office-cleanup-digest.ts) now expose a stable `delivery draft` with reusable `subject / summary / body / runSummary` fields, so future job-runner or internal mail delivery work can reuse one digest contract instead of rebuilding wording at each surface
- FO appointment internal mail continuity now has route-level coverage instead of relying on shared mail tests alone:
  - [apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts) and [apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.test.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.test.ts) now lock the `401 / 403 / 404 / 409 / 201` contract, `no-store` response, and payload shape behind a testable route helper, so manual continuity can evolve without route-only regressions
- FO intake assist now reports source provenance and warnings as part of the server-ingest contract:
  - [apps/web/lib/front-office-intake-assist-server.ts](../../apps/web/lib/front-office-intake-assist-server.ts), [apps/web/lib/front-office-intake-assist-server.test.ts](../../apps/web/lib/front-office-intake-assist-server.test.ts), [apps/web/app/api/agent/clients/intake-assist/route.test.ts](../../apps/web/app/api/agent/clients/intake-assist/route.test.ts), and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) now return and surface explicit `metadata.provenance` and `metadata.warnings`, so transcript-only, image-only, OCR-failed, transcript-fallback, and empty-source cases remain honest without implying provider-backed ingestion
- FO notifications can now open the cleanup digest as an internal Acre mail thread while staying manual-only:
  - [apps/web/app/api/agent/notifications/cleanup-digest/route.ts](../../apps/web/app/api/agent/notifications/cleanup-digest/route.ts), [apps/web/app/api/agent/notifications/cleanup-digest/mail-thread/route.ts](../../apps/web/app/api/agent/notifications/cleanup-digest/mail-thread/route.ts), [apps/web/app/api/agent/notifications/cleanup-digest/mail-thread/route.test.ts](../../apps/web/app/api/agent/notifications/cleanup-digest/mail-thread/route.test.ts), [apps/web/app/agent/notifications/front-office-cleanup-digest-card.tsx](../../apps/web/app/agent/notifications/front-office-cleanup-digest-card.tsx), and [apps/web/app/agent/notifications/page.tsx](../../apps/web/app/agent/notifications/page.tsx) now let the operator open an internal cleanup-digest thread with one guarded route and a card action, while keeping the notifications workbench intact and explicitly preserving the `manual-only / no scheduler / no provider sync` boundary
- FO cleanup digest now records manual runs and internal-thread continuity in the shared activity log instead of leaving those manual steps invisible:
  - [packages/db/src/activity-log.ts](../../packages/db/src/activity-log.ts), [packages/db/src/front-office-cleanup-digest.ts](../../packages/db/src/front-office-cleanup-digest.ts), [packages/db/src/front-office-cleanup-digest.test.ts](../../packages/db/src/front-office-cleanup-digest.test.ts), [packages/db/src/index.ts](../../packages/db/src/index.ts), [apps/web/app/api/agent/notifications/cleanup-digest/route.ts](../../apps/web/app/api/agent/notifications/cleanup-digest/route.ts), [apps/web/app/api/agent/notifications/cleanup-digest/route.test.ts](../../apps/web/app/api/agent/notifications/cleanup-digest/route.test.ts), [apps/web/app/api/agent/notifications/cleanup-digest/mail-thread/route.ts](../../apps/web/app/api/agent/notifications/cleanup-digest/mail-thread/route.ts), [apps/web/app/api/agent/notifications/cleanup-digest/mail-thread/route.test.ts](../../apps/web/app/api/agent/notifications/cleanup-digest/mail-thread/route.test.ts), and [apps/web/app/agent/notifications/front-office-cleanup-digest-card.tsx](../../apps/web/app/agent/notifications/front-office-cleanup-digest-card.tsx) now give manual digest runs and cleanup-thread openings first-class audit events plus an explicit `Run manual digest` action, so FO cleanup continuity can be reviewed later without pretending a scheduler or provider sync already exists
- FO intake OCR now goes through a dedicated local adapter boundary instead of mixing normalization, metadata, and Tesseract calls inside one server helper:
  - [apps/web/lib/front-office-intake-ocr.ts](../../apps/web/lib/front-office-intake-ocr.ts), [apps/web/lib/front-office-intake-assist-server.ts](../../apps/web/lib/front-office-intake-assist-server.ts), [apps/web/lib/front-office-intake-assist-server.test.ts](../../apps/web/lib/front-office-intake-assist-server.test.ts), [apps/web/app/api/agent/clients/intake-assist/route.test.ts](../../apps/web/app/api/agent/clients/intake-assist/route.test.ts), and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) now expose explicit `local_tesseract / server_side / transcript fallback` metadata, making the current local-only OCR path more modular and more honest about its non-provider-backed boundary
- FO cleanup digest runner now has an explicit runner contract instead of one implicit script output path:
  - [packages/db/src/front-office-cleanup-digest.ts](../../packages/db/src/front-office-cleanup-digest.ts), [packages/db/src/front-office-cleanup-digest.test.ts](../../packages/db/src/front-office-cleanup-digest.test.ts), and [scripts/front-office-cleanup-digest.ts](../../scripts/front-office-cleanup-digest.ts) now expose explicit `report / dry-run / json` output modes plus a reusable `runner contract`, so future scheduler or job-runner work can consume one manual-only contract instead of scraping free-form CLI output
- FO appointment internal mail continuity now writes its own appointment-level activity event instead of only relying on generic mail-thread creation:
  - [packages/db/src/activity-log.ts](../../packages/db/src/activity-log.ts), [packages/db/src/mail.ts](../../packages/db/src/mail.ts), [packages/db/src/mail.test.ts](../../packages/db/src/mail.test.ts), [packages/db/src/index.ts](../../packages/db/src/index.ts), [apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.ts), [apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.test.ts](../../apps/web/app/api/agent/appointments/[appointmentId]/mail-thread/route.test.ts), and [apps/web/app/agent/calendar/front-office-calendar-client.tsx](../../apps/web/app/agent/calendar/front-office-calendar-client.tsx) now record `appointment.internal_mail_thread_opened` with manual-only continuity details, so the operator can audit appointment brief handoffs separately from the generic office-mail event stream
- FO intake OCR now resolves through a richer local-only capability contract instead of a flat provider/mode pair:
  - [apps/web/lib/front-office-intake-ocr.ts](../../apps/web/lib/front-office-intake-ocr.ts), [apps/web/lib/front-office-intake-assist-server.ts](../../apps/web/lib/front-office-intake-assist-server.ts), [apps/web/lib/front-office-intake-assist-server.test.ts](../../apps/web/lib/front-office-intake-assist-server.test.ts), [apps/web/app/api/agent/clients/intake-assist/route.test.ts](../../apps/web/app/api/agent/clients/intake-assist/route.test.ts), and [apps/web/app/agent/\_components/front-office-lead-intake-card.tsx](../../apps/web/app/agent/_components/front-office-lead-intake-card.tsx) now return a `local_only` resolver contract with accepted MIME types, max image size, provider chain, and fallback story, so the current OCR path is easier to extend later without pretending provider-backed ingestion already exists

## Next recommended work

- Continue product and system cleanup before adding more large modules
- Remove remaining mixed legacy assumptions from docs, specs, and operational notes
- Tighten permissions, audit coverage, admin-managed configuration, and workflow state fidelity in existing modules
- Keep hardening the minimal internal account system before adding forgot-password, email delivery, or 2FA
- Keep filling practical module specs so future Codex runs can rely on repo docs instead of chat history
- Plan future storage, job-runner, and integration upgrades only after the current single-Droplet baseline is fully normalized

## Known limitations / open issues

- Some routes and UI areas still carry transitional patterns even though the main Back Office flows are already Prisma-backed
- After Prisma schema/client changes, the running Next dev server still needs an explicit restart or it may hold a stale Prisma Client in memory
- The active deployment is still a simple single-Droplet line with no staging environment and no object storage
- Background job infrastructure is not yet established, so some reminders/automation remain manual or request-time only
- External integrations remain intentionally absent:
  - MLS ingestion
  - external eSignature vendors
  - QuickBooks sync
  - ACH / payout processor execution
  - OCR or third-party document ingestion

## Update rule

When a major feature, baseline, or deployment truth changes:

1. update this file in the same task
2. update `docs/deployment.md` if runtime or environment truth changed
3. keep this log concise, operational, and current

- Front Office `Resources` training videos now open inside a single lazy-loaded full-screen player instead of sending agents straight out to a new tab or preloading many embeds in the list:
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) and [apps/web/app/agent/resources/front-office-training-gallery.tsx](../../apps/web/app/agent/resources/front-office-training-gallery.tsx) now keep `Video Academy` as a lightweight card list, open one browser-style full-screen overlay at a time, lazy-load the active YouTube iframe only after click, lock background scroll while the player is open, and keep a direct `Open on YouTube` fallback inside the player chrome
- Front Office `Video Academy` now reads like a thumbnail gallery instead of a document list:
  - [apps/web/app/agent/resources/front-office-training-gallery.tsx](../../apps/web/app/agent/resources/front-office-training-gallery.tsx) now renders clickable YouTube covers in a paginated grid, keeps the training list capped at `12` videos per page, shows `Page X of Y` navigation, and opens the same full-screen player from the cover tile instead of from a text-heavy row card
- Front Office `Resources` tab browsing now uses the same paginated rhythm across all three sections:
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now applies shared `page` query-param pagination to `Documents`, `Vendors`, and `Video Academy`, keeps each tab capped at `12` results per page, and preserves the current search term while agents move between page numbers
- Front Office `Documents` and `Vendors` now use the same compact directory-card rhythm as the rest of Resources instead of taller detail-heavy rows:
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now renders both sections in the same multi-column grid, clamps long summaries, keeps card heights steadier, and switches vendor quick actions from inline text links to small directory-style action buttons so the three tabs feel like one consistent searchable library
- Front Office `Resources` content stats now stay out of the way instead of dominating the section header:
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now replaces the larger top-of-section stat cards with a lighter inline status-strip so the result grid remains the visual focus while still showing current-page, matching, and library-total counts
- Front Office `Video Academy` pagination now avoids the false “last row means no more content” feeling on wide desktop screens:
  - [apps/web/app/agent/resources/front-office-training-gallery.tsx](../../apps/web/app/agent/resources/front-office-training-gallery.tsx) now uses stable responsive column counts instead of auto-fit, so `12` videos land as a full desktop grid instead of a sparse trailing row, while [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now shows more page numbers up front (`1 2 3 4 5 ... last`) before collapsing into gaps
- Front Office `Resources & Training` no longer repeats the old top summary counters above the actual directory content:
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx) now removes the redundant `Documents / Training / Vendors` summary strip from the page template so the first visual focus is the tab switcher and the directory itself
- Front Office Resources and Training now share one route again, but use internal tabs instead of a separate sidebar module:
  - [apps/web/app/agent/resources/page.tsx](../../apps/web/app/agent/resources/page.tsx), [apps/web/app/agent/resources/front-office-resource-search-form.tsx](../../apps/web/app/agent/resources/front-office-resource-search-form.tsx), [apps/web/app/agent/training/page.tsx](../../apps/web/app/agent/training/page.tsx), [apps/web/app/agent/agent-nav.tsx](../../apps/web/app/agent/agent-nav.tsx), [apps/web/app/office/resources/page.tsx](../../apps/web/app/office/resources/page.tsx), [apps/web/app/office/resources/office-resources-client.tsx](../../apps/web/app/office/resources/office-resources-client.tsx), [apps/web/app/office/training/page.tsx](../../apps/web/app/office/training/page.tsx), [apps/web/app/office/office-nav.tsx](../../apps/web/app/office/office-nav.tsx), [packages/db/src/front-office-resources.ts](../../packages/db/src/front-office-resources.ts), and [apps/web/app/api/resources/interactions/route.ts](../../apps/web/app/api/resources/interactions/route.ts) now keep `Resources` as the only sidebar entry, add page-internal tabs for `Documents / Vendors / Training`, route all training context back to `?tab=training`, and leave the old `/agent/training` + `/office/training` routes as compatibility redirects instead of primary entry points

- Listing Studio detail page now moves listed-style actions into the media stage and opens a full-screen edit workspace instead of the old packet modal:
  - [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx), [apps/web/app/api/listing-studio/listings/[packId]/route.ts](../../apps/web/app/api/listing-studio/listings/[packId]/route.ts), [packages/db/src/studio-listings.ts](../../packages/db/src/studio-listings.ts), and [apps/web/app/globals.css](../../apps/web/app/globals.css) now render four in-image action buttons (`collection / share / source / edit`), add a listed-style full-screen edit surface for address, details, media, and amenity editing, and expand the listing PATCH flow so those edits persist back into the saved listing snapshot instead of only updating Acre packet copy
- Listing Studio edit media grid now follows the listed app interaction pattern instead of the old Acre button-heavy card controls:
  - [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx), [apps/web/app/globals.css](../../apps/web/app/globals.css), [apps/web/app/api/listing-studio/listings/[packId]/assets/route.ts](../../apps/web/app/api/listing-studio/listings/[packId]/assets/route.ts), [apps/web/app/api/listing-studio/listings/[packId]/assets/[assetId]/route.ts](../../apps/web/app/api/listing-studio/listings/[packId]/assets/[assetId]/route.ts), [packages/db/src/studio-listings.ts](../../packages/db/src/studio-listings.ts), and [packages/db/src/index.ts](../../packages/db/src/index.ts) now present media as a simple cover-aware tile grid, remove photos via a hover `×`, set cover by clicking a tile, and support real drag/click upload into Listing Studio instead of leaving the uploader as a non-functional placeholder
- Listing Studio edit page shell now follows the listed app composition more closely instead of reading like an Acre modal:
  - [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) and [apps/web/app/globals.css](../../apps/web/app/globals.css) now render the editor as a two-panel listed-style workspace with a left tool rail, simplified page title bar, tighter white section cards, and a footer layout that reads like the original listed edit flow rather than a generic fullscreen dialog
- Listing Studio edit page `Additional` block now follows the listed app field order instead of mixing amenities with side metadata:
  - [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) and [apps/web/app/globals.css](../../apps/web/app/globals.css) now render `Description`, `Building Amenities`, `Unit Number`, `Neighborhood`, and `Listing URL` in the same top-to-bottom rhythm as the listed reference, while the editor description fallback now prefers real listing copy or address text instead of leaking packet summary values like `499 units` into the description field
- Listing Studio detail page now uses a listed-style presentation shell and stronger fact/transit fallback logic instead of the earlier Acre-flavored summary layout:
  - [packages/db/src/studio-listings.ts](../../packages/db/src/studio-listings.ts), [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx), and [apps/web/app/globals.css](../../apps/web/app/globals.css) now fall back to transit lines extracted from raw detail sections when the canonical transit array is empty, render the listing inside a listed-style side-rail frame, promote the primary fact row to `Bedrooms / Bathrooms / Sqft / Availability`, show photo position inside the hero stage, and tighten amenity/transit styling so the internal detail view tracks the listed reference more closely
- Listing Studio detail page now prioritizes functional parity over further visual mimicry:
  - [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) and [apps/web/app/globals.css](../../apps/web/app/globals.css) now keep the existing Acre detail styling while adding missing consumer-detail behaviors, including hero previous/next photo controls, address copy, a stable four-card `Bedrooms / Bathrooms / Sqft / Availability` fact row, availability date formatting, amenity regrouping into the catalog sections used by the editor, and a client-side transit fallback that can still render nearby stations when the normalized transit array is empty
- Listing Studio detail page now trims non-listed sections out of the main consumer flow:
  - [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) no longer renders `Overview` or `Source facts` in the public-facing detail stack, and its financial highlight row is now limited to the `HOA / Common charges` and `Taxes` chips that match the target listing experience instead of showing extra operational labels in that slot
- Listing Studio detail page now keeps the HOA / Taxes chip row visible even when the source import is missing those values:
  - [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) always renders both chips in the summary block, formats bare numeric edits like `886` into `$886/mo`, and falls back to `—` when the current listing has not yet been enriched from the editor or parser
- Listing Studio detail page nearby transit now exposes the extra-station hint that existed in the reference listing flow:
  - [apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx](../../apps/web/app/listing-studio/listings/[packId]/listing-studio-detail-client.tsx) and [apps/web/app/globals.css](../../apps/web/app/globals.css) now render a transit icon inside each visible station row, cap the default station list at three entries, and show the `+ N more stations within 1km` footer when additional nearby stations exist beyond the initially visible rows
- Listing Studio extension capture now prefers richer structured fields and performs a one-time deep page sweep before save instead of relying only on shallow top-of-page text:
  - [apps/extension/src/content.js](../../apps/extension/src/content.js) now normalizes the first valid price token instead of concatenating multiple dollar amounts, extracts city/state/ZIP and coordinates more reliably, prefers real listing description text over placeholder counts like `499 units`, widens `sqft / availability / HOA / taxes` regex coverage, collects `Nearby Transit / Transportation / Subway` sections into both canonical transit items and detail sections, attempts to capture floor-plan assets from labeled DOM nodes and embedded script URLs, filters floor-plan URLs out of the normal gallery asset set, and performs a one-time scroll-based deep capture before `Save to Acre` so StreetEasy content that only appears further down the page has a better chance of being included in the imported listing snapshot
